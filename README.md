# PIC-tutor

`PIC-tutor` 是一本面向深入学习 Particle-In-Cell 程序的书稿项目。核心目标是从同级目录 `../warpx` 的真实代码、官方文档、示例和参考文献出发，事无巨细地讲解 PIC 程序的物理基础、理论推导、数值算法、代码实现、模块架构、HPC 优化、多物理扩展、诊断分析和典型应用。

本项目不修改 `../warpx` 原仓库；所有书稿、计划、素材索引和后续脚本都应保存在 `PIC-tutor` 内。

## 当前状态

- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `warpx.n_current_deposition_buffer` 从 mask/分区摘要压到更精确的 coarse-buffer 沉积链：本轮补清了它不只在 `BuildBufferMasks()` 中决定 `current_buffer_masks` 的宽度，也会继续控制 `WarpX.cpp` 对 `current_buf / rho_buf / current_buffer_masks` 的分配、`PhysicalParticleContainer::PartitionParticlesInBuffers()` 对 fine-vs-buffer 沉积分流的判定，以及 `WarpXEvolve.cpp` 把这些 buffer 送入 `AddCurrentFromFineLevelandSumBoundary(...) / AddRhoFromFineLevelandSumBoundary(...)` 做 coarse-level 汇总的后续路径。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `warpx.refine_plasma` 从条件摘要压到更精确的 refined continuous-injection 运行链：本轮补清了它不只在 `PhysicalParticleContainer::findRefinedInjectionBox()` 中建立 coarse 级 `fine_injection_box + rrfac`，还会继续进入 `AddParticles.cpp::AddPlasma()` 的局部 `num_ppc * rrfac` 倍增与 `inj_pos->getPositionUnitBox(..., rrfac, ...)` 位置加密链，以及 `AddPlasmaFlux()` 的 `compute_area_weights(rrfac, flux_normal_axis)` 面积权重修正和 refined `flux_pos` 采样链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `warpx.ref_patch_function(x,y,z)` 从 parser 构造摘要压到更精确的 refined-patch 打标链：本轮补清了它不只在 `WarpX::ReadParameters()` 中通过 `makeParser({"x","y","z"})` 建成 `ref_patch_parser`，还会继续进入 `WarpX::ErrorEst()` 编译成 `ParserExecutor<3>`，再按 3D/XZ/RZ/1D_Z/RCYLINDER/RSPHERE 各自的坐标映射逐 cell 求值，只有返回 `1` 时才真正把该 cell 标成 `TagBox::SET`；若 parser 不存在，`ErrorEst()` 才退回 `fine_tag_lo/fine_tag_hi` 的 bbox 判定。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `warpx.do_moving_window` 从总开关摘要压到更精确的 runtime gate：本轮补清了它不只在 `read_moving_window_parameters()` 中决定是否继续读取方向、速度和起止 step，还会继续进入 `WarpX.H` 的 `moving_window_active(step)`、`WarpX.cpp/WarpXMovingWindow.cpp` 的 `moving_window_x` 与注入位置更新链、`WarpXEvolve.cpp` 对 continuous-injection/refined-injection 分支的调度，以及 `GuardCellManager` 的 guard-cell 预算和 `BTDiagnostics` 的兼容性硬约束。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `warpx.break_signals / checkpoint_signals` 从配置表说明压到更精确的 signal-handling 运行链：本轮补清了它们不只在 `WarpX::ReadParameters()` 中把字符串写进 `signal_conf_requests[...]`，还会继续进入 `SignalHandling::InitSignalHandling()` 的 `sigaction` 安装、`CheckSignals()/WaitSignals()` 的 rank-0 接收与 MPI 广播链，以及 `WarpX::checkStopSimulation()` 的主 loop 退出条件和 `WarpX::HandleSignals()` 的 `FilterComputePackFlushLastTimestep(...)`/`oncheckpointsignal` 回调路径。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `amrex.abort_on_out_of_gpu_memory` 从泛化性能说明压到更精确的 startup 边界：本轮补清了它不只是“GPU 内存不够时是否回退到 host”的通用 AMReX 文档参数，而是在 `WarpXAMReXInit.cpp` 的 `overwrite_amrex_parser_defaults()` 中通过 `queryAdd("abort_on_out_of_gpu_memory", true)` 主动把 AMReX 默认 `false` 覆写成 WarpX 默认 `true`，并经 `amrex::Initialize(...)` 在 startup 早期生效；因此它在 `parameter-map` 中现在明确落成“WarpX 对 AMReX GPU OOM 默认策略的覆盖点”，而不是抽象运行建议。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `TimeAveraged` diagnostics 相邻条目压到更细的窗口与 flush 语义：本轮细化了 `<diag>.time_average_mode / average_period_steps / average_period_time / average_start_step`，明确 `time_average_mode` 不只分派 `Static/Dynamic/None`，还决定 `Flush()` 是写瞬时 `m_mf_output` 还是写累计后的 `m_sum_mf_output`、以及 dynamic 模式写后是否清零；同时补清 `average_period_steps/time` 会继续进入 dynamic-start 窗口长度、重叠合法性检查和归一化分母，而 `average_start_step` 则控制 fixed-start 窗口起点与累计长度更新。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 BTD 相邻 instance-local 条目压到更细的 snapshot/buffer 调度链：本轮细化了 `<diag>.num_snapshots_lab / dt_snapshots_lab / dz_snapshots_lab / buffer_size / do_back_transformed_fields / do_back_transformed_particles`，明确 `num_snapshots_lab` 现在直接决定 `m_intervals -> m_num_buffers` 和整套 BTD 状态数组尺寸，`dt/dz_snapshots_lab` 会继续进入每个 snapshot 的 `m_t_lab` 时间轴与 `final_snapshot_fill_iteration` 估算，`buffer_size` 同时控制 buffer 数量、snapshot z 向对齐和运行时 buffer-box 滑动，而 fields/particles 两个开关则分别压实到 field-side MultiFab/functor 分配链、以及 per-species pinned particle buffer 和纯粒子 BTD 几何外壳补建链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `virtual photons` 相邻尾项补成更完整的 QED 运行链：本轮不仅把 `<species>.do_qed_virtual_photons` 从 lepton gate 压实到 `CollisionHandler::doCollisions()` 每步先重建 virtual-photon species 的总调度链，还正式补出了 `<species>.qed_virtual_photon_species_name / qed_virtual_photons_min_energy / qed_virtual_photons_multiplier` 三条缺失 companion，明确它们分别控制目标 photon container 绑定、`y_min` 能量下限对计数/采样双链的约束，以及“生成数放大、单粒子权重按同一比例缩小”的 tile 级重采样合同。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `resampling` 的 algorithm-specific companions 正式补成 `parameter-map` 条目：本轮新增并压实了 `<species>.resampling_algorithm_target_ratio / target_weight / velocity_grid_type / delta_ur / n_theta / n_phi / delta_u`，把它们分别接回 `LevelingThinning` 的 `level_weight = average_weight * target_ratio` 链，以及 `VelocityCoincidenceThinning` 的 `cluster_weight` 上界、spherical/cartesian `VelocityBinCalculator` 几何分派、球坐标角向步长和直角坐标三向 bin 尺度构造链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `<species>.do_adk_correction / physical_element / ionization_product_species / ionization_initial_level` 继续压到更细的 field-ionization companion 链：本轮补清了 `do_adk_correction` 不只把 `table_correction_factors` 搬到设备端，还会连同 `ion_atomic_number` 与 `GetIntCompIndex("ionizationLevel")` 一起进入 `IonizationFilterFunc`；`physical_element` 也继续压实到 `ion_map_ids / ion_atomic_numbers / ion_energy_offsets` 驱动的电离能与 ADK 系数数组构造，并同时约束氢元素外禁止 ADK correction；此外 `ionization_product_species / ionization_initial_level` 也继续压实到 `mapSpeciesProduct() / CheckIonizationProductSpecies()` 的 product 绑定检查，以及 `DefaultInitialization.H` 与 `AddParticles.cpp` 对 `ionizationLevel` 初值的真实写入链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `<species>.save_particles_at_xlo/.../eb / do_field_ionization` 继续压到更细的粒子后处理与多物理主循环链：本轮补清了前者不只是在 `ParticleBoundaryBuffer` 构造期形成 `m_do_boundary_buffer / m_do_any_boundary` 激活表，还会继续进入 `gatherParticlesFromDomainBoundaries()` 与 `gatherParticlesFromEmbeddedBoundaries()` 的 buffer container 动态分配、`filterAndTransformParticles` 收集链，以及 `BoundaryScrapingDiagnostics` 的后续消费；同时补清了后者不只是在 `InitIonizationModule()` 中安装 `ionizationLevel` 和电离模块，还会继续进入 `WarpXEvolve.cpp -> WarpX::doFieldIonization() -> MultiParticleContainer::doFieldIonization()` 的 level-wise 调度，并通过 `filterCopyTransformParticles` 把电离产物写入 product species。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `<species>.do_continuous_injection / do_backward_propagation` 继续压到更细的 moving-window 与 boosted-frame 初始化链：本轮补清了前者不只是在 `MultiParticleContainer` 中做布尔筛选，还会继续进入 `WarpXMovingWindow.cpp` 对 `particleBox`、`m_current_injection_position` 和实际 `pc.ContinuousInjection(...)` 调度的更新链，以及 `PhysicalParticleContainer::findRefinedInjectionBox()` 里 `refine_plasma + numLevels()==2` 的特例分支；同时修正并补清了后者在 `MapParticletoBoostedFrame()` 中的真实时序，即先算 boosted-frame `tpr/xpr/ypr/zpr` 与 `vxpr/vypr/vzpr`，再按标志翻转 `uz`，最后用这些速度把位置回推到当前 boosted-frame 时间。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `<species>.random_theta / do_splitting / split_type` 继续压到更细的粒子初始化与 AMR split 链：本轮补清了 `random_theta` 不只是一个 RZ 几何开关，而是 `AddParticles.cpp` 中“按 cell 统一随机旋转整组新粒子方位角”的 runtime gate；`do_splitting` 则继续压实到 `particlePostLocate()` 的跨 level tagging、`PhysicalParticleContainer::Evolve()` 对 `subcycling_half / position_push_type` 的一步末尾调度 gate，以及最终 `SplitParticles(lev)` 的执行链；`split_type` 也继续压实到 `SplitParticles()` 中 1D/2D/3D 各种模板、子粒子数和 `wp/np_split` 权重分配语义。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `<species>.num_particles_per_cell_each_dim` 继续压到更细的规则注入几何链：本轮补清了它不只在 `PlasmaInjector::setupNuniformPerCell()` 中读取和做 `RZ` 多模约束，还会继续进入 `InjectorPositionRegular::getPositionUnitBox(...)` 的 cell 内规则格点生成、`WarpXInitData.cpp::get_nppc()` 的 `split_high_density_boxes` 预估路径、`AddParticles.cpp::AddPlasma()` 的每 cell 候选粒子枚举数，以及 `PhysicalParticleContainer::SplitParticles()` 的 split-offset 缩放公式。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `<species>.theta_distribution_type / beta_distribution_type / zinject_plane / rigid_advance` 继续压到更细的 compile-to-functor 与 rigid 状态机链：本轮补清了前两者不只是在 `TemperatureProperties / VelocityProperties` 中分派 `constant / parser`，还会继续进入 `GetTemperature / GetVelocity` 的 `ParserExecutor<3>` 编译与方向/符号固化链，再被 `InjectorMomentumBoltzmann / Juttner` 逐粒子消费；同时补清了后两者不只是在 `RigidInjectedParticleContainer` 构造期解析，还会继续进入 `InitData()` 的 boosted-frame 注入平面初始化、`Evolve()` 的平面移动与 `done_injecting_lev` 判定，以及 `PushPX()/PushP()` 对未越过注入平面的粒子回退到 `vzbar / vz / v` 三种 rigid-advance 轨迹。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `<species>.radial_numpercell_power / momentum_distribution_type` 继续压到更细的体注入/flux 注入与动量采样链：本轮补清了前者不只影响 `AddParticles.cpp` 的径向位置采样，还会继续进入 `AddPlasmaFlux()` 的 surface-emission 半径采样与权重修正；同时补清了后者不只是在 `SpeciesUtils::parseMomentum()` 中做 `InjectorMomentum*` 工厂分派，还会继续进入 ballistic-correction 用到的 `getBulkMomentum(...)` 链，以及 `AddParticles()/AddPlasmaFlux()` 对 `inj_mom->getMomentum(..., engine)` 的逐粒子随机采样链，其中 `gaussianflux`、Boltzmann、Juttner 各自保留专门的法向采样或 Lorentz/flipping 分支。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `<species>.profile / flux_profile / density_min / density_max` 继续压到更细的密度/流量生成链：本轮补清了 `profile` 不只在 `SpeciesUtils::parseDensity()` 中分派 `constant / parse_density_function / read_from_file`，还会继续进入 `InjectorDensity::{prepare,getDensity}` 的文件密度预加载与逐粒子统一取值链；`flux_profile` 继续压实到 `ContinuousFluxInjection() -> AddPlasmaFlux() -> inj_flux->getFlux(...)` 的 runtime 权重生成链；同时 `density_min` 现在明确会把低密度候选粒子直接 `ZeroInitializeAndSetNegativeID(...)`，`density_max` 则会在 `weight = dens * scale_fac` 之前做硬截断。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `<species>.do_not_deposit / do_not_gather / do_not_push` 继续压到更细的 species runtime gate：本轮补清了它们不只是在 `PhysicalParticleContainer` 中读成布尔位，还会分别继续进入 `PhysicalParticleContainer::Evolve()` 对 `deposit_charge/deposit_current` 和 push 链的运行时关断、`WarpXParticleContainer::{DepositCurrent,DepositCharge,DepositMassMatrices,PushX}` 的函数级 early-return、`ImplicitPushPX` 与 `PhotonParticleContainer::PushPX()` 的 gather 分支，以及 `MultiParticleContainer::{DepositCharge,GetChargeDensity,SortParticlesByBin}` 对整 species 的汇总层跳过。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `<species>.injection_style` 继续压到更完整的 injector 工厂与 runtime 分派边界：本轮补清了它不只是在 `PlasmaInjector` 构造期分派 `setup*` 例程，还会继续进入 `PhysicalParticleContainer` 对基准/多 source injector 的构造链、`WarpXInitData.cpp::get_nppc()` 的 style-specific 负载预估路径，以及 `AddParticles()/ContinuousInjection()/ContinuousFluxInjection()` 对 `AddNParticles / AddGaussianBeam / AddPlasmaFromFile / AddPlasma / AddPlasmaFlux` 的真实运行分支。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `<species>.xmin/ymin/zmin/xmax/ymax/zmax` 和 `injection_sources` 继续压到更细的几何裁剪与 multi-source 运行边界：本轮补清了前者不只是在 `PlasmaInjector` 中作为静态边界保存，还会以“下闭上开”的 `insideBounds()` 和 box-level `overlapsWith()` 进入 `AddParticles.cpp` 的 tile 级 early-return、cell/粒子逐点裁剪以及 ballistic-correction 后的注入判定；同时补清了后者不只扩展 parser 作用域，还会驱动 `PhysicalParticleContainer::AddParticles()/ContinuousInjection()/ContinuousFluxInjection()` 对多个 `PlasmaInjector` 逐个执行，而 `WarpXInitData.cpp` 的 `get_nppc()` 预估路径目前仍明确不支持它。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-20：继续推进阶段 A 总索引人工收口，已把 `<species>.charge / mass` 继续压到更完整的 source-priority 与 external-file 边界：本轮补清了它们不只是在 `PhysicalParticleContainer` 中按 `PlasmaInjector -> species_type -> 显式 parser` 三层优先级解析，还会继续进入 `PlasmaInjector::setupExternalFile()` 从 openPMD 记录读取并按 `unitSI` 还原质量电荷、向全部 MPI rank 广播，以及 `SpeciesUtils::extractSpeciesProperties()` 对 `injection_style == external_file` 的豁免分支与后续缺失断言。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：已在项目根目录初始化 git 仓库，默认分支为 `main`，并新增最小 `.gitignore` 以忽略 macOS 垃圾文件、AppleDouble sidecar 和 Python 缓存文件；后续项目推进默认按仓库状态持续保留版本记录。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `particles.rigid_injected_species` 和 `<species>.species_type` 继续压到更完整的工厂分派与运行链：本轮补清了前者不只是在 `MultiParticleContainer` 中把目标 species 标成 `PCTypes::RigidInjected`，还会继续进入 `RigidInjectedParticleContainer` 的 `zinject_plane / rigid_advance` 解析、`InitData() -> AddParticles(0) -> RemapParticles() -> Redistribute()` 初始化链，以及后续每步 `Evolve()` 对 `zinject_plane_levels` 和 `done_injecting_lev` 的更新；同时补清了后者不只给出默认 `charge/mass`，还会在 `MultiParticleContainer` 中把 `species_type == photon` 的 species 切到 `PhotonParticleContainer`，并在 injector/external-file 路径上重复参与质量电荷优先级和边界检查。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `particles.use_fdtd_nci_corr` 继续压到更完整的 FDTD NCI corrector 运行链：本轮补清了它不只是在 `MultiParticleContainer` 中读取并在 `WarpX.cpp` 里接受 `PSATD / Esirkepov / implicit` 兼容性约束，还会继续进入 `WarpX::InitNCICorrector()`，按 level 的 `c*dt/dz` 和 `nodal_gather` 构造两套 `NCIGodfreyFilter` 并 `ComputeStencils()`；随后在 `PhysicalParticleContainer::Evolve()` 的 fine/coarse gather 两条路径里，又会先对 tile 级 `E/B` 分量做 `ApplyStencil(...)` 到临时 `filtered_*` `FArrayBox`，再把后续 `PushPX(..., lev, gather_lev)` 的 gather 指针改绑到这些过滤后的临时场。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `particles.do_tiling` 继续压到更完整的粒子创建运行边界：本轮补清了它不只是在 `WarpXAMReXInit.cpp` 启动期用 `queryAdd("do_tiling", !amrex_use_gpu)` 覆写默认值、在 `WarpXParticleContainer` 中静态读取，还会继续进入 `AddParticles.cpp` 与 `MultiParticleContainer.H` 的 `MFItInfo::EnableTiling(tile_size)` 路径，并在多 species 粒子创建 helper 里要求参与同一次创建过程的所有 species 保持一致的 tiling 选择；同时也明确了这条开关只在 `Gpu::notInLaunchRegion()` 的 CPU 路径下真正生效。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `particle_thermalizer.*` 继续压到更完整的热化运行链：本轮补清了 `normal / species / start / end / momentum_threshold / theta` 不只是在 `ParticleThermalizer` 构造期做定义和合法性检查，还会继续进入 `WarpXEvolve.cpp` 的每步 `applyThermalizer(*mypc)` hook、per-species/per-level/per-tile 的执行链、`find_overlap(...)` 的 tile 级早退、沿法向的粒子位置概率筛选，以及“只对超过阈值的动量分量按 `RandomNormal` 重采样并保留原符号”的逐粒子热化 kernel。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `particles.deposit_on_main_grid` 继续压到更完整的 coarse-buffer 沉积链：本轮补清了它不只是在 `MultiParticleContainer` 中登记 species 级 side-list、在 `WarpX.cpp` 中把 `n_current_deposition_buffer` 至少抬到 `1` 并强制分配 `current_buf/rho_buf`，还会继续进入 `Partition.cpp` 的 `nfine_current=0` 分区边界，以及 `PhysicalParticleContainer.cpp` 中按 `np_to_deposit` 把后段粒子显式切到 `rho_buf/current_buf`、以 `(lev, lev-1)` 做 `DepositCharge/DepositCurrent`；随后这些 buffer 还会在 `WarpXEvolve.cpp` 中继续进入 `AddCurrentFromFineLevelandSumBoundary(...)` 和 `AddRhoFromFineLevelandSumBoundary(...)` 的 coarse-level 汇总链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `particles.gather_from_main_grid` 继续压到更完整的 coarse-gather 运行链：本轮补清了它不只是在 `MultiParticleContainer` 中登记 species 级 side-list、在 `WarpX.cpp` 中强制分配 `Efield_cax/Bfield_cax`，还会继续进入 `Partition.cpp` 的 `nfine_gather=0` 分区边界，以及 `PhysicalParticleContainer.cpp` 中按 `np_gather` 把前段粒子保留在 fine gather 路径、把后段粒子显式切到 `Efield_cax/Bfield_cax` 并以 `gather_lev = lev-1` 做 push/gather 的真实运行分支。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `particles.E/B_ext_particle_init_style` 继续压到更完整的粒子外场链：本轮补清了它不只是在 `MultiParticleContainer::ReadParameters()` 中分派 `parser / repeated_plasma_lens / read_from_file`，还会继续进入 `ExternalParticleFields::ReadParameters()` 的 named-field/单字段 fallback 与时间依赖 parser，随后在 `WarpX.cpp` 中按 metadata 数量分配多分量 `E/B_external_particle_field`，在 `WarpXInitData.cpp` 中把 openPMD 文件场读入对应 component，并最终在 `WarpXComm.cpp` 的 `UpdateAuxilaryData()` 中按 `time_executor(t_new[lev])` 做 `Saxpy` 叠加到粒子真正 gather 的 `Efield_aux/Bfield_aux`。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `lasers.deposit_on_main_grid` 继续压到更完整的多层沉积链：本轮补清了它不只是在 `MultiParticleContainer` 中把 laser 名字 side-list 回绑到 `m_deposit_on_main_grid`，还会继续进入通用 `Partition.cpp` 的 `nfine_current=0` 分区分支，以及 `LaserParticleContainer::Evolve()` 里按 `np_to_deposit=0` 把高层粒子的 charge/current 从本层 `rho_fp/current_fp` 改走 `rho_buf/current_buf -> (lev-1)` 的 coarse-buffer 沉积路径。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `fluid` 相邻尾项继续压到更完整的 runtime 链：本轮补清了 `fluids.species_names` 不只决定 `MultiFluidContainer` 的对象构造，还通过 `do_fluid_species` 直接 gate `WarpX.cpp` 中的 `AllocateLevelMFs / InitData / DepositCharge / DepositCurrent / Evolve` 整条 cold-fluid 子系统执行链；同时补清了 `<fluid>.E/B_ext_init_style` 在 `GatherAndPush()` 中不是简单 parser 开关，而是先按 runtime flag 决定是否编译 parser，再区分“lab-frame 求值 + boosted-frame 回变换”和“直接在模拟系求值”两条外场叠加路径。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `external-grid init` 相邻条目继续压到更完整的初始化/窗口平移链：本轮补清了 `E/B_external_grid` 不只在 `ExternalFieldParams` 中作为 constant branch 的输入，还会在 `WarpXInitData.cpp` 中写入 `*_aux/*_cp/*_avg_*` 场容器，并在 `WarpXMovingWindow.cpp` 的 `shiftMF(...)` 中作为新入窗区域的默认外场填充值；同时补清了 `maxlevel_extEMfield_init` 对 constant 与 parser 分支都是 level gate，但 `B` parser 还带有 `lev > 0` 的更窄生效边界，而 `E` parser 可从 level 0 起进入。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `load_balance` 相邻尾项继续压到更完整的运行链：本轮补清了 `algo.load_balance_intervals / load_balance_efficiency_ratio_threshold / load_balance_with_sfc / load_balance_knapsack_factor` 除了 `LoadBalance()` 本体外，还带有旧 `warpx.*` 别名拒绝边界；同时补清了 `load_balance_costs_update / costs_heuristic_*` 会并行影响 `WarpX::LoadBalance()` 和 `LoadBalanceCosts` reduced diagnostic 两条链，而 `roundrobin_sfc / split_high_density_boxes*` 则会联动 `PostProcessBaseGrids()` 与 `MakeDistributionMap()`，共同决定初始化期 box 切分和初始 rank 分发策略。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `num_mirrors / mirror_z / mirror_z_width / mirror_z_npoints` 继续压到真实镜面清零链：本轮补清了 `num_mirrors` 不只是启用 `ApplyMirrors()`，还会把 fine/coarse patch 上的 `E/B/F/G` 全部接入 `NullifyMF(...)` 清零 pass；同时补清了 `mirror_z` 在 boosted frame 下会先平移成当前窗口坐标，`mirror_z_width` 只提供初始物理厚度，而最终 `z_max` 还要和 `mirror_z_npoints * dz` 逐 level 取最大值，从而形成真实的 per-level mirror 区间。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `serialize_initial_conditions / safe_guard_cells / ablastr.fillboundary_always_sync` 继续压到更具体的 runtime 分支：本轮补清了 `serialize_initial_conditions` 除了控制 `AddParticles.cpp` 的 OpenMP 初始化并行 gate，还显式带有旧别名 `serialize_ics` 的拒绝边界；`safe_guard_cells` 不只放大 `WarpXComm` 的 guard-cell 通信范围，还会在 `WarpXEvolve.cpp` 的若干 field-push/PML 分支后触发额外 `FillBoundaryB/F/E(...)` 补同步，并继续传入 moving-window 的 `shiftMF(...)`；而 `ablastr.fillboundary_always_sync` 则明确落在 `ablastr::utils::communication::FillBoundary(...)` 内部，对普通与 single-precision 临时副本两条路径统一切换 `FillBoundary(...)` / `FillBoundaryAndSync(...)`。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `parse_field_function + from_file` 继续压到真实幅值/缓存链：本轮补清了 `field_function(X,Y,t)` 在 `FieldFunctionLaserProfile` 中是“`update()` 为 no-op、`fill_amplitude()` 每步 `compile<3>()` 后直接逐粒子求值”的 profile 本体；同时补清了 `binary_file_name / lasy_file_name / time_chunk_size / delay` 不只决定初始化期读哪个 backend 和首块数据，还会继续进入 `FromFileLaserProfile::update()` 的时间块滑窗加载，以及 `fill_amplitude()` 的“越界直接清零、有效时再分流到 binary/cartesian/cylindrical 插值”运行链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `Gaussian STC + continuous-injection` 继续压到真实运行链：本轮补清了 `zeta / beta / phi2` 不只是 `GaussianLaserProfile::init(...)` 的可选系数，而是分别落到 `stretch_factor`、逐粒子 `stc_exponent` 和 chirp 虚部修正里，共同决定 STC/chirp 下的复时间包络；同时补清了 `do_continuous_injection / min_particles_per_mode` 会分别控制 boosted-frame 天线位置回推与“首次入域才建粒子”的 gate，以及 RZ 模式下 `n_spokes = (n_modes-1) * min_particles_per_mode` 的实际 spoke 离散与权重归一化。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `laser plane-geometry + gaussian main profile` 继续压到真实几何与幅值链：本轮补清了 `<laser>.position / polarization / direction` 不只在构造期做归一化和正交检查，还会继续进入 `InitData()` 的注入盒相交、天线粒子平面铺设以及“整片天线完全在 simulation box 外时禁用 laser”的分支；同时补清了 Gaussian 主参数 `profile_t_peak / duration / waist / focal_distance / phi0 / stc_direction` 在 `GaussianLaserProfile::fill_amplitude()` 中分别落到 `oscillation_phase`、`stretch_factor`、`stc_exponent`、`diffract_factor` 和横向 `exp_argument`，也就是直接构成逐粒子 amplitude kernel，而不是只停留在 profile 初始化期。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `laser common-entry` 继续压到真实构造/运行链：本轮补清了 `lasers.names` 不只是在 `WarpX::ReadParameters()` 中触发“有 laser 时必须读取相关全局参数”的路径，还会在 `MultiParticleContainer` 中扩展 `allcontainers`、实例化每个 `LaserParticleContainer` 并回绑 `deposit_on_main_grid`；同时补清了 `<laser>.e_max / a0 / wavelength / profile` 会在 `LaserParticleContainer` 构造期汇合到 `CommonLaserParameters` 与 profile 工厂，施加 `e_max xor a0`、正波长和零振幅禁用等边界，然后统一进入 `ILaserProfile::init(...)` 与运行期 `update() -> fill_amplitude() -> update_laser_particle()` 主链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `<species>.do_temperature_deposition` 继续压到真实温度沉积链：本轮补清了它不只是在 `PhysicalParticleContainer` 构造期安装 species 级 `T_<species>` 三方向场和 `VarianceAccumulationBuffer`，还会在 `MultiParticleContainer::DepositTemperatures()` 中进入 `AccumulateVelocitiesAndComputeTemperature()` 的双遍 variance deposition 流程，对 `n/w/vbar/w2` 累积数组做 boundary sum，再按 `mass/k_B` 换算成 Kelvin 并经 `WarpX::use_filter` 收口；同时补清 diagnostics 侧并不是直接读原始 `T_<species>` MultiFab，而是通过 `TemperatureFunctor -> pc.GetAverageNGPTemperature(m_lev)` 二次取值后再 coarsen 到输出网格。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `resampling_trigger_max_avg_ppc` 和 resampling 算法 companion 参数再压实一层：本轮补清了 `resampling_trigger_max_avg_ppc` 不只是在 `ResamplingTrigger::triggered(...)` 中与 `avg_ppc` 比较，还明确经过 `PhysicalParticleContainer::resample()` 的全局粒子数同步链；同时补清了 `LevelingThinning` 的 `resampling_algorithm_target_ratio` 会直接控制 `level_weight = average_weight * target_ratio` 的 thinning 门槛，而 `VelocityCoincidenceThinning` 的 `resampling_algorithm_target_weight / velocity_grid_type / delta_ur / n_theta / n_phi / delta_u` 会继续决定 momentum-bin 构造方式、cluster weight 上界以及 spherical/cartesian 两类速度网格离散语义。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `do_resampling / resampling_algorithm / resampling_trigger_intervals / resampling_min_ppc` 继续压到真实 resampling 激活链：本轮补清了 `do_resampling` 不只在 `PhysicalParticleContainer` 构造期安装 `Resampling(species_name)`，还会继续进入 `WarpXEvolve -> MultiParticleContainer::doResampling() -> PhysicalParticleContainer::resample()`，在全局粒子数同步后由 `ResamplingTrigger` 决定是否触发；同时补清了 `resampling_algorithm` 在 `leveling_thinning / velocity_coincidence_thinning` 两条 tile-kernel 语义上的真实分叉，`resampling_min_ppc` 是两种算法共享的 per-cell early-return gate，而 `resampling_trigger_intervals` 会与 `resampling_trigger_max_avg_ppc` 和全局 `avg_ppc` 条件共同组成触发判定。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `momentum_distribution_type / theta_distribution_type / beta_distribution_type` 继续压到真实动量注入分派链：本轮补清了这组参数的真正上游调用者是 `PlasmaInjector` 在 `gaussian_beam / nrandompercell / nfluxpercell / nuniformpercell` 构造期对 `SpeciesUtils::parseMomentum(...)` 的统一调用，而 `momentum_distribution_type` 会继续分派到各类 `InjectorMomentum*`；同时补清了 `theta_distribution_type` 与 `beta_distribution_type` 并不是抽象子对象，而是分别通过 `GetTemperature / GetVelocity` 进入 `InjectorMomentumBoltzmann / InjectorMomentumJuttner`，从而定义 Maxwell-Boltzmann / Maxwell-Juttner 注入链中的温度与 bulk drift 取值模式。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `addIntegerAttributes / addRealAttributes / do_adk_correction / physical_element / ionization_product_species / ionization_initial_level` 继续压到真实 species 初始化与 field-ionization consumer 链：本轮补清了 `add*Attributes` 不只在 `PhysicalParticleContainer` 构造期扩展 SoA runtime components，还会在 `DefaultInitialization.H` 中经 `getUser{Int,Real}Attribs()` 与 `compile<7>()` 重新求值并写入新粒子；同时补清了 `do_adk_correction` 如何装入 Hydrogen correction factors 并传入 `IonizationFilterFunc`，`physical_element` 如何决定 ionization energy / atomic-number 表装载与修正模型边界，`ionization_product_species` 如何经 `mapSpeciesProduct() -> CheckIonizationProductSpecies() -> doFieldIonization()` 绑定到真实 product container，而 `ionization_initial_level` 则会先建立 `ionizationLevel` runtime int component，再在初始化和 ADK 运行链中持续充当 charge-state 状态。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `qed_schwinger.y_size / xmin...xmax... / threshold_poisson_gaussian` 继续压到真实 Schwinger consumer 链：本轮补清了 `y_size` 不只在 `doQEDSchwinger()` 中把 2D 单元体积写成 `dx*dz*y_size`，还会继续进入 `SchwingerTransformFunc{m_qed_schwinger_y_size, PIdx::w}`，把新生成电子/正电子权重写成 `total_weight/N/y_size`；同时补清了 `xmin...xmax...` 会在 `ComputeSchwingerGlobalBox()` 中被转换成 level-0 index-space 全局掩膜 box，并在每个 tile 上与 cell-centered `box` 做相交裁剪；`threshold_poisson_gaussian` 则会通过 `SchwingerFilterFunc` 继续传进 `getSchwingerProductionNumber(...)`，决定 Schwinger pair 采样走 Poisson 还是 Gaussian 分支。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `warpx.do_qed_schwinger` 与 `qed_schwinger.ele/pos_product_species` 继续压到真实 Schwinger 初始化/运行链：本轮补清了 `do_qed_schwinger` 不只是在构造期打开 `qed_schwinger` 参数簇，还会继续把电子/正电子产物 species 名解析成整数索引，并在 `doQEDSchwinger()` 中通过 `allcontainers[...]` 取到目标 container、再调用 `filterCreateTransformFromFAB(...)` 真正注入电子-正电子对；同时补清了 `ele_product_species / pos_product_species` 各自如何完成“名字 -> 索引 -> 真实容器落点”的 wiring。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `qed_qs.chi_min / qed_bw.chi_min` 继续压到真实 QED 门槛链：本轮补清了两者不只在 `InitQuantumSync()` / `InitBreitWheeler()` 中进入 `init_lookup_tables_from_raw_data(...)`、`init_builtin_tables(...)` 和表生成路径，还会分别写入 `QuantumSyncEngineWrapper` / `BreitWheelerEngineWrapper` 的内部 `minimum_chi` 状态，并由运行期 `build_evolve_functor()` 直接消费；因此 `chi < chi_min` 时会在 optical-depth 推进阶段提前返回，分别抑制 photon generation 或 pair production。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `qed_qs.lookup_table_mode / qed_bw.lookup_table_mode` 继续压到真实 QED 表初始化分流链：本轮补清了两者在 `InitQuantumSync()` / `InitBreitWheeler()` 中都会把 `lookup_table_mode` 分派成 `generate / load / builtin` 三条路径，其中 `generate` 进一步落到 `save_table_in`、`compute_lookup_tables()`、写盘与 `ReadAndBcastFile(...)` 广播，`load` 则从 `load_table_from` 直接广播原始表数据，`builtin` 走 wrapper 的内置低分辨率表初始化；最终两条链都以 `are_lookup_tables_initialized()` 作为硬断言。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `qed_quantum_sync_phot_product_species` 与 `do_classical_radiation_reaction` 继续压到真实 QED / push consumer 链：本轮补清了 `qed_quantum_sync_phot_product_species` 会先在 `MultiParticleContainer` 初始化期经 `getSpeciesID(...)` 解析成 `m_qed_quantum_sync_phot_product`，再在 `doQedQuantumSync()` 中通过 `allcontainers[...]` 取到真实 photon product container；同时补清了 `do_classical_radiation_reaction` 不只控制普通 `PhysicalParticleContainer` push，还会切到 `RigidInjectedParticleContainer` 和 `PushSelector.H` 中的 `UpdateMomentumBorisWithRadiationReaction(...)` 分支。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `qed_qs.photon_creation_energy_threshold` 与 `<species>.do_qed_quantum_sync` 继续压到真实 QED 初始化/运行链：本轮补清了前者默认值来自 `MultiParticleContainer.H` 中的 `2*m_e*c^2`，并在 `doQedQuantumSync()` 的 photon-emission pass 后通过 `cleanLowEnergyPhotons(...)` 直接充当低能 photon 保留门槛；同时补清了 `do_qed_quantum_sync` 不只是在 species 构造期添加 `opticalDepthQSR`，还会驱动共享 `QuantumSynchrotronEngine` 绑定、`opticalDepthQSR` 的默认初始化，以及普通 push / `ImplicitPushPX` / `doQedQuantumSync()` 三条运行链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `ParticleHistogram2D / DifferentialLuminosity2D` 的 openPMD 输出合同继续压到真实 writer 链：本轮补清了两者都在实例构造函数里自行解析 `openpmd_backend / file_min_digits` 并回写默认 backend，而 `WriteToFile()` 会把文件模板生成为 `openpmd_%0NT.<backend>`、按 `series.iterations[step+1]` 组织输出；其中 `ParticleHistogram2D` 还会把 `function_string_abs/ord` 写成 axis label 与 mesh attribute，`DifferentialLuminosity2D` 则会把 `d2L_dE1_dE2`、`E1/E2` 轴和二维能量网格元数据一起写入 openPMD。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `ParticleHistogram / ParticleHistogram2D` 的 parser companion 条目继续压到真实 reduced-diag kernel 链：本轮补清了 `ParticleHistogram.filter_function(...)` 如何在与主 histogram 相同的 `(t,x,y,z,ux/c,uy/c,uz/c)` 约定下先做样本裁剪，再阻断后续 `histogram_function(...)` 与一维分箱；同时补清了 `ParticleHistogram2D` 的 `histogram_function_abs/ord(...)`、`value_function/filter_function(...,w)` 如何在 `ComputeDiags()` 中先恢复位置与归一化动量，再进入 abs/ord 双轴求值、二维落 bin 和 `Atomic::Add` 的累加链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `<reduced>.species / bin_number / histogram_function / normalization` 继续压到真实 reduced-diag 实例消费链：本轮补清了 `species` 在 `ParticleHistogram / ParticleHistogram2D / BeamRelevant / DifferentialLuminosity` 四类构造函数中的分流绑定方式，明确 `ParticleHistogram*` 走 `m_selected_species_id`、`BeamRelevant` 只保留目标束流、`DifferentialLuminosity` 则固定为两束碰撞对；同时补清了 `bin_number/bin_min/bin_max` 如何共同定义 header bin center 和 `ComputeDiags()` 的实际分箱网格，以及 `histogram_function(...)` 和 `normalization` 分别如何进入逐粒子 parser 求值、原子累加和 MPI 归约后的归一化分支。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `reduced_diags.separator / precision` 继续压到真实文本输出合同链：本轮补清了 `separator` 不仅控制基类和多种派生类的列拼接，还会被 `LoadBalanceCosts` 用来重新拆解旧文件并补 `NaN`；同时补清了 `precision` 的实例级覆盖主要只稳定作用于走基类 `WriteToFile()` 的 reduced-diag，而 `FieldProbe / LoadBalanceCosts` 这类自定义 writer 目前仍部分绕开 `m_precision`。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `reduced_diags.path` 继续压到真实 reduced-diag 输出合同链：本轮补清了它在 `ReducedDiags` 基类里不仅决定目录创建、restart 时 `m_write_header` 的续写判断和默认文本文件落点，还会被 `ParticleHistogram2D / DifferentialLuminosity2D` 这类 openPMD reduced-diag 继续当作上游根目录，派生成 `m_path + m_rd_name + "/" + filename` 的实例级子目录布局。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `plot_raw_fields / plot_raw_fields_guards` 继续压到真实 raw-plotfile 分流链：本轮补清了它们真正只在 `FullDiagnostics::ReadParameters()` 中解析、BTD 只是在 flush 时原样转发成员值；同时补清了 plotfile 路径会把 `"raw_fields"` 加进 root-component 列表并在 `plotfilename/raw_fields` 下写出 `E/B/j` 的 `aux/fp/cp`、`rho_fp`、`phi_fp`、时均场和 coarse-path 原始 MultiFab，而 openPMD 路径则直接断言禁止 raw 输出。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `particle_fields_species / particle_fields.<field_name>.*` 继续压到真实 `ParticleReductionFunctor` 消费链：本轮补清了 `particle_fields_species` 不只做 species 名字校验，还决定 `m_pfield_species_index`、`<field>_<species>` 写入 `m_varnames` 的顺序，以及 `ParticleReductionFunctor::m_ispec -> GetParticleContainer(m_ispec)` 的真实物种绑定；同时补清了 `particle_fields.<field_name>(...) / .filter(...)` 都会被编译成 `ParserExecutor<6>`，在 `get_particle_position(...)`、`ux,uy,uz /= c`、`ParticleToMesh(...)` 和最终 `Coarsen(...)` 这条 reduction 链上运行，而 `.do_average` 则决定是否额外构造 `ppc_mf` 做归一化。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `fields_to_plot / particle_fields_to_plot` 继续压到真实 diagnostics 变量表与 functor 分派链：本轮补清了 `BaseReadParameters()` 中 `phi/A/F/G/proc_number/none` 的约束与重写规则、`m_varnames_fields -> m_varnames` 的统一变量表写入，以及 `FullDiagnostics` 如何把它们落成 `CellCenterFunctor/RhoFunctor/TemperatureFunctor/JFunctor/...`；同时补清了 `particle_fields_to_plot` 不只构造 `ParticleReductionFunctor`，还会先把 `<field>_<species>` 展开进 `m_varnames`，而 `BTDiagnostics` 对它是显式 `queryarr(...)` 后立刻 abort 的硬禁止边界。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `buffer_flush_limit_btd / dump_rz_modes` 继续压到真实 diagnostics/openPMD 分流链：本轮补清了 `buffer_flush_limit_btd` 的默认值来自 `FlushFormatOpenPMD` 成员 `m_NumAggBTDBufferToFlush=5`，其真实 consumer 是 `WriteToFile()` 中按 `bufferID % threshold` 触发的 `FlushBTDToDisk()`，并且这条刷盘链直接服务于 partial-read 与 BTD restart append；同时补清了 `dump_rz_modes` 只控制普通 full diagnostics 的 `AddRZModesToDiags(...)` 展开，而 RZ + openPMD 会在 `InitializeFieldFunctors()` 中提前分流到 `InitializeFieldFunctorsRZopenPMD(...)`，始终走独立的全-mode functor 初始化分支。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `<diag>.<species>.plot_filter_function(...)` 继续压到真实 writer-side parser filter 链：本轮补清了 `ParticleDiag` 只负责 `Store_parserString + makeParser(...)` 保存 parser，而 `FlushFormatPlotfile` 与 `WarpXOpenPMD` 会在 writer 侧用 `compileParser<ParticleDiag::m_nvars>(...)` 生成 `ParserFilter`、显式设定 `InputUnits::SI`，并在 `copyParticles(...)` 中同时覆盖普通 `pc` 与 pinned/BTD 的 `pinned_pc` 路径，而且筛选发生在 `storePhiOnParticles()` / `storeFieldOnParticles()` 之前。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `<diag>.<species>.additional_variables / random_fraction / uniform_stride` 继续压到真实 writer-side 粒子复制与附加字段链：本轮补清了 `additional_variables` 最终落到 `storePhiOnParticles()` / `storeFieldOnParticles()`、且只作用于经过筛选后的临时输出副本 `tmp`，以及 `random_fraction / uniform_stride` 在 plotfile/openPMD 中都作为 `copyParticles(...)` 的 filter functor 参与筛选，并同时覆盖普通 `pc` 路径与 pinned/BTD 的 `pinned_pc` 路径。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `species / <diag>.<species>.variables` 继续压到真实粒子对象构造与变量筛选链：本轮补清了 `FullDiagnostics`、`BTDiagnostics`、`BoundaryScrapingDiagnostics` 三条 species 默认回退与 `ParticleDiag` 构造分流，明确 BTD 还会为每个 species/snapshot 构造 pinned `make_alike<>` buffer；同时补上了 `ParticleDiag` 中 `variables` 对 `m_plot_flags` 与 `m_plot_phi/E/B` 的双路分派、RZ/RSPHERE 下 `x/y/z -> r/theta/phi` 的坐标重写，以及“未包含位置变量”的高优先级 warning 与角坐标强制输出边界。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `file_min_digits / write_species` 继续压到真实 diagnostics 命名合同与粒子输出 gate：本轮补清了 `BaseReadParameters()` 对 `file_min_digits` 的统一读取、各 flush backend 与 openPMD `SetStep()/GetFileName()` 对编号宽度的共同消费、BTD 用它派生 snapshot/buffer 子路径的链路，以及 `write_species` 在 `InitData()/InitDataAfterRestart()`、`FullDiagnostics::InitializeParticleBuffer()` 与 `BTDiagnostics::DerivedInitData()` 中对 species 回退、particle buffer 初始化和 checkpoint 例外的真实分支。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `file_prefix / diag_lo / diag_hi` 继续压到真实 diagnostics 输出路径与几何裁剪链：本轮补清了 `BaseReadParameters()` 的默认前缀和 `BTDiagnostics` 的重复读取、full/boundary-scraping/BTD 在 flush 时对 `m_file_prefix` 的不同派生方式，以及 `diag_lo/hi` 在 boosted moving-window 下的坐标换算、`InitBaseData()` 的 restart 平移、`FullDiagnostics/BTDiagnostics` 对 `diag_dom / diag_box / buffer_box` 的裁剪链和 reduced-domain particle I/O 暂不支持的禁用边界。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `particle_fields_* / plot_raw_fields* / coarsening_ratio` 继续压到真实 diagnostics instance-local 链：本轮补清了 `BaseReadParameters()` 对 particle-field parser、species、`do_average` 与可选 filter 的展开规则，`FullDiagnostics` 按 `field × species × level` 实例化 `ParticleReductionFunctor` 的路径，`ParticleReductionFunctor` 中 `w*value` 沉积、可选加权平均与 filter 同时作用于分子/分母的运行链，以及 `plot_raw_fields*` 只允许 plotfile raw 支路、`coarsening_ratio` 同时进入 diagnostics 几何裁剪、functor coarsen 和 BTD 禁止 coarsening 的边界。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `<diag_name>.adios2_operator.* / adios2_engine.*` 继续压到真实 openPMD backend-options 链：本轮补清了 `FlushFormatOpenPMD` 对 operator/engine 类型与参数前缀的整体收集、`WarpXOpenPMD.cpp::getSeriesOptions()` 里 `"adios2" -> "dataset" -> "operators"` 与 `"engine"` 两块 JSON 的生成条件，以及这些 JSON 最终经 `m_OpenPMDoptions` 进入 `openPMD::Series(...)` 的真实落点。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `<diag_name>.openpmd_backend / openpmd_encoding` 继续压到真实 writer/backend 链：本轮补清了 `FlushFormatOpenPMD` 的 backend 默认值回写、`WarpXOpenPMDPlot` 中 `openpmd[_%0NT].<backend>` 文件模板与 `openPMD::Series(...)` 后端创建链，以及 `openpmd_encoding` 对 `fileBased/groupBased/variableBased` 文件布局、BTD 兼容性和 `m_Series->setIterationEncoding(...)` 的真实影响。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已对 diagnostics 段内部做一轮去重收口：删除了与总表 `<diag_name>.intervals` 重复的 BTD 同名行，保留“总表统一入口 + `num_snapshots_lab / dt_snapshots_lab / dz_snapshots_lab / buffer_size` 等 BTD 特化补充”的结构，避免同名参数在表内出现两次。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `<diag_name>.dump_last_timestep` 继续压到真实末步 flush 链：本轮补清了 `Diagnostics::BaseReadParameters()` 的布尔入口和默认值、`MultiDiagnostics::FilterComputePackFlushLastTimestep()` 对 `force_flush=true` 的统一分派，以及 `WarpXEvolve.cpp` 在最终步/中断退出时、`WarpX::HandleSignals()` 在 checkpoint signal 时复用这条尾部 flush 路径的实际触发点。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `<diag_name>.intervals` 继续压到真实 diagnostics 调度链：本轮补清了 `FullDiagnostics` 对 `intervals` 的强制读取与 `IntervalsParser` 调度、`BoundaryScraping` 的 `queryarr + {"0"}` 默认回退，以及 `BTDiagnostics` 中 `intervals` 与 `num_snapshots_lab` 二选一、再改写成 `BTDIntervalsParser` 的 snapshot 索引生成链；同时明确了 time-averaged diagnostics 还会用 `nextContains()/previousContains()` 反推 averaging window。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `<diag_name>.format` 继续压到真实 writer/backend 分派链：本轮补清了 `BaseReadParameters()` 的 `m_format` 入口、`Full / BTD / BoundaryScraping` 三类 diagnostics 的允许集合差异、`checkpoint` 对“全量状态转储”的兼容性合同，以及 `InitBaseData()` 中 `plotfile / checkpoint / ascent / catalyst / sensei / openpmd` 的实际 flush-backend 构造与 `sensei/ascent` 邻接 `FillBoundary` 路径。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `<diag_name>.diag_type` 继续压到真实 diagnostics 分派链：本轮补清了 `MultiDiagnostics` 工厂按 `diag_type` 在 `Full / TimeAveraged / BackTransformed / BoundaryScraping` 间分派的入口，其中 `TimeAveraged` 实际仍落到 `FullDiagnostics` 类；同时补上了 `BoundaryScraping -> format=openpmd`、`BackTransformed` 的几何/encoding 邻接约束，以及 `ParticleIO.cpp` 中“粒子附加 `phi/E/B` 只允许 `diag_type = Full`”的能力边界。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `diagnostics.enable / diagnostics.diags_names` 继续压到真实 diagnostics 工厂链：本轮补清了 `MultiDiagnostics::ReadParameters()` 的 `enable -> ndiags` gate、`MultiDiagnostics` 构造函数按 `diag_type` 的对象分派、`WarpX::InitData()/InitDiagnostics()` 对整张 diagnostics 列表的初始化/首步 flush 遍历，以及 `WarpX.cpp` 为 `checkpoint_signals` 兼容性检查重新扫描 `diags_names` 的邻接 consumer。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `warpx.numprocs` 继续压到真实 startup/runtime 链：本轮补清了 `WarpX.cpp` 中对维数、MPI 进程总数以及 `RZ+PSATD` 的输入约束，`WarpXAMReXInit.cpp` 为显式进程拓扑触发的 `blocking_factor=1` workaround，`WarpX::PostProcessBaseGrids()` 的手工最粗层切块逻辑，以及 `FullDiagnostics.cpp` 对 `coarsening_ratio * numprocs` 的专门断言分支。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `fine_tag_lo/hi` 继续压到真实 refined-patch 链：本轮补清了 `WarpX.cpp` 中与 `ref_patch_function(x,y,z)` 的互斥/覆盖规则、`WarpXUtil.cpp` 的 boosted-frame 坐标改写、`WarpX::ErrorEst()` 的静态打标 consumer，以及 `WarpXInitData.cpp` 在 RZ 下对 `fine_tag_lo[0] == 0` 的 PML 特例分支。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `start_moving_window_step / end_moving_window_step` 继续压到真实 runtime gate：本轮补清了 `WarpX.H` 中 `moving_window_active(step)` 的区间判定、`WarpXMovingWindow.cpp` 的启动/停止日志与位置更新链，以及 `BTDiagnostics.cpp` 与 `FieldProbe.cpp` 对窗口活动区间边界的 consumer。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `moving_window_dir / moving_window_v` 继续压到真实运行链：本轮补清了 `WarpX.cpp` 的连续注入边界选择、`WarpXMovingWindow.cpp` 的窗口/注入位置更新、`WarpXUtil.cpp` 的 boost-frame 域换算，以及 `BTDiagnostics.cpp` 对 `z` 方向约束和 `dz_snapshots_lab -> dt_snapshots_lab` 的 consumer 链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把两条 `amrex.*` startup 输入继续压到真实初始化链：本轮细化了 `amrex.the_arena_is_managed / amrex.omp_threads`，明确它们都落到 `WarpXAMReXInit.cpp` 的 `overwrite_amrex_parser_defaults()` 覆写链，其中 `omp_threads` 还额外接到 `Source/Python/WarpX.cpp` 的 runtime setter 与 `amrex::OpenMP::set_num_threads(...)`。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `amr.max_level` 继续压到更具体的本地 consumer 链：本轮补清了 `WarpXUtil.cpp` 中 `max_level > 0` 才进入 `fine_tag_lo/hi` refined-patch 几何链的条件分支，以及 `FieldEnergy / FieldMaximum / FieldMomentum / FieldProbe` 构造期用 `max_level + 1` 展开 reduced-diag 多层数据与 header 布局的 consumer 链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `amr.ref_ratio / ref_ratio_vect` 继续压到更具体的本地 consumer 链：本轮补清了 `GuardCellManager.cpp` 中 `moving_window_dir` 对 grow-cell 下界的使用，以及 `PML.cpp` 中 `grid_cba.coarsen(ref_ratio)`、`ncell/ref_ratio[idim]` 和 `IntVect(...)/ref_ratio` 的 coarse/fine PML 缩并链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把两条 `AMReX-owned pass-through` 邻接输入继续压到真实解析链：本轮细化了 `amr.n_cell / amr.max_grid_size`，明确它们落到 `WarpXAMReXInit.cpp` 的 `preparse_amrex_input_int_array(...)` 启动期整数预解析链、`WarpXUtil.cpp` 的 RZ spectral 分块修正链，以及 `WarpXInitData.cpp` 的 coarse-box / MPI 规模性能 warning 链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `Hybrid-QED` 相邻两条继续压到真实解析链：本轮细化了 `warpx.use_hybrid_QED / quantum_xi`，明确它们落到 `WarpX::ReadParameters()` 的解析入口、`WarpXEvolve.cpp` 中插入 `Hybrid_QED_Push(dt)` 的主 field-push gate，以及 `m_quantum_xi_c2 -> WarpX_QED_Field_Pushers.cpp -> warpx_hybrid_QED_push(...)` 的系数传递链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `ParticleHistogram2D` 的 openPMD 输出合同条目继续压到真实解析链：本轮细化了 `<reduced>.openpmd_backend / file_min_digits`，明确它们落到 `ParticleHistogram2D` 构造期的 backend 选择与回写链，以及 `WriteToFile()` 的 `openpmd_%0NT.<backend>` 文件模板、`io::Series` 创建和 axis-label 元数据链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把一组 `FieldProbe` 几何铺设参数继续压到真实解析链：本轮细化了 `<reduced>.x/y/z_probe / x1/y1/z1_probe / target_normal_* / target_up_* / detector_radius`，明确它们落到 `FieldProbe::InitData()` 的点/线/平面三条探针粒子铺设路径，以及平面探针的归一化、叉乘构型、四角求解和二维网格生成链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把一组 `FieldProbe + DifferentialLuminosity2D` 的 reduced-diags 专有条目继续压到真实解析链：本轮细化了 `<reduced>.integrate / do_moving_window_FP` 以及 `<reduced>.bin_number_1/2 / bin_min_1/2 / bin_max_1/2 / openpmd_backend / file_min_digits`，明确它们落到 `FieldProbe::ComputeDiags()` 的积分语义与 moving-window 探针平移链，以及 `DifferentialLuminosity2D` 的二维碰撞能量网格和专有 openPMD 输出文件合同。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把一组 `FieldProbe + ParticleHistogram2D` 的 reduced-diags 专有条目继续压到真实解析链：本轮细化了 `<reduced>.probe_geometry / resolution / interp_order / raw_fields` 以及 `<reduced>.bin_number_abs/ord / histogram_function_abs/ord / value_function/filter_function(...,w)`，明确它们落到 `FieldProbe` 的探测器几何分派、探针粒子铺设与场插值 gate，以及 `ParticleHistogram2D::ComputeDiags()` 的二维分箱、parser 筛选和值累加链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把一组 reduced-diags instance-local histogram/species 条目继续压到真实解析链：本轮细化了 `<reduced>.species / bin_number / bin_min / bin_max / histogram_function(...) / normalization / filter_function(...)`，明确它们落到 `ParticleHistogram / BeamRelevant / DifferentialLuminosity / ParticleHistogram2D` 的 species-container 绑定链、分箱几何构造链，以及 `ParticleHistogram::ComputeDiags()` 的 parser 评估、filter gate 和归一化分支。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把一组 reduced-diags 输出文件格式条目继续压到真实解析链：本轮细化了 `reduced_diags.extension / separator / precision`，明确它们落到 `ReducedDiags` 基类的文件名合同、restart/header 预处理和 `WriteToFile()` 列格式链，并补清 `LoadBalanceCosts / FieldProbe` 这类自定义 writer 对 `precision` 的例外路径。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把一组 reduced-diags 基类/工厂条目继续压到真实解析链：本轮细化了 `warpx.reduced_diags_names / <reduced>.type / reduced_diags.intervals / reduced_diags.path`，明确它们落到 `MultiReducedDiags` 的实例注册与多态调度链、`ReducedDiags` 基类的 `IntervalsParser` 构造链，以及 `UtilCreateDirectory / restart 续写判断 / WriteToFile(step)` 的文件合同链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把一组 time-averaged diagnostics 条目继续压到真实解析链：本轮细化了 `<diag>.time_average_mode / average_period_steps / average_period_time / average_start_step`，明确它们落到 `FullDiagnostics::ReadParameters()` 的模式与互斥检查链、`DerivedInitData()` 的 period 回填链，以及 `DoComputeAndPack()/Flush()` 的 averaging window 判定与归一化写出链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把一组 particle-diagnostics species-local filter 条目继续压到真实解析链：本轮细化了 `<diag>.<species>.additional_variables / random_fraction / uniform_stride / plot_filter_function(...)`，明确它们落到 `ParticleDiag` 构造期的 attribute/filter 安装链，以及 `FlushFormatPlotfile` 与 `WarpXOpenPMD` 中 `RandomFilter / UniformFilter / ParserFilter / GeometryFilter` 共同参与的 `copyParticles(...)` writer-side 筛选链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把一条 multi-source injection 条目和一条 diagnostics species 条目继续压到真实解析链：本轮细化了 `<species>.injection_sources` 与 `<diag>.species`，明确它们分别落到 `PhysicalParticleContainer -> PlasmaInjector(source_name)` 的多注入源工厂链、以及 `Diagnostics::BaseReadParameters()` 之后 `FullDiagnostics / BTDiagnostics / BoundaryScrapingDiagnostics` 三套默认回退与 `ParticleDiag`/BTD buffer 初始化链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把一条 thermal particle-boundary companion 参数和一条 embedded-boundary 电势条目继续压到真实解析链：本轮新增并细化了 `boundary.<species_name>.u_th` 与 `warpx.eb_potential(x,y,z,t)`，明确它们分别落到 `PhysicalParticleContainer -> ParticleBoundariesData::m_uth -> ParticleBoundaries_K.H` 的 thermal re-sampling 链，以及 `PoissonBoundaryHandler::{ReadParameters,BuildParsersEB} -> ablastr::fields::{PoissonSolver,EffectivePotentialPoissonSolver}` 的 EB Dirichlet 分派链，并补上 `ion_beam_extraction` 这一条稳定 validation 入口。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把两条 boundary 相邻参数继续压到真实解析链：本轮细化了 `boundary.field_lo/hi / boundary.particle_lo/hi`，明确它们分别落到 `FieldBoundaries.cpp / ParticleBoundaries.cpp` 的逐方向枚举解析链、`WarpXAMReXInit.cpp` 把 periodic 信息回写到 `geometry.is_periodic` 的几何初始化链，以及 `WarpX.cpp / WarpXInitData.cpp / WarpX_PEC.cpp` 对 PML side 分配、solver/RZ 兼容性和 thermal/reflecting 粒子边界行为的运行时 consumer 链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把两条 AMR 通信/粗网格 gather 相邻参数继续压到真实解析链：本轮细化了 `warpx.do_single_precision_comms / particles.gather_from_main_grid`，明确它们分别落到 `WarpX::ReadParameters()` 中的读取与单精度构建覆写链、`ablastr::utils::communication::FillBoundary/SumBoundary` 及 moving-window/PML/Poisson 通信路径的全局精度模式链，以及 `MultiParticleContainer` 的 species 名注册链、`nSpeciesGatherFromMainGrid()` 汇总链、AMR coarse auxiliary field 分配链和 `Partition.cpp` 的 `nfine_gather = 0` 强制 coarse-grid gather 链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里两条 AMR/moving-window 相邻参数继续压到真实解析链：本轮细化了 `warpx.refine_plasma / n_field_gather_buffer`，明确它们分别落到 `PhysicalParticleContainer::findRefinedInjectionBox()` 的 refined continuous injection gate，以及 `WarpX.cpp` 中与 `n_current_deposition_buffer + 1` 相连的默认值回填链、`BuildBufferMasks()` 的 gather-mask 构造链、coarse auxiliary field 分配链和 `Partition.cpp` 的 coarse/fine gather 分流链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一条 AMR coarse-fine buffer 邻接参数继续压到真实解析链：本轮细化了 `warpx.n_current_deposition_buffer`，明确它落到 `WarpX::ReadParameters()` 的 AMR 条件读取链、level 分配前与 `deposit_on_main_grid / guard_cells.ng_alloc_J.max()` 交织的默认值回填链、`BuildBufferMasks()` 的 current-mask 构造链，以及 `PhysicalParticleContainer::PartitionParticlesInBuffers()` 到 `Partition.cpp` 的 coarse/fine 沉积分流链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一条 `NUniformPerCell` 核心入口继续压到真实解析链：本轮细化了 `<species>.num_particles_per_cell_each_dim`，明确它落到 `PlasmaInjector::setupNuniformPerCell()` 的按维度长度检查与 `RZ` 模数约束链、`InjectorPositionRegular` 的规则单元内布点链、`WarpXInitData.cpp` 的 high-density-box 负载估计路径，以及 `PhysicalParticleContainer::SplitParticles()` 的 split offset 修正链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `random-theta + AMR splitting gate` 的 still-coarse 条目压到真实解析链：本轮细化了 `<species>.random_theta / do_splitting`，明确它们分别落到 `AddParticles.cpp` 里按 cell 生成 `theta_offset` 的 RZ 系粒子初始方位角随机化链，以及 `WarpXParticleContainer::particlePostLocate()` 打 tag 到 `PhysicalParticleContainer::SplitParticles()` 真正生成子粒子的 AMR 上行 split 链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `species initial self-fields` 的 still-coarse 条目继续压到更真实的初始化/求解链：本轮补清了 `<species>.initialize_self_fields / self_fields_required_precision / self_fields_absolute_tolerance / self_fields_max_iters / self_fields_verbosity` 不只在 `PhysicalParticleContainer` 构造函数读取，还会先参与 `WarpXInitData.cpp` 里的 `ComputeSpaceChargeField(reset_fields)` 初始化触发条件；同时明确 species 级 `self_fields_*` override 只进入 `RelativisticExplicitES::AddSpaceChargeField(...)` 的 per-species `computePhi(...)` 求解链，边界电势支路 `AddBoundaryField(...)` 仍继续走 solver 级默认值。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `self_fields + magnetostatic solver` 的 still-coarse 条目压到真实解析链：本轮细化了 `warpx.self_fields_required_precision / absolute_tolerance / max_iters / verbosity`，以及 `warpx.magnetostatic_solver_required_precision / absolute_tolerance / max_iters / verbosity`，明确它们分别落到 `ElectrostaticSolver::ReadParameters()` 与 `PhysicalParticleContainer` 构造函数的全局默认/species override 双入口链，以及 `LabFrameExplicitES / EffectivePotentialES / RelativisticExplicitES / PhiFunctor` 的 `computePhi(...)` consumer 和 `WarpX::ComputeMagnetostaticField()` 对 `computeVectorPotential(...)` 的 backward-compatibility 覆盖链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `from_file + parse_field_function` 的 still-coarse 条目压到真实解析链：本轮细化了 `<laser>.field_function(X,Y,t)`、`<laser>.binary_file_name / lasy_file_name`、以及 `<laser>.time_chunk_size / delay`，明确它们分别落到 `FieldFunctionLaserProfile::init/fill_amplitude` 的平面坐标 parser 编译链、`FromFileLaserProfile::init()` 内 binary/lasy backend 的互斥分派与首块预读链，以及 `update(t) / fill_amplitude()` 共享的时间块缓存窗口与运行时 `t_min - delay` 时间平移链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `gaussian STC + continuous-injection` 的 still-coarse 条目压到真实解析链：本轮细化了 `<laser>.stc_direction / do_continuous_injection / min_particles_per_mode`，明确它们分别落到 `GaussianLaserProfile::init()` 里的默认 `p_X` 覆写、平面正交性检查与 `theta_stc` 派生链，以及 `LaserParticleContainer::{ContinuousInjection,UpdateAntennaPosition,InitData}` 的天线入域/位置更新链和 RZ spoke 粒子布局/权重归一化链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `gaussian laser profile` 的 still-coarse 条目压到真实解析链：本轮细化了 `<laser>.profile_t_peak / profile_duration / profile_waist / profile_focal_distance / phi0 / zeta / beta / phi2`，明确它们分别落到 `GaussianLaserProfile::init()` 的强制读取或可选读取链，以及 `fill_amplitude()` 中的 `oscillation_phase / diffract_factor / inv_complex_waist_2 / stretch_factor / stc_exponent` 这些真实 runtime 公式 consumer。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `laser plane-geometry` 的 still-coarse 条目压到真实解析链：本轮细化了 `<laser>.position / polarization / direction`，明确它们分别落到 boosted-frame 天线位置改写、`Transform/InverseTransform` 的平面原点、`p_X -> p_Y -> u_X/u_Y` 的激光平面基底构造、`CommonLaserParameters::{p_X,nvec}` 的 profile 初始化输入，以及 `calculate_laser_plane_coordinates()` 与 `update_laser_particle()` 的实际运行时 consumer。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `laser common-entry` 的 still-coarse 条目压到真实解析链：本轮细化了 `lasers.names`、`<laser>.e_max / a0 / wavelength / profile`，明确它们分别落到 `WarpX + MultiParticleContainer + LaserParticleContainer` 的 laser-name registry 与 side-list 映射链、`a0/e_max -> CommonLaserParameters::e_max` 的公共幅值归一化链、`wavelength` 对 `a0` 换算和 Gaussian `k0` 相位公式的双重 consumer，以及 `laser_profiles_dictionary` 对 `gaussian / parse_field_function / from_file` 的对象工厂分派。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `temperature-deposition + diagnostics/openPMD` 的 still-coarse 条目压到真实解析链：本轮细化了 `<species>.do_temperature_deposition`、`<diag>.buffer_flush_limit_btd`、`<diag>.adios2_operator.type`、以及 `<diag>.fields_to_plot`，明确它们分别落到 `PhysicalParticleContainer + MultiParticleContainer + FullDiagnostics` 的 `T_<species>` 场分配/每步沉积/diagnostics functor 链、openPMD BTD buffer 聚合刷盘阈值、ADIOS2 operator backend JSON 分派键，以及 `BaseReadParameters + FullDiagnostics/BTDiagnostics` 的 field-functor 与 species-local `rho/T` 校验链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `resampling thresholds` 的 still-coarse 条目压到真实解析链：本轮细化了 `<species>.resampling_min_ppc / resampling_trigger_max_avg_ppc`，明确它们分别落到 `LevelingThinning / VelocityCoincidenceThinning` 的 per-cell early-return gate，以及 `ResamplingTrigger::triggered()` 里按全局 `numPts()` 计算 `avg_ppc` 的触发门槛。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `momentum-distribution main entry + resampling` 的 still-coarse 条目压到真实解析链：本轮细化了 `<species>.momentum_distribution_type / do_resampling / resampling_algorithm / resampling_trigger_intervals`，明确它们分别落到 `SpeciesUtils::parseMomentum()` 的 `InjectorMomentum*` 总分派链、`PhysicalParticleContainer + MultiParticleContainer + WarpXEvolve` 的重采样总 gate、`Resampling` 对 `LevelingThinning / VelocityCoincidenceThinning` 的对象工厂分派，以及 `ResamplingTrigger::triggered()` 的 intervals-plus-avg-ppc 触发链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `momentum-distribution companion` 的 still-coarse 条目压到真实解析链：本轮细化了 `<species>.theta_distribution_type / beta_distribution_type / radial_numpercell_power`，明确它们分别落到 `SpeciesUtils::parseMomentum()` 触发的 `TemperatureProperties / VelocityProperties` 分派链，以及 `PlasmaInjector + AddParticles` 的径向采样与权重修正链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `user attributes + ionization companion parameters` 的 still-coarse 条目压到真实解析链：本轮细化了 `<species>.addIntegerAttributes / addRealAttributes / do_adk_correction / physical_element / ionization_product_species / ionization_initial_level`，明确它们分别落到 `PhysicalParticleContainer` 构造期的 runtime component 扩展与 parser 编译链、`DefaultInitialization.H` 的属性初始化、`InitIonizationModule()` 的 ADK 表参数装配，以及 `MultiParticleContainer::{mapSpeciesProduct,doFieldIonization}` 的产物 species 绑定链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `rigid injection + backward propagation + splitting + boundary buffer` 的 still-coarse 条目压到真实解析链：本轮细化了 `<species>.zinject_plane / rigid_advance / do_backward_propagation / split_type / save_particles_at_*`，明确它们分别落到 `RigidInjectedParticleContainer::{InitData,Evolve,PushPX,PushP}` 的注入平面与未注入粒子回退链、boosted-frame `MapParticletoBoostedFrame()` 的反向注入分支、`WarpXParticleContainer::particlePostLocate()` 到 `PhysicalParticleContainer::SplitParticles()` 的 AMR split 模板分派，以及 `ParticleBoundaryBuffer` 的逐边界缓存激活表。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `boundary potential + injection bounds + field ionization` 的 still-coarse 条目压到真实解析链：本轮细化了 `boundary.potential_lo/hi_x/y/z`、`<species>.xmin/ymin/zmin/xmax/ymax/zmax`、以及 `<species>.do_field_ionization`，明确它们分别落到 `PoissonBoundaryHandler::{ReadParameters,BuildParsers,DefinePhiBCs}` 的边界电势/初始 Poisson solve 链、`PlasmaInjector + AddParticles` 的注入区域几何 gate，以及 `PhysicalParticleContainer + MultiParticleContainer + AddParticles` 的 `ionizationLevel` 属性安装与运行时 charge-state 更新链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `grid_type + field/pusher/solver + psatd guard` 的 mixed-consumer 条目压到真实解析链：本轮细化了 `warpx.grid_type`、`algo.field_gathering / particle_pusher / maxwell_solver`，以及 `psatd.nx/ny/nz_guard`，明确它们分别落到 `ComputeDt + GuardCellManager + FiniteDifference/SpectralSolver + field allocation` 的总几何 gate、`ReadParameters + runtime kernel dispatch + openPMD metadata` 的算法选择链，以及 `GuardCellManager + PML` 共用的 PSATD guard-cell 覆盖入口。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `sorting + shared-memory deposition` still-coarse 条目压到真实解析链：本轮细化了 `warpx.sort_particles_for_deposition / sort_idx_type / sort_bin_size`，以及 `warpx.do_shared_mem_current_deposition / shared_tilesize / shared_mem_current_tpb`，明确它们分别落到 `WarpXEvolve -> MultiParticleContainer -> AMReX SortParticlesForDeposition` 的排序节奏/重排分派链，以及 `WarpXParticleContainer::{CurrentDeposition,DepositCharge}` 的 `DenseBins + tile-geometry + shared-memory kernel launch` 路径。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `psatd runtime-mode` still-coarse 条目压到真实解析链：本轮细化了 `psatd.periodic_single_box_fft / update_with_rho / v_galilean / use_default_v_galilean / v_comoving / do_time_averaging`，明确它们分别落到 `WarpX::ReadParameters()` 中的单-box FFT 前提、`rho`-update 默认值与兼容性断言、Galilean/comoving 速度的默认值选择与互斥边界，以及 `SpectralSolver + InitLevelData + Evolve` 的谱算法分派、平均场分配与主循环同步链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `collision placement / filter / particle-shape / implicit-crossing / EM-medium` still-coarse 条目压到真实解析链：本轮细化了 `collisions.split_momentum_push`、`warpx.filter_npass_each_dir / use_filter_compensation`、`algo.charge_deposition`、`algo.particle_shape`、`particles.max_grid_crossings`、以及 `algo.em_solver_medium`，明确它们分别落到 `WarpX::ReadParameters() -> OneStep/OneStep_nosub` 的碰撞插位分支、`WarpX::InitFilter + SpectralSolverRZ` 的过滤器构造链、当前源码树里 charge-deposition 选择接口的实现边界、`nox/noy/noz` 总开关与多条沉积/温度统计模板分派、implicit guard-cell / sigma-deposition 预算链，以及 `EvolveE` vs `MacroscopicEvolveE` 的 Maxwell 主分支。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `pulsed_decay + bremsstrahlung` still-coarse 条目压到真实解析链：本轮细化了 `<collision_name>.decay_rate(x,y,z,t) / fixed_product_weight / productA_temperature_eV / productB_temperature_eV`，以及 `<collision_name>.Z / multiplier / create_photons / koT1_cut`，明确它们分别落到 `PulsedDecay` 的 `cell-center decay-rate parser + macro-product count/weight + thermal broadening` 主链，以及 `BremsstrahlungFunc / PhotonCreationFunc` 的 `cross-section table selection + probability/weight scaling + photon materialization + energy cutoff` 链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `nuclearfusion / pairwise-Coulomb correction` still-coarse 条目压到真实解析链：本轮细化了 `<collision_name>.scattering_angle_model`，以及 `collisions.correct_energy_momentum / energy_fraction / energy_fraction_max / beta_weight_exponent / energy_correction_sort_by_weight`，明确它们分别落到 `ParticleCreationFunc -> fusion momentum initialization` 的产物发射角分支，以及 `BinaryCollision` 中“全局默认 + collision 覆盖 + tile-level correction pass”的 Coulomb 修正链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `background_mcc / dsmc` still-coarse 条目压到真实解析链：本轮细化了 `<collision_name>.scattering_processes / <scattering_process>_cross_section / <scattering_process>_energy / ionization_species / ionization_target_species`，明确它们分别落到 `BackgroundMCCCollision` 与 `DSMCFunc` 的 `ScatteringProcess` 构造链、`SplitAndScatterFunc` 的产物布局判断，以及 `BinaryCollision` 对 DSMC impact-ionization 的 target-species 顺序修正。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `binary-event + background collision` still-coarse 条目压到真实解析链：本轮细化了 `<collision_name>.event_multiplier / probability_threshold / probability_target_value`，以及 `<collision_name>.background_density / background_temperature / background_mass / background_charge_state / background_type`，明确它们分别落到 `LinearCompton / LinearBreitWheeler / NuclearFusion` 三个 event functor 的高概率重标定链，以及 `BackgroundMCC / BackgroundStopping` 的常数-vs-parser 输入、初始化期约束和 tile-level 物理 consumer。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里 `pairwise Coulomb` 邻接 still-coarse 条目继续压到真实解析链：本轮细化了 `<collision_name>.CoulombLog` 与 `<collision_name>.use_global_debye_length`，明确它们分别落到 `PairWiseCoulombCollisionFunc` 的 Coulomb-log 读取与自动估算分支，以及 `PairWiseCoulombCollisionFunc -> BinaryCollision -> CollisionHandler -> global_debye_length MultiFab` 的跨层 gate。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `lattice / collisions` still-coarse 条目压到真实解析链：本轮细化了 `lattice.elements / lattice.reverse / <element_name>.type`，以及 `collisions.collision_names / <collision_name>.type / species / product_species / ndt_supercycle / ndt_subcycle`，明确它们分别落到 `AcceleratorLattice::ReadLattice()` 的递归元素工厂与顺序控制，以及 `CollisionHandler + CollisionBase + BinaryCollision(+BinaryCollisionUtils)` 的 collision 对象工厂、容器绑定、产物 species 分派和 supercycle/subcycle 调度链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `maxlevel_extEMfield_init + fluid external-field` still-coarse 条目压到真实解析链：本轮细化了 `warpx.maxlevel_extEMfield_init` 与 `<fluid_species_name>.E/B_ext_init_style`，明确它们分别落到 `WarpX::ReadParameters() + WarpXInitData.cpp` 的 external-grid 初始化层级上界，以及 `WarpXFluidContainer::ReadParameters() + GatherAndPush()` 的 parser-based fluid external field gate。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 AMReX-owned / fluid / laser / external-grid still-coarse 条目压到真实解析链：本轮细化了 `amr.max_grid_size`、`fluids.species_names`、`lasers.deposit_on_main_grid`、`warpx.E_ext_grid_init_style` 与 `warpx.E/B_external_grid`，明确它们分别落到 AMReX-owned grid-partition 输入与 WarpX 本地消费者、`WarpX::ReadParameters() + MultiFluidContainer` 的 fluid 子系统启用链、`MultiParticleContainer -> LaserParticleContainer` 的主网格沉积 gate，以及 `ExternalFieldParams + WarpXInitData + WarpXMovingWindow` 的 constant/parser/file 三路外场初始化链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批粒子初始化 still-coarse 条目压到真实解析链：本轮细化了 `particles.do_tiling`、`<species>.species_type / charge / mass / profile / flux_profile`、`particles.E/B_ext_particle_init_style` 与 `particle_thermalizer.species`，明确它们分别落到 AMReX 启动期 tiling 默认值覆写、`PhysicalParticleContainer` 的 species 属性优先级链、`SpeciesUtils/PlasmaInjector` 的 density/flux 分派、`MultiParticleContainer + GetExternalEBField + WarpXInitData` 的粒子外加场初始化路径，以及 `ParticleThermalizer::applyThermalizer` 的按-species 过滤逻辑。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 species/runtime 与 load-balance / mirror / 外场初始化 still-coarse 条目压到真实解析链：本轮细化了 `particles.species_names / use_fdtd_nci_corr / rigid_injected_species`、`<species>.injection_style / do_continuous_injection / density_min / density_max / do_not_deposit / do_not_gather / do_not_push`，以及 `algo.load_balance_* / costs_heuristic_*`、`warpx.do_dynamic_scheduling / roundrobin_sfc / split_high_density_boxes*`、`warpx.num_mirrors / mirror_z / mirror_z_width / mirror_z_npoints`、`warpx.B_ext_grid_init_style`，明确它们分别落到 species-name 到 container-index 的主索引链、连续注入与粒子生成 runtime gate、`WarpXRegrid.cpp` 的 load-balance 触发与采纳逻辑、`PostProcessBaseGrids()` 的高密度 box 预切分、`ApplyMirrors()` 的域内场清零路径，以及 `ExternalFieldParams` 的外加磁场初始化分派链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批边界/PML still-coarse 条目压到真实解析链：本轮细化了 `boundary.reflect_all_velocities`、`boundary.verboncoeur_axis_correction`、`warpx.pml_ncell / pml_delta / do_pml_in_domain / pml_has_particles / do_pml_j_damping / v_particle_pml / do_pml_dive_cleaning / do_pml_divb_cleaning`，以及 `do_similar_dm_pml`，明确它们分别落到 reflecting 粒子边界速度翻转分支、axis-volume correction、PML 几何与通信构造、PML 中粒子/电流相关更新，以及 PML cleaning 子系统的真实 consumer。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里剩余几条 still-coarse 的 QED 尾项继续压到真实解析链：本轮细化了 `<species>.do_qed_virtual_photons / qed_virtual_photons_do_beam_size_effect`、`<species>.do_classical_radiation_reaction`、`qed_schwinger.y_size` 与 `qed_schwinger.xmin/ymin/zmin/xmax/ymax/zmax`，明确它们分别落到 virtual-photon 生成 pass、Boris+RR 专用推进分支，以及 Schwinger 2D 体积归一化与 `ComputeSchwingerGlobalBox()` 空间掩膜路径。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批仍停留在参数手册原句的 QED / Schwinger 条目压到真实解析链：本轮细化了 `qed_qs.photon_creation_energy_threshold`、`<species>.do_qed_quantum_sync / qed_quantum_sync_phot_product_species`、`<species>.do_qed_breit_wheeler / qed_breit_wheeler_{ele,pos}_product_species`、`qed_qs/qed_bw.lookup_table_mode`、`qed_qs/qed_bw.chi_min`、`warpx.do_qed_schwinger`、`qed_schwinger.{ele,pos}_product_species` 与 `qed_schwinger.threshold_poisson_gaussian`，明确它们分别落到 `InitQuantumSync / InitBreitWheeler / doQEDSchwinger`、species 构造期 optical-depth 属性挂接、以及 Schwinger 采样分支阈值的真实 consumer。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里两条 parallelization 邻接调试开关从说明书语气压到真实解析链：本轮细化了 `warpx.safe_guard_cells` 与 `ablastr.fillboundary_always_sync`，明确它们分别落到 `GuardCellManager/WarpXComm/WarpXMovingWindow` 的全量 guard-cell 通信放大路径，以及 `ablastr::utils::communication::FillBoundary(...)` 内部在 `FillBoundary` 与 `FillBoundaryAndSync` 之间的切换逻辑。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 startup / verbosity 邻接条目从说明书语气压到真实解析链：本轮细化了 `warpx.limit_verbose_step`、`warpx.always_warn_immediately`、`warpx.abort_on_warning_threshold` 和 `warpx.serialize_initial_conditions`，明确它们分别落到 `WarpXEvolve.cpp` 的日志节流、`initialize_warning_manager()` 到 `WarnManager::RecordWarning(...)` 的即时打印/阈值终止链，以及 `AddParticles.cpp` 里的 OpenMP 初始化串行化开关。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里 `reduced_diags.path / extension / separator / precision` 四条从“全局默认 + 实例覆盖”的摘要语气继续压到真实 consumer：本轮明确了目录创建、restart 旧文件探测、`MultiReducedDiags::WriteToFile(step)` 分派、以及各派生类 header/data 文本格式写出如何统一消费这四条参数。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 BTD snapshot/buffer 相邻条目从摘要语气压到真实解析链：本轮细化了 `<diag>.dt_snapshots_lab`、`<diag>.dz_snapshots_lab`、`<diag>.buffer_size` 和 `<diag>.do_back_transformed_particles`，明确它们分别落到 snapshot 时间轴换算、buffer 几何建立、`compute_max_step_from_btd` 运行步数外推，以及 `MultiParticleContainer::SetDoBackTransformedParticles(...)` 的粒子容器状态改写路径。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里两条 diagnostics 邻接运行时开关从参数手册语气压到真实解析链：本轮细化了 `warpx.synchronize_velocity_for_diagnostics` 与 `warpx.write_diagnostics_on_restart`，明确它们分别落到 `WarpXEvolve.cpp` 的半步速度同步路径和 `WarpXInitData.cpp` 的 restart 后首帧 diagnostics 补写路径。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 diagnostics 调度与 SENSEI 尾项从参数手册语气压到真实解析链：本轮细化了 `<diag>.intervals`、`<diag>.dump_last_timestep`、`<diag>.sensei_config` 和 `<diag>.sensei_pin_mesh`。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 diagnostics 尾项从参数手册语气压到真实解析链：本轮细化了 `<diag>.buffer_flush_limit_btd`、`<diag>.adios2_operator.type` 与 `<diag>.fields_to_plot`。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 openPMD / I/O 邻接尾项从文档链接语气压到真实解析链：本轮细化了 `<diag>.adios2_operator.parameters.* / adios2_engine.type / adios2_engine.parameters.*`、`<diag>.dump_rz_modes` 和 `warpx.mffile_nstreams`。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 particle-field 与 reduced-diags 节奏条目从说明书语气压到真实解析链：本轮细化了 `<diag>.particle_fields_to_plot / particle_fields_species / particle_fields.<field>.do_average / particle_fields.<field>(...) / particle_fields.<field>.filter(...)`，以及 `reduced_diags.intervals`。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / particle-output / reduced-diags instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 still-coarse 的 diagnostics instance-local 条目继续压到真实解析链：本轮细化了 `<diag>.plot_raw_fields / plot_raw_fields_guards / coarsening_ratio / file_prefix / file_min_digits / diag_lo / diag_hi / write_species / species / <species>.variables`。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量还没压成 parser/consumer 链的 diagnostics / particle-output / reduced-diags 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 time-averaged、BTD 与 reduced-diags 覆盖参数从说明书语气压到真实解析链：本轮细化了 `<diag>.time_average_mode / average_period_steps / average_period_time / average_start_step`、`<diag>.num_snapshots_lab / dt_snapshots_lab / dz_snapshots_lab / buffer_size / do_back_transformed_fields / do_back_transformed_particles`，以及 `reduced_diags.path / extension / separator / precision`。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 diagnostics instance-local 条目从说明书语气压到真实解析链：本轮细化了 `ParticleDiag` 里的 `<diag>.<species>.additional_variables / random_fraction / uniform_stride / plot_filter_function(...)`、`warpx.field_io_nfiles / particle_io_nfiles`，以及 `MultiReducedDiags` 里的 `warpx.reduced_diags_names / <reduced_diag>.type`。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及 time-averaged / BTD / particle-output 里仍偏文档化的 instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批典型 `AMReX-owned pass-through + diagnostics` 顶层 gate 从“文档复述”压成真实源码边界：本轮细化了 `amr.ref_ratio / ref_ratio_vect`、`diagnostics.enable / diagnostics.diags_names`、`<diag_name>.diag_type / format`、`<diag_name>.openpmd_backend / openpmd_encoding`、以及 `amrex.async_out / async_out_nfiles / abort_on_unused_inputs / use_profiler_syncs`。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through 与 instance-local mixed-consumer 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里剩余的旧式计划章节编号整批迁到当前书稿语义：本轮补齐了 runtime / startup、AMR / boundary、filter / deposition / pusher / solver、PSATD / hybrid / macroscopic、collision / QED，以及 diagnostics 零散尾项的 `第…章` 写法。实测表内旧编号残留数已从 `142` 进一步清到 `0`；当前 `parameter-map` 这条线剩下的工作，不再是章节编号迁移，而是继续对“初步源码命中”做更严格的 `ParmParse` / consumer 二次人工复核。
- 2026-05-18：继续推进阶段 A 总索引人工收口，已开始把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 从“文档原句 + 粗源码命中”压到真实解析链：本轮集中细化了 `WarpX::ReadParameters()` 主控参数，覆盖 `warpx.random_seed`、`algo.evolve_scheme`、`break/checkpoint_signals`、`warpx.cfl / const_dt / max_dt`、`warpx.use_filter`、`algo.current_deposition / field_gathering / particle_pusher / maxwell_solver`、`psatd.nox/noy/noz / current_correction / JRhom`、以及 `do_subcycling / override_sync_intervals / do_device_synchronize / sort_intervals / do_shared_mem_charge_deposition`。当前这条线的重点已经明确转成：逐批把“初步源码命中”压成真实 `ParmParse + compatibility checks + downstream consumers` 三层摘要。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `parameter-map` 的下一批子系统参数也压到源码级解析链：本轮细化了 `hybrid_pic_model.*`、`external_vector_potential.*`、`interpolation.galerkin_scheme`、`warpx.field/current_centering_*`、`warpx.do_current_centering`、`warpx.do_dive_cleaning`、`warpx.do_initial_div_cleaning`、`warpx.projection_div_cleaner.{rtol,atol}`、以及 `warpx.use_hybrid_QED / quantum_xi`。当前这条线已经从“章节语义收口”转入真正的 parser/sub-parser 精读阶段。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `parameter-map` 的又一批 still-coarse 条目压到真实解析链：本轮细化了 `particles.deposit_on_main_grid / gather_from_main_grid`、`particle_thermalizer.*`、`algo.macroscopic_sigma_method`、`macroscopic.sigma/epsilon/mu(_function)`、以及 `warpx.grid_type`。当前 `parameter-map` 剩余工作已经进一步收缩到更零散的 grouped alias、AMReX pass-through、以及 mixed-consumer 摘要条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已完成 [module-inventory.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/module-inventory.md) 的两条靠前 open item：一方面清掉了最后 3 个 `其他源码/待定` 悬空条目，把 `Source/Fields.H` 归并到 `WarpX 主类`、把 `Source/Make.WarpX` 和根 `Source/Make.package` 归并到 `构建系统`；另一方面新增了“自动模块名 -> 阶段 0-14”的人工合并总表，使整张索引可以直接按 [warpx-source-reading-framework.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/warpx-source-reading-framework.md) 的 15 个精读阶段导航。当前这条总索引线的下一前沿已经前移到 `parameter-map` 与 `literature-map` 的人工复核，而不再是 `module-inventory` 的分类悬空项。
- 2026-05-18：继续推进阶段 C 样章收口，已按计划文档把样章对象锁定为 [02-pic-loop.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/manuscript/chapters/02-pic-loop.md) 和 [03-warpx-evolve.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/manuscript/chapters/03-warpx-evolve.md)，新增正式审查记录 [sample-chapter-audit-2026-05-18.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/sample-chapter-audit-2026-05-18.md)，并把这两章缺的源码版本声明、参数示例、最小运行案例、进一步阅读和练习题补齐；当前第 2/3 章已达到“样章可审阅”状态，下一前沿不再是补骨架，而是把同等级审查逐步扩展到后续核心章节。
- 2026-05-18：继续推进阶段 C 最小实跑验证，已首次在本地真实运行 `Langmuir` 与 `uniform_plasma` 两条应用主线：`runs/stage-c-validation/langmuir_1d` 已用无沙箱 MPI 成功跑通 `inputs_test_1d_langmuir_multi`，并手工复现 `analysis_1d.py` 的核心断言，得到解析场相对误差 `1.70e-3`、`divE-rho/eps0` 相对误差 `8.35e-12`，因此已形成运行级强基准；`runs/stage-c-validation/uniform_plasma_2d` 已成功跑通 `inputs_test_2d_uniform_plasma` 并生成 `diag1000010`，但当前本机仍缺 `yt/openpmd_viewer`，因此这轮只拿到 workflow/checksum-baseline 级运行证据，还没有独立 physics hard assert。
- 2026-05-18：继续推进阶段 B 基础物理收口，已新增 [基础章节文献清单](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/foundations-literature-list.md)，把第 1 / 2 章当前真正依托的来源压成独立窄表：现已明确 `Birdsall 1985`、`Dawson 1983` 可直接作为正文证据，而 `Hockney-Eastwood` 与 `Yee 1966` 当前仍只到 acquisition / metadata / abstract-level 边界；相关清单已回链到第 1 章、第 2 章和 `docs/literature-map.md`，因此阶段 B 当前可视为 chapter-writing 与 literature-list 两侧都已收口。
- 2026-05-18：继续推进阶段 B 基础物理重写，已把 `manuscript/chapters/02-pic-loop.md` 从“时间层和主循环骨架”扩成更完整的数值边界章：当前已正式补入 `\omega_p` 作为最快 plasma timescale、`\lambda_D` 作为 shielding 分辨率边界、`ComputeDt()` 与 `CartesianYee/Nodal/CKC::ComputeMaxDt()` 的 CFL 决策层，以及 Yee/Nodal/CKC 从差分算子开始改写数值色散合同这一层；这部分边界现已和基础章节文献清单一起形成阶段 B 的完整收口。
- 2026-05-18：启动并推进阶段 B 基础物理重写，已把 `manuscript/chapters/01-kinetic-models.md` 从短版总述重写成分层基础模型章，正式补入 `Vlasov` 作为相空间守恒律与 Liouville 图像、`C[f]+S-L` 碰撞/源汇边界、`Vlasov-Maxwell` 的源项与约束传播、`Vlasov-Poisson` 的 electrostatic 极限，以及宏粒子/非等权粒子/shape factor/噪声与 Debye 统计时间尺度的文献边界；相关 `leapfrog/CFL/\omega_p/\lambda_D/数值色散` 边界现已继续回填到第 2 章。
- 2026-05-18：回填并收口第 3 章阶段 3 主线，已把 `ComputeDt()` 的 solver/geometry 决策表、`const_dt` 与 `dt_update_interval` 互斥、自适应步长入口、moving-window 的 active 区间与两类连续注入区别、subcycling 的 coarse/fine current 保存-恢复语义，以及 JRhom 的输入字符串语义与功能边界正式写回 `manuscript/chapters/03-warpx-evolve.md`，并关闭对应 `TODO` 条目。
- 2026-05-18：完成 FieldSolver 验证主线收口，已把 `nci_fdtd_stability`、`nci_psatd_stability`、`electrostatic_sphere`、`implicit`、`ohm_solver_*` 五组 regression 的 `analysis/checksum` 语义统一压回 `notes/code-reading/fieldsolver/13/14` 与第 6 章 `6.11`，明确区分强断言、谱/增长率历史值回归和纯 checksum/可视化基线；当前结论仍基于源码与脚本精读，未运行本地 WarpX 二进制。
- 2026-05-18：继续推进沉积主线，新增 `notes/code-reading/particles/32-current-deposition-continuity-and-geometry-boundaries.md`，把第 5 章真正要保住的离散连续性合同、implicit 时间层恢复，以及 `RZ / 1D_Z / RCYLINDER / RSPHERE / 3D / XZ` 的 `CurrentDeposition.H` 实现边界压成独立笔记，并回填 `manuscript/chapters/05-deposition-shapes.md`。
- 2026-05-18：继续推进沉积主线，新增 `notes/code-reading/particles/33-mass-matrix-and-temperature-variance-deposition.md`，把 `MassMatricesDeposition.H`、`TemperatureDeposition.H` 和 `VarianceAccumulationBuffer` 压成一条“线性响应矩阵与统计矩沉积”主线，并把第 5 章从只讲 `rho/J` 扩成 `rho/J + MassMatrices + temperature/variance` 的三分结构。
- 2026-05-18：继续推进沉积主线，新增 `notes/code-reading/particles/34-sync-current-rho-and-source-synchronization.md`，把 `SyncCurrentAndRho()`、`SyncCurrent()`、`SyncRho()`、owner-mask 去重、buffer 合并、filter 和 PEC 边界反射压成正式笔记，并把第 5 章中“沉积后为什么还要同步”扩写成 solver/algorithm 时序、same-level/fine-coarse source synchronization 与 boundary 处理的分层说明。
- 2026-05-18：继续推进沉积主线，新增 `notes/code-reading/particles/35-current-correction-and-vay-validation-map.md`，把 `Langmuir + current_correction` 与 `vay_deposition` 两组 regression 的真实断言边界压成正式验证图，并把第 5 章里 source-synchronization 的验证层分工写清：前者是 `解析场解 + divE-rho/eps0` 组合验证，后者是更窄的 `PSATD + collocated + Vay deposition` 离散 Gauss-law 断言。
- 2026-05-13：完成初始信息整合和总计划。
- 2026-05-14：完成第一版 Markdown-first 书稿骨架和核心章节草稿。
- 2026-05-14：完成阶段 A 自动索引，并启动阶段 C 主生命周期与 `Evolve` 样章重写。
- 2026-05-14：继续扩写粒子推进与沉积样章，已把 `FieldGather.H`、`ShapeFactors.H`、`ChargeDeposition.H`、`CurrentDeposition.H`、`UpdateMomentumVay.H`、`UpdateMomentumHigueraCary.H` 的关键源码原文写入第 4/5 章。
- 2026-05-14：根据反馈暂停局部模块扩写，完成 `../warpx/Source` 全局模块盘点，并建立全源码精读框架。
- 2026-05-14：阶段 0 第一批完成，已为全部 Source 顶层模块建立 `notes/code-reading/<module>/README.md` 精读入口和总索引。
- 2026-05-14：阶段 1 已启动，完成 `WarpX.H/WarpX.cpp/Fields.H` 主类状态与 field registry 第一轮精读笔记 `notes/code-reading/root/01-warpx-state-map.md`，并继续完成构造期/level 分配笔记 `notes/code-reading/root/02-construction-and-level-allocation.md`。
- 2026-05-14：阶段 2 Initialization 已启动，完成 `WarpXInitData.cpp` 初始化调用链、外场初始化、`PlasmaInjector` 总图、`SpeciesUtils` 密度/动量分派、`AddParticles/AddPlasma` 粒子创建 kernel、projection div cleaner、Gaussian beam/openPMD 粒子文件注入、温度/漂移速度属性八篇精读笔记。
- 2026-05-14：阶段 3 全局时间推进已启动，完成 `WarpXComputeDt.cpp` 与自适应时间步、`OneStep_sub1()`/`OneStep_JRhom()`、`Utils/WarpXMovingWindow.cpp` moving window、连续注入和 boosted-frame 速度变换精读。
- 2026-05-14：阶段 4 FieldSolver 已启动，完成 `WarpXPushFieldsEM.cpp` 顶层分派、`FiniteDifferenceSolver/EvolveE/B/F/G.cpp` 第一轮精读、Cartesian Yee/Nodal/CKC 差分算法精读、FDTD PML split-field 更新、PML sigma/damping/current、RZ/RCYLINDER/RSPHERE 非 Cartesian FDTD、PSATD 谱求解主流程、标准/Galilean PSATD 系数和 current correction、PSATD-JRhom 多次源项沉积与一阶/二阶谱更新、RZ PSATD/Hankel transform、`Tools/Algorithms/psatd.ipynb` 系数推导、electrostatic/magnetostatic Poisson/vector-Poisson 求解路径、implicit/hybrid 首轮精读、`NonlinearSolvers` 的 Newton/Picard/PETSc 细化、`HybridPICModel` 深层拆分、第 6 章 6.10 Hybrid PIC 正文回填、FieldSolver 验证样例索引、FieldSolver analysis 判据精读，以及第 6 章 6.11 验证判据正文回填。
- 2026-05-15：阶段 5 Boundary / AMR 已实质启动，完成 `notes/code-reading/boundary/00-field-boundary-parameters.md`、`01-pml-data-and-update.md`、`02-pec-insulator-silver-mueller.md`、`03-boundary-parameter-table.md`、`04-silver-mueller-internal-stencil.md`，把 field/particle periodic 一致性、`WarpXFieldBoundaries.cpp` 顶层分派、PEC/PMC/PECInsulator 的场与 rho/J 镜像规则、边界参数总表、Silver-Mueller half-push 条件与内部 guard-cell 递推、PML `SigmaBox` / split-field / PML current 数据流回填到第 7 章。
- 2026-05-15：继续推进阶段 5，完成 `notes/code-reading/embedded-boundary/00-eb-initialization.md`，梳理 `Enabled.*` 运行时 EB 开关、`WarpX::InitEB()` 的 AMReX EB2 构建、`ComputeDistanceToEB()` 的 signed-distance 场、以及 `EmbeddedBoundaryInit.*` 中 reduced-shape / stair-case / ECT 更新标记，并回填第 7 章的 embedded boundary 初始化层。
- 2026-05-15：继续推进阶段 5，完成 `notes/code-reading/embedded-boundary/01-face-extensions.md`，梳理 `S_stab` 稳定性阈值、`flag_info_face/flag_ext_face` 语义、one-way / eight-ways extension、BCK fallback，以及 `FaceInfoBox` 如何把借用关系传给 ECT `B` 更新，并回填第 7 章的 face extension 层。
- 2026-05-15：继续推进阶段 5，完成 `notes/code-reading/embedded-boundary/02-particle-scraping-and-deposition-near-eb.md`，梳理 `scrapeParticlesAtEB()` 的 signed-distance 判定、`DistanceToEB` 法向重建、`ParticleBoundaryProcess::Absorb()` 的 invalid-id 吸收语义，以及 `save_particles_at_eb` / `ParticleBoundaryBuffer` 的交点回溯与 scraped buffer 记录，并回填第 7 章的粒子侧 EB 层。
- 2026-05-15：切回阶段 5 的并行/AMR 通信主线，完成 `notes/code-reading/parallelization/00-guard-cell-model.md`，梳理 `GuardCellManager` 的 guard-cell 分配上限与阶段性需求、`FillBoundaryE/B/F/G/Aux` 的 copy/sync 语义、`WarpXSumGuardCells` 的 source accumulation 语义，以及 `SyncCurrent()` / `SyncRho()` 中 coarse-fine / owner-mask 去重的基本结构，并回填第 7 章的 guard-cell 通信层。
- 2026-05-15：继续推进 `Parallelization`，完成 `notes/code-reading/parallelization/01-current-rho-sync-paths.md`，梳理 `SyncCurrent()` / `SyncRho()` 的 finest-to-coarsest 级联、`mf_comm`、`OwnerMask` 去重、`RestrictCurrentFromFineToCoarsePatch` / `AddCurrentFromFineLevelandSumBoundary` 与对应 rho 路径，并回填第 7 章的 coarse-fine source 同步层。
- 2026-05-15：继续推进 `Parallelization`，完成 `notes/code-reading/parallelization/02-regrid-and-load-balance.md`，梳理 `CheckLoadBalance()` 的触发条件、SFC/knapsack 候选映射与效率阈值比较、`RemakeLevel()` 当前只支持同 `BoxArray` 的 rank 重映射、fields/EB/PSATD/buffer masks/particle boundary buffer/diagnostics 的整体重建，以及 heuristic / timer 两类 costs 更新模型，并回填第 7 章的 load-balance / remake-level 层。
- 2026-05-16：继续推进 `Parallelization / AMR`，完成 `notes/code-reading/parallelization/03-amr-coarse-fine-substitution.md`，梳理 `amr.rst` 的 substitution 公式、`UpdateAuxilaryData*()` 的 `aux = fp + I(parent_aux-cp)` 主链、`E/Bfield_cax` 的 coarse-aux 副本角色、`gather/current buffer masks` 的 transition-zone 语义，以及 `PartitionParticlesInBuffers()` 如何把粒子按 fine / lower-level gather-deposit 路径稳定分区，并回填第 7 章的 refinement-interface 主问题。
- 2026-05-16：继续推进 `Parallelization`，完成 `notes/code-reading/parallelization/04-warpxcomm-kernel-execution-model.md`，梳理 `WarpXComm_K.H` 的 coarse-fine / centering kernel 分层、`MFIter + Array4 + ParallelFor` 的统一执行壳、`TilingIfNotGPU()` / `Gpu::notInLaunchRegion()` / OpenMP 条件并行的 CPU-GPU 双栈写法，以及 `FillBoundary` 的 `do_single_precision_comms` / `nodal_sync` / `m_safe_guard_cells` 分支，并回填第 7 章的执行模型层。
- 2026-05-16：继续推进粒子与 AMR 的交界层，完成 `notes/code-reading/particles/04-amr-gather-deposition-buffers.md`，梳理 `PartitionParticlesInBuffers()` 如何把粒子数组稳定切成 fine / lower-level 两段，`PushPX()` 如何分别走 `E/Bfield_aux` 与 `E/Bfield_cax`，以及 `DepositCurrent()` / `DepositCharge()` 如何通过 `depos_lev`、coarsened tilebox 和 `current_buf/rho_buf` 直接在 coarse buffer patch 上沉积，并回填第 5 章和第 7 章。
- 2026-05-16：继续推进粒子沉积算法与 AMR buffer 的交界层，完成 `notes/code-reading/particles/05-current-deposition-algorithms-near-amr-buffer.md`，梳理 Villasenor、Vay、Esirkepov implicit 与 Direct/implicit 在 coarse-fine buffer 下共享的 `depos_lev` / coarsened tilebox / `dinv` / `xyzmin` 几何外壳，以及它们在 `relative_time`、`x_n`、`u_n`、`u_{n+1/2}` 和 charge-conserving 轨迹恢复上的差异，并回填第 5 章。
- 2026-05-16：继续推进 `CurrentDeposition.H` kernel 本体，完成 `notes/code-reading/particles/06-charge-conserving-current-kernel-structures.md`，梳理 Esirkepov 的 old/new shape 对齐、`compute_shifted_shape_factor`、`dil/diu` 边界宽度控制和差分前缀累加，以及 Villasenor 的 cell-crossing 计数、segment loop、cell/node 混合权重与 tighter stencil，并回填第 5 章。
- 2026-05-16：继续推进 Vay deposition 主线，完成 `notes/code-reading/particles/07-vay-deposition-d-field-structure.md`，梳理 `doVayDepositionShapeN()` 先沉积 `D`、再通过 temporary arrays 和第二阶段 box kernel 重组 `Dx/Dy/Dz` 的两阶段结构，并明确其 explicit-only、Cartesian-only、非 shared-memory 的实现边界，随后回填第 5 章。
- 2026-05-16：回到 `Particles/` 顶层，完成 `notes/code-reading/particles/08-particle-class-and-attribute-map.md`，梳理 `MultiParticleContainer -> WarpXParticleContainer -> Physical/Photon/Laser/RigidInjected` 的容器层次、`PIdx` 编译期 SoA、`opticalDepthQSR/BW`、`prev_*`、`ionizationLevel`、用户 parser 属性的注册与默认初始化机制，以及 implicit solver 动态加入的 `x_n/ux_n/nsuborbits` 和 back-transformed particles 的 `*_btd` 临时属性，并回填第 4 章。
- 2026-05-16：继续推进 `Particles/Pusher`，完成 `notes/code-reading/particles/09-radiation-reaction-implicit-photon-pushers.md`，梳理 `PushSelector.H` 中 classical radiation reaction 对 Boris/Vay/Higuera-Cary 的分派优先级、`UpdateMomentumBorisWithRadiationReaction()` 的 Boris 后修正结构、`ImplicitPushPX.cpp` 的 fixed-point / suborbit fallback / Villasenor-only 沉积约束，以及 `PhotonParticleContainer::PushPX()` 如何复用 species 外壳却改写为无 charge/current 沉积的无质量推进，并回填第 4 章。
- 2026-05-16：继续下钻 `ImplicitPushPX.cpp`，完成 `notes/code-reading/particles/10-implicit-suborbit-mass-matrices-jfnk.md`，梳理 `current_fp_non_suborbit`、`MassMatrices_X/Y/Z`、`MassMatrices_PC` 与 `J(E)=J_suborbit+J0+MM(E-E0)` 的分工、普通 mass-matrix deposition 为什么跳过 `nsuborbits>1` 粒子、suborbit 路径为何强制切到 Villasenor，以及 nonlinear stage 与 linear-stage-of-JFNK 在 suborbit 收敛和重沉积策略上的差异，并继续回填第 4 章。
- 2026-05-16：继续下钻 implicit field-solver 耦合层，完成 `notes/code-reading/fieldsolver/15-implicit-jacobian-preconditioner-coupling.md`，梳理 `ImplicitSolver::InitializeMassMatrices()` / `PreLinearSolve()` / `ComputeJfromMassMatrices()` 如何把 `J0 + MM(E-E0)` 组装成 linear-stage 电流，以及 `MassMatrices_PC` 如何经由 `MatrixPC`、`JacobiPC`、`CurlCurlMLMGPC` 进入不同 preconditioner，并回填第 6 章。
- 2026-05-16：继续下钻 nonlinear solver 消费链，完成 `notes/code-reading/nonlinear-solvers/03-residual-jacobian-operator-chain.md`，梳理 `ThetaImplicitEM` / `SemiImplicitEM::ComputeRHS()` 提供的物理右端、`NewtonSolver::EvalResidual()` 的 `F(U)=U-b-R(U)` 定义、`JacobianFunctionMF::apply()` 的 matrix-free 差商 Jacobian，以及 PETSc shell operator / native preconditioner 如何分别消费 `apply()` 与 `precond()`，并继续回填第 6 章。
- 2026-05-16：继续下钻 `pc_petsc` 支线，完成 `notes/code-reading/nonlinear-solvers/04-petsc-matrixpc-assembly-chain.md`，梳理 `StrangImplicitSpectralEM::ComputeRHS()` 的 `-dt*mu0*c^2*J/2` 右端特例、SNES Jacobian callback 如何触发 `assemblePCMatrix()`、`KSPSetOperators(A,P)` 的 shell-Jacobian / sparse-PC 双对象模式，以及 `MatrixPC::Assemble()` 如何把单位阵、curl-curl stencil 与 `MassMatrices_PC` 累加成 PETSc 稀疏矩阵，并继续回填第 6 章。
- 2026-05-16：继续下钻 `pc_petsc` 的显式矩阵装配底层，完成 `notes/code-reading/nonlinear-solvers/05-warpxsolverdof-and-matrixpc-geometry.md`，梳理 `WarpXSolverDOF` 的 `{local,global}` 编号契约、dot-mask 裁剪、`WarpXSolverVec` 与线性向量的映射关系，以及 `MatrixPC::Assemble()` 在 1D / XZ / RZ / 3D / RCYLINDER 下的 curl-curl、mixed-derivative、边界 mask 与 `MassMatrices_PC` 具体行写入模式，并继续回填第 6 章。
- 2026-05-16：继续下钻 `pc_petsc` 的来源链与提交链，完成 `notes/code-reading/nonlinear-solvers/06-bcmask-massmatrix-window-and-petsc-submit.md`，梳理 `ThetaImplicitEM::InitializeCurlCurlBCMasks()` 如何把 PEC/PMC/Silver-Mueller/PECInsulator/axis 条件预编码进 `curl2_BC_mask`，`ImplicitSolver::InitializeMassMatrices()` / `GetMassMatricesPCnComp()` 如何从 Jacobian mass matrices 裁出 `MassMatrices_PC` 的 diagonal-block 局域窗口，以及 `assemblePCMatrix()` 如何把 device 端行存条目拷回 host 并逐行提交给 PETSc `Mat P`，随后继续回填第 6 章。
- 2026-05-16：继续把 implicit solver 支线接回 regression 层，完成 `notes/code-reading/nonlinear-solvers/07-implicit-regression-assertion-map.md`，梳理 `analysis_petsc_matrix.py`、`analysis_planar_pinch.py`、`analysis_implicit.py` 等脚本分别在验证 `pc_petsc` 的 DOF 映射 / 矩阵装配 / 预条件器质量，还是在验证能量账本、Gauss 定律与 JFNK 主链，并继续回填第 6 章。
- 2026-05-16：继续推进 implicit regression 支线，完成 `notes/code-reading/nonlinear-solvers/08-vandb-jfnk-validation-map.md`，梳理 `analysis_vandb_jfnk_2d.py`、`analysis_vandb_jfnk_2d_cropping.py` 与 `inputs_test_2d_theta_implicit_jfnk_vandb*` 如何分别验证 `JFNK + Villasenor` 的 periodic 守恒、filtering 后守恒，以及 PEC / absorbing / cropping / suborbit 组合下的局部 charge conservation，并继续回填第 6 章。
- 2026-05-16：继续回到 `Particles/` 与 `Boundary/` 源码层，完成 `notes/code-reading/particles/11-particle-boundaries-buffer-sorting.md`，梳理 `boundary.particle_lo/hi`、`particles.crop_on_PEC_boundary`、absorbing/reflecting/thermal 边界、scraped particle buffer、invalid 删除、Redistribute/RedistributeLocal、以及 sorting 在 Villasenor / implicit suborbit 路径中的交界顺序，并回填第 7 章。
- 2026-05-16：正式启动 `Diagnostics/` 模块，完成 `notes/code-reading/diagnostics/00-boundary-scraping-diagnostics-python.md`，梳理 `BoundaryScrapingDiagnostics` 如何直接绑定 `ParticleBoundaryBuffer`、怎样按 boundary 写 openPMD 并在 flush 后清空 buffer，以及 Python `ParticleBoundaryBufferWrapper` / PICMI `ParticleBoundaryScrapingDiagnostic` 如何零拷贝消费同一份 scraped-particle 状态，并回填第 8 章。
- 2026-05-16：继续推进 `Diagnostics/` 主线，完成 `notes/code-reading/diagnostics/01-diagnostics-dispatch.md`，梳理 `MultiDiagnostics` 的类型分派、`Diagnostics` 的 init/compute/flush 模板骨架、`FullDiagnostics` 的 field-functor / time-averaged 主链，以及 `ParticleDiag` 在普通 full diagnostics 中作为 species 输出配置对象而不是独立粒子 buffer 的真实角色，并继续回填第 8 章。
- 2026-05-16：继续推进 `Diagnostics/` 主线，完成 `notes/code-reading/diagnostics/02-field-and-particle-functors.md`，梳理 `ComputeDiagFunctors` 的字段计算层、`ParticleReductionFunctor` 作为“从粒子生成 cell field”的特殊 field functor、以及 `WarpXOpenPMD` / `FlushFormatPlotfile` 在 writer 阶段如何通过 `tmp.copyParticles(...)` 真正应用 `ParticleDiag` 的过滤与变量选择，并明确 `phi` / `E/B` on particles 只允许 `diag_type=Full`，随后继续回填第 8 章。
- 2026-05-16：继续推进 `Diagnostics/` 剩余分支，完成 `notes/code-reading/diagnostics/03-reduced-diagnostics.md` 与 `04-io-formats-and-restart.md`，梳理 `MultiReducedDiags/ReducedDiags` 的工厂与表格输出协议、`FieldPoyntingFlux` 的 checkpoint 钩子、`FlushFormatCheckpoint` 的运行态序列化边界，以及 `BTDiagnostics` 的 slice/buffer 状态机和 flush 条件，并继续回填第 8 章。
- 2026-05-16：继续把 `Diagnostics/` 从框架层推进到案例层，完成 `notes/code-reading/diagnostics/05-reduced-diagnostic-case-studies.md`，梳理 `FieldProbe` 如何通过 probe-particle 容器对 `E/Bfield_aux` 做 point/line/plane gather、`ParticleHistogram` / `ParticleHistogram2D` 如何分别实现一维文本 histogram 与 openPMD 二维相空间网格，以及 `LoadBalanceCosts/LoadBalanceEfficiency` 如何把 box-level 负载分布、效率标量和对应 regressions 暴露出来，并继续回填第 8 章。
- 2026-05-16：继续把 `Diagnostics/` 的 writer 层补齐，完成 `notes/code-reading/diagnostics/06-writer-comparison-and-minimal-cases.md`，梳理 `format` 如何分派到 `FlushFormatPlotfile/OpenPMD/Checkpoint`、为什么 checkpoint 是受限的运行态序列化分支、plotfile 与 openPMD 在粒子过滤和附加粒子场上的真实差异，以及 `FieldProbe`、`ParticleHistogram2D`、`LoadBalanceCosts` 在本地 examples 中可直接复用的最小输入骨架，并继续回填第 8 章。
- 2026-05-16：继续把 `Diagnostics/` 的读者侧收尾，完成 `notes/code-reading/diagnostics/07-output-layouts-and-reading-tools.md`，整理 `plotfile/openPMD/checkpoint` 的典型目录树、`yt` / `openPMD-viewer` / `openPMD-api` / `read_raw_data.py` / `plot_distribution_mapping.py` 等读取入口，以及三类输出各自最合适的使用场景，并继续回填第 8 章。
- 2026-05-16：继续把 `Diagnostics/` 压成固定模板案例页，完成 `notes/code-reading/diagnostics/08-template-cases-and-boundaryscraping-example.md`，把 `plotfile/openPMD/checkpoint/BoundaryScraping` 四类输出统一整理成“最小输入片段 + 目录树 + 读取入口 + 适用场景”的固定模板，并用 `thomson_parabola_spectrometer`、`point_of_contact_eb`、`scraping` 等本地例子补上 `BoundaryScraping/openPMD` 的真实读法和 writer 边界，同时回填第 8 章。
- 2026-05-16：继续把 `BoundaryScraping` 的 Python 即时消费路径收尾，完成 `notes/code-reading/diagnostics/09-python-boundary-buffer-callback-case.md`，整理 `ParticleBoundaryBufferWrapper` 的两种最小模式：`particle_boundary_scrape` 的“运行结束后统一检查 buffer”和 `spacecraft_charging` 的“diagnostics 落盘与 Python 在线消费并存”，并把 `get_particle_scraped_this_step()`、tile 级数组归约和 `clear_buffer()` 的责任边界回填到第 8 章。
- 2026-05-16：切回 `Initialization/` 的前置启动层，完成 `notes/code-reading/initialization/08-initialization-bootstrap.md`，梳理 `main.cpp`、`WarpXAMReXInit.*`、`WarpXInit.*` 和 `WarpX::MakeWarpX()` 如何组织 MPI/AMReX/FFT/PETSc 生命周期、AMReX 默认 parser 覆盖、`geometry.is_periodic` 反推、几何表达式预解析、`geometry.dims` 契约、moving window 全局读参与 warning manager 初始化，并回填第 3A 章。
- 2026-05-16：继续细化 `Initialization` 启动层，完成 `notes/code-reading/initialization/09-preconstruct-parameter-locking.md`，梳理 `MakeWarpX()` 与构造期 `ReadParameters()` 的交界，解释 `ConvertLabParamsToBoost()` 如何先改写 geometry/tagging 参数、`CheckGriddingForRZSpectral()` 如何先改写 AMR 分块参数，以及 moving window / boosted frame / warning policy / 容器初始化分别在哪个时机锁定或落地，并继续回填第 3A 章。
- 2026-05-16：继续把启动层接回构造期主体，完成 `notes/code-reading/initialization/10-readparameters-runtime-landing.md`，梳理 `ReadParameters()` 如何把 solver/grid/boost/moving-window 等输入真正落到 `WarpX` 成员、对象存在性开关和默认派生状态上，并解释这些状态怎样立即决定 `MultiParticleContainer`、`ParticleBoundaryBuffer`、`MultiFluidContainer`、静电 solver 与 `HybridPICModel` 的创建条件，同时继续回填第 3A 章。
- 2026-05-16：继续细化 `ReadParameters()` 的组合约束，完成 `notes/code-reading/initialization/11-readparameters-combination-constraints.md`，梳理 `grid_type`、`field_gathering`、`do_current_centering`、`current_deposition`、PSATD/JRhom/Galilean/comoving、`particle_shape`、implicit evolve scheme 与 `use_filter` 之间的约束闭包，并把这些条件怎样继续决定 `current_fp_nodal`、`current_fp_vay`、`current_fp_non_suborbit`、`E_old`、nodal `aux` 场、filter/guard-cell 配额和 `ProjectionDivCleaner` 前置条件回填到第 3A 章。
- 2026-05-16：继续把 `ReadParameters()` 后的分配特例补齐，完成 `notes/code-reading/initialization/12-alloclevelmfs-specialized-branches.md`，梳理 `rho_fp/phi_fp/F_fp/G_fp` 的条件分配、external grid fields 与 external particle fields 的双合同、`aux` alias 何时被粒子专属外场打断，以及 `HybridPICModel`、fluids、macroscopic medium、EB/ECT 特例怎样继续扩展 field registry 并在 `InitData()` 后半段被立刻消费，同时继续回填第 3A 章。
- 2026-05-16：继续把 `Initialization` 后半主链闭合，完成 `notes/code-reading/initialization/13-initdata-postallocation-consumption.md`，梳理 `ComputeMaxStep/PML/NCI/buffer masks` 收尾、`m_electrostatic_solver->InitData()` 与 `ComputeSpaceChargeField()` 的职责区分、`ProjectionCleanDivB()` 与初始场求解的先后、`LoadExternalFields()` / `AddExternalFields()` 的装填-提交关系、fresh-run/restart 的 callback 分叉，以及第 0 步 full/reduced diagnostics 写出的真实状态边界，并继续回填第 3A 章。
- 2026-05-16：继续把 `Initialization` 从源码主链推进到 regression 证据层，完成 `notes/code-reading/initialization/14-initialization-validation-map.md`，整理 `langmuir`、`space_charge_initialization`、`dive_cleaning`、`gaussian_beam`、`effective_potential_electrostatic`、`electrostatic_sphere_eb` 与 `projection_div_cleaner` 这些 examples/regressions 如何分别覆盖 parser 初始化、`gaussian_beam`/`external_file` 注入、`initialize_self_fields`、electrostatic/EB Poisson 初始化和 `ProjectionCleanDivB()`，并明确哪些路径有显式物理断言、哪些目前主要依赖 checksum、哪些在当前本地 checkout 中还存在 analysis 脚本缺口，同时继续回填第 3A 章。
- 2026-05-16：继续把 `Initialization` 验证层补齐，完成 `notes/code-reading/initialization/15-initialization-validation-map-external-relativistic-openbc.md`，整理 `load_external_field` 如何分别验证 grid external field、particle external field、时间依赖 dependency parser 与 restart 保真，`relativistic_space_charge_initialization` 如何验证 `RelativisticExplicitES` 的初始 self-field，以及 `open_bc_poisson_solver` 如何验证 open boundary + FFT/sliced FFT 的 relativistic Poisson 初始化，并继续回填第 3A 章与索引文档。
- 2026-05-16：继续把 `Initialization` 验证层从“主要入口”补到“剩余粗分类条目”，完成 `notes/code-reading/initialization/16-initialization-validation-map-density-magnetostatic-nodal.md`，整理 `load_density` 如何验证 file-driven density profile 与 moving-window 连续注入、`magnetostatic_eb` 如何验证 `labframe-electromagnetostatic + EB + initialize_self_fields` 的解析/ checksum 双层证据，以及 `nodal_electrostatic` 如何通过 reduced diagnostics 对 collocated relativistic electrostatic 初始 self-field 做零触发验证，并同步清理第 3A 章和 `docs/example-regression-map.md` 里的对应粗分类。
- 2026-05-16：判断 `Initialization` 的这一轮验证层已经阶段性收口后，切到下一未完成主线 `Laser/`，完成 `notes/code-reading/laser/00-laser-profile-dispatch.md`，梳理 `ILaserProfile` 合同、`gaussian / parse_field_function / from_file` profile 字典、`LaserParticleContainer` 构造期的公共参数归一化、`e_max`/`a0` 二选一、boosted-frame 与 moving-window continuous-injection 约束，以及人工天线粒子和普通物理粒子容器的职责差异，并回填第 3A 章和 `docs/parameter-map.md`。
- 2026-05-16：继续推进 `Laser/`，完成 `notes/code-reading/laser/01-gaussian-from-file-and-runtime.md`，梳理 `GaussianLaserProfile` 的 diffraction / chirp / STC 复包络、`FromFileLaserProfile` 的 `lasy/binary` 独占后端与 `time_chunk_size` / `delay` 时间块读取、`LaserParticleContainer::InitData()` 的人工天线粒子布局、`Evolve()` 的 `profile -> plane coordinates -> amplitude -> update_laser_particle -> J/rho deposition` 主链，以及 `ContinuousInjection()` 的“天线进入域后单次建粒子”语义，并继续回填第 3A 章、`docs/parameter-map.md`、`docs/source-map.md` 与 laser regression 索引。
- 2026-05-16：继续推进 `Laser/`，完成 `notes/code-reading/laser/02-field-function-and-particle-update-kernel.md`，梳理 `FieldFunctionLaserProfile` 的 parser 合同、`field_function(X,Y,t)` 的完整场值语义、`calculate_laser_plane_coordinates()` 的平面回投、`ComputeWeightMobility()` 的速度上限与权重反推、`update_laser_particle()` 的显式/隐式一致更新逻辑，以及 `particle_absorbing_boundary` 对 `parse_field_function` 的当前间接验证边界，并继续回填第 3A 章、`docs/parameter-map.md`、`docs/source-map.md` 与 regression 索引。
- 2026-05-16：继续推进 `Laser/`，完成 `notes/code-reading/laser/03-laser-validation-map.md`，系统区分 `laser_injection`、implicit laser injection、`laser_injection_from_file`、`laser_acceleration`、RZ openPMD、BTD 与 Python callback 条目中哪些有真实 envelope/frequency 或理论/格式断言，哪些主要仍依赖 checksum，并同步清理 `docs/example-regression-map.md` 中相应条目的验证口径。
- 2026-05-16：继续推进 `Laser/` 与全局几何/外场交界，完成 `notes/code-reading/laser/04-moving-window-external-field-coupling.md`，梳理 `WarpX::MoveWindow()` 如何分别更新普通 species 的 `m_current_injection_position` 和 laser antenna 的 `m_updated_position`，为什么 species 与 laser 的 `ContinuousInjection()` 语义不同、AMR 下前者按 level-0 cell 而后者按 finest spacing 起作用，以及为什么 constant/parser 外场能随 moving window 重建而 `read_from_file` 外场被源码显式禁止，并同步细化相关 regression 条目的验证口径。
- 2026-05-16：继续推进 `Laser/` 应用层，完成 `notes/code-reading/laser/05-application-and-diagnostic-cases.md`，梳理 `laser_ion` 如何把 Gaussian laser 接到 full/time-averaged/reduced diagnostics 组合、`free_electron_laser` 如何通过 rigid bunch + external particle field + BTD 产生辐射并验证 gain length / wavelength，以及 `laser_on_fine` 为什么更像 AMR placement checksum test，并同步细化 `docs/example-regression-map.md` 中相关条目的断言口径。
- 2026-05-16：继续把 `Laser` 向束流/boosted-diagnostics 分叉推进，完成 `notes/code-reading/laser/06-rigid-injection-btd-and-undulator-coupling.md`，梳理 `free_electron_laser` 为什么本质上是在组合 `RigidInjectedParticleContainer`、external particle field undulator 和 `BackTransformed` diagnostics，而不是在测试 `lasers.names`；同时用 `rigid_injection` 和 `boosted_diags` 条目把 rigid propagation、BTD plotfile/openPMD 一致性、`random_fraction` 子采样与 FEL gain-length / wavelength 断言分层拆开，并同步清理 `docs/example-regression-map.md`。
- 2026-05-16：继续把 `Laser` 切向多物理 target 分叉，完成 `notes/code-reading/laser/07-laser-ion-multiphysics-switches.md`，梳理 `laser_ion` 当前真正激活的是 laser + 预电离 target + diagnostics，而 field ionization、collisions、QED 只是输入层可选开关；并把 `do_field_ionization -> InitIonizationModule -> doFieldIonization -> ionizationLevel`、`collision_names -> CollisionHandler -> split_momentum_push` 和 `QED runtime attributes -> InitQED` 这三条接入链，以及各自依赖的独立 regression 证据分开写清。
- 2026-05-16：继续把 `field_ionization` 从 `laser_ion` 输入级分叉下钻到 `Particles/` 主模块，完成 `notes/code-reading/particles/12-field-ionization-adk-pipeline.md`，梳理 `InitIonizationModule()` 如何按 `dt` 计算 ADK prefactors、`IonizationFilterFunc` 如何在粒子系场幅值上做概率抽样、`filterCopyTransformParticles` 如何把电离事件拆成源离子 `ionizationLevel += 1` 和 product electron 创建两部分，以及 `ionizationLevel` 如何继续进入 `rho/J` 沉积，并同步回填第 4 章与回归索引。
- 2026-05-16：继续沿 `Particles/` 多物理主线推进，完成 `notes/code-reading/particles/13-collision-handler-stepping-and-regression-map.md`，梳理 `CollisionHandler` 如何从 `collision_names` 和 `<collision>.type` 分派到 pairwise Coulomb、MCC、DSMC、pulsed decay、bremsstrahlung、linear Breit-Wheeler / Compton 等对象，`CollisionBase` 的 `ndt_supercycle / ndt_subcycle` 如何定义碰撞步频，以及 `collisions.split_momentum_push` 如何和 pusher / solver 选择强耦合，并同步回填第 4 章与回归索引。
- 2026-05-16：继续沿 `Particles/` 多物理主线推进，完成 `notes/code-reading/particles/14-qed-entrypoints.md`，梳理 species 构造期如何加入 `opticalDepthQSR/BW`、`mapSpeciesProduct()` / `CheckQEDProductSpecies()` / `InitQED()` 如何装配 product species 与 lookup tables，以及 Quantum Synchrotron、Breit-Wheeler、Schwinger 三条 QED 事件链与 `analysis_quantum_sync.py`、`analysis_breit_wheeler_core.py`、`analysis_schwinger.py` 三组 regression 证据之间的对应关系，并同步回填第 4 章与回归索引。
- 2026-05-16：继续沿 `Particles/` 多物理主线推进，完成 `notes/code-reading/particles/15-qed-kernels-and-wrapper-contracts.md`，梳理 `opticalDepthQSR/BW` 先在 `PhysicalParticleContainer::PushPX()` / `PhotonParticleContainer::PushPX()` 中演化、后在 `PhotonEmissionFilterFunc` / `PairGenerationFilterFunc` 里以 `< 0` 作为事件触发条件的两阶段合同，以及 `QuantumSyncEngineWrapper` / `BreitWheelerEngineWrapper` 如何只桥接 PICSAR-QED core 而不重写物理，并把这层语义回填到第 4 章和 QED regression 索引。
- 2026-05-16：继续沿 `Particles/` 多物理主线推进，完成 `notes/code-reading/particles/16-qed-table-generation-and-serialization.md`，梳理 `qed_qs/qed_bw.lookup_table_mode = builtin/load/generate` 三条 table 生命周期、wrapper 内部两张子表与最小 `chi` 门槛的存储结构、raw binary 的 `size_first + table1 + table2` 序列化格式，以及 IOProcessor 生成后写文件再广播给各 rank 的统一合同，并把这层语义回填到第 4 章。
- 2026-05-16：继续沿 `Particles/` 多物理主线推进，完成 `notes/code-reading/particles/17-qed-chi-virtual-photons-and-linear-breit-wheeler.md`，梳理 `QedChiFunctions.H` 只服务强场 QED 的 `chi` 包装角色、virtual photons 如何在 `CollisionHandler` 前置生成并每步覆盖重建辅助 photon species，以及 `linear_breit_wheeler` / `linear_compton` 如何作为 `BinaryCollision` 分叉而不复用 `ElementaryProcess` 的 optical-depth / lookup-table 主链，并把这层分叉关系回填到第 4 章和回归索引。
- 2026-05-16：继续沿 `Particles/` 多物理主线推进，完成 `notes/code-reading/particles/18-binary-collision-product-creation-and-linear-compton.md`，梳理 `BinaryCollision` 如何先输出事件表、`ParticleCreationFunc` 如何统一把 product species 落到 tile 内，以及 `LinearBreitWheeler` 的四宏粒子实现与 `LinearCompton` 的单-product-per-species 特例，并把这层 product 创建语义回填到第 4 章与回归索引。
- 2026-05-16：继续沿 `Particles/` 多物理主线推进，完成 `notes/code-reading/particles/19-backgroundmcc-pulseddecay-and-dsmc-branches.md`，梳理 `BackgroundMCC` 的背景气体 null-collision 频率上界、impact-ionization 的 filter/copy/transform 分支、`PulsedDecay` 的 cell 内 fixed-weight 衰变，以及 DSMC `SplitAndScatterFunc` 与 `ParticleCreationFunc` 的后半段分叉，并把对应 regression 的 0D 衰变、全局模型、通量衰减、两体产物速度和 boundary-scraping 快电子证据同步回填到第 4 章与回归索引。
- 2026-05-16：继续沿 `Particles/` 多物理主线推进，完成 `notes/code-reading/particles/20-coulomb-bremsstrahlung-stopping-and-fusion.md`，梳理 `pairwisecoulomb` 的 Perez 弹性散射与 `CoulombLog/global Debye length` 分叉、`bremsstrahlung` 的 photon-energy 事件表与专门 `PhotonCreationFunc`、`background_stopping` 的电子/离子 slowing-down 解析公式，以及 `nuclearfusion` 的 `event_multiplier / probability_threshold / cross-section` 分叉，并把对应 regression 的 relaxation、守恒、`dE/dx`、解析 stopping 和 fusion-yield 对照回填到第 4 章与回归索引。
- 2026-05-16：继续沿 `Particles/` 多物理主线推进，完成 `notes/code-reading/particles/21-collision-kernel-details-and-product-initializers.md`，梳理 `ElasticCollisionPerez` 的 Debye/atomic 尺度闭合与加权 `n12`、Bremsstrahlung photon `ux/uy/uz` 在 `PhotonCreationFunc` 尾部的 lab-frame 写回，以及两产物 fusion 的 4 宏粒子复制和 p-B11 两步反应的 6 alpha 初始化语义，并把这些 kernel 细节回填到第 4 章。
- 2026-05-16：继续沿 `Particles/` 多物理主线推进，完成 `notes/code-reading/particles/22-perez-bremsstrahlungevent-and-fusion-probability-control.md`，梳理 `UpdateMomentumPerezElastic.H` 的 `s12` 四段式散射角抽样与 weighted rejection、`BremsstrahlungEvent(...)` 的 plasma-frequency cutoff 与表驱动 photon-energy 反演、`SingleNuclearFusionEvent.H` 的 `probability_threshold / probability_target_value` 有效倍率回退，并把 2D/3D/isotropization/RZ collision regressions 与 `background_mcc` 的 analysis-vs-checksum 边界回填到第 4 章和回归索引。
- 2026-05-16：继续沿 `Particles/` 多物理主线推进，完成 `notes/code-reading/particles/23-resampling-thermalizer-and-sorting.md`，梳理 `ResamplingTrigger` 的 intervals / global-average-ppc 触发合同、`LevelingThinning` 与 `VelocityCoincidenceThinning` 两条重采样算法、`ParticleThermalizer` 在 boundary 之后 collisions 之前的局部热化路径，以及 `sort_intervals` / deposition-specialized sort / generic bin sort 的全局性能排序语义，并把 `resampling` 与 `capacitive_discharge/background_mcc` 的回归索引回填到第 4 章和项目文档。
- 2026-05-16：继续沿 `Particles/` 的性能与验证层收口，完成 `notes/code-reading/particles/24-thermalizer-validation-and-checksum-boundaries.md`，修正 `ParticleThermalizer` 并非“无本地 example”，而是已被 `particle_absorbing_boundary` 以耦合式 regression 间接强验证；同时把 `particle_absorbing_boundary`、`resampling`、`capacitive_discharge` 中 `analysis_default_regression.py` 的 checksum-helper 边界压实到第 4 章和 regression 索引。
- 2026-05-16：继续回到 `Particles/Gather` 未完成分支，完成 `notes/code-reading/particles/25-gather-variants-and-external-particle-fields.md`，补齐 `FieldGather.H` 在 `1D_Z / RCYLINDER / RSPHERE / 3D` 下的剩余 gather 分支、energy-conserving 与 momentum-conserving 的真实代码差异、implicit gather 对沉积算法的 stencil 耦合，以及 external particle fields 在 parser / repeated-lens / read-from-file 三条路径下怎样进入 `PushPX()`，并同步把相关 regression 索引回填到第 4 章和项目文档。
- 2026-05-16：继续沿 `Particles/` 主线推进，完成 `notes/code-reading/particles/26-pusher-single-particle-and-photon-validation-map.md`，梳理 `particle_pusher`、`single_particle`、`larmor`、`photon_pusher` 四组 regression 分别在验证 Higuera-Cary force-free push、单粒子 current filter、diagnostics 半步速度同步、massless photon transport，还是仅作为应用级 checksum 基线，并同步清理相关 regression 索引。
- 2026-05-16：继续沿 `Particles/` 主线推进，完成 `notes/code-reading/particles/27-particle-fields-plasma-lens-and-mpi-validation-map.md`，梳理 `particle_fields_diags` 如何验证 `particle_fields_to_plot` 的 reduction/writer 合同，`plasma_lens` 如何验证 repeated-plasma-lens 与 hard-edged lattice lens 两条 external particle field 路径，以及 `pass_mpi_communicator` 为什么当前仍是未启用的 Python/MPI communicator handoff 脚手架，并同步清理相关 regression 索引。
- 2026-05-16：继续沿 `Particles/` 的 diagnostics/Python-interface validation 层推进，完成 `notes/code-reading/particles/28-particle-diagnostics-python-interface-validation-map.md`，梳理 `particle_boundary_scrape` 如何同时验证 EB scraping 删除与 `ParticleBoundaryBufferWrapper`，`particle_data_python` 如何验证 runtime attributes / 手动沉积 / 旧位置缓存的 Python 接口链，以及 `particle_fields_diags` single-precision 为什么当前仍停在“脚本已存在但 CMake 未启用”的保留状态，并同步清理相关 regression 索引。
- 2026-05-16：继续沿 `Particles/` 的边界与 Python front-end validation 层推进，完成 `notes/code-reading/particles/29-boundary-and-python-front-end-validation-map.md`，梳理 `particle_boundary_interaction` 如何通过 scraped-buffer 几何与 Python callback 实现自定义镜面反射，`particle_boundary_process` 如何分别验证 absorbing boundary 的随机反射模型与粒子吸收删除，`particle_thermal_boundary` 如何用 reduced-diag 能量账本约束 thermal particle boundary，以及 `plasma_lens_python` 如何把 repeated-plasma-lens 的同一 physics analysis 扩展到纯 Python 参数前端，并同步清理相关 regression 索引。
- 2026-05-16：继续沿 `Particles/` 的 EB/PML/MR validation 层推进，完成 `notes/code-reading/particles/30-pml-eb-contact-and-mr-validation-map.md`，梳理 `point_of_contact_eb` 如何验证 `BoundaryScraping/openPMD` 里的接触点、时间戳和法向，`particles_in_pml` 如何验证带粒子 PML 后残余场是否足够小，`subcycling_mr` 为什么当前仍只是 AMR+subcycling 组合稳定性的 checksum 基线，以及 `Langmuir multi_mr` 如何继续用解析场解与 charge conservation 做 MR 强基准，并同步清理相关 regression 索引。
- 2026-05-16：继续沿 `Particles/` validation 收尾，完成 `notes/code-reading/particles/31-embedded-boundary-and-helper-validation-cleanup.md`，梳理 `particle_absorbing_boundary/plot_*.py` 为什么只是可视化 helper，`embedded_boundary_cube/rotated_cube/diffraction/em_particle_absorption` 各自在验证哪类解析场解或吸收后 `divE` 合同，`embedded_boundary_python_api` 怎样在 PICMI 输入脚本内部断言 `edge_lengths/face_areas` wrapper，以及 `electrostatic_sphere_eb`、`scraping` 这两组 EB 条目如何从粗分类改写成具体验证语义，并同步清理相关 regression 索引。
- 2026-05-16：从 `Particles` validation 阶段性切回 `Initialization`，完成 `notes/code-reading/initialization/17-initialization-validation-map-electrostatic-sphere.md`，梳理 `electrostatic_sphere` 如何通过解析电场膨胀解与 lab-frame 势能账本验证初始自场建立，并区分 relativistic / lab-frame / MR / collocated / adaptive-dt / RZ uniform-weighting 这些变体各自覆盖的初始化合同，同时把这组条目从过粗的 `electrostatic / Poisson` 分类中拆出来。
- 2026-05-16：继续沿 `Initialization` validation 推进，完成 `notes/code-reading/initialization/18-initialization-validation-map-dirichlet-and-effective-potential.md`，梳理 `electrostatic_dirichlet_bc` 如何验证 time-dependent Dirichlet 边界势 parser 和 `phi` 边界保真，以及 `effective_potential_electrostatic` 如何以 PICMI-only 导体球绝热膨胀 benchmark 验证 effective-potential solver，并继续清理静电初始化回归索引。
- 2026-05-16：继续沿 `Initialization` validation 推进，完成 `notes/code-reading/initialization/19-initialization-validation-map-openbc-relativistic-nodal.md`，把 `nodal_electrostatic`、`open_bc_poisson_solver` 和 `relativistic_space_charge_initialization` 从过粗的 `electrostatic / Poisson` 索引桶里拆开，分别压实为 collocated relativistic electrostatic 零触发基准、open-boundary FFT/sliced-FFT relativistic Poisson 初始化，以及 relativistic Gaussian beam 初始 self-field，并继续回填第 3A 章与回归索引。
- 2026-05-16：从 `Initialization` validation 切到参数系统主线，完成 `notes/code-reading/utils/01-parser-system.md` 和 `docs/parameter-chapter-index.md`，梳理 `ParmParse` 前缀命名空间、`queryWithParser` / `Store_parserString` / `IntervalsParser` / `query_enum_sloppy` 四类读取壳，并把 `max_step`、`stop_time`、`authors`、AMReX startup 参数等高频 `待定` 条目开始人工回填到真实书稿章节。
- 2026-05-16：继续推进参数系统主线，完成 `parameter-map.md` 的第二批人工校正，把无前缀运行控制、`algo.*`、`diagnostics.*`、`warpx.reduced_diags_names` / `reduced_diags.*`、以及 `safe_guard_cells` / `fillboundary_always_sync` 这组并行通信调试参数从旧计划编号和 `待定` 状态回填到第 3/3A/7/8 章。
- 2026-05-16：继续推进参数系统主线，完成 `parameter-map.md` 的第三批人工校正，把 `warpx.self_fields_*` / `magnetostatic_solver_*`、boundary/PML/EB、mirrors、accelerator lattice、collision/QED、PSATD、sorting/shared-memory deposition、以及 full diagnostics / BTD / particle-output / restart 这几组高频参数从旧计划编号和 `待定` 状态回填到第 3/3A/4/5/6/7/8 章，并同步扩展 `docs/parameter-chapter-index.md`。
- 2026-05-16：继续推进参数系统主线，把 `docs/parameter-chapter-index.md` 从“第几批修了什么”的补丁式清单升级为对象前缀导航，新增 `species / laser / diagnostics / collision` 四组子族入口，便于后续把参数索引稳定接回第 3A/4/8/6 章与对应源码笔记。
- 2026-05-16：继续推进参数系统主线，新增 `notes/code-reading/utils/02-parameter-family-entrypoints.md`，把 `species / laser / diagnostics / reduced diagnostics / collision` 五组高频参数族的真实读取入口压成源码图，明确区分 `WarpX.cpp` 的全局门闩、`Multi*` 工厂分派层和实例内部配置层，并把 `parameter-chapter-index.md` 接到这篇入口笔记。
- 2026-05-16：继续推进参数系统主线，扩写 `notes/code-reading/utils/02-parameter-family-entrypoints.md`，把 `boundary / PML / periodicity`、`solver / electrostatic / timestep / filter`、`psatd.*`、`implicit_evolve.*` 四组参数入口也接回 `WarpX.cpp`、`WarpXAMReXInit.cpp`、`FieldBoundaries.cpp` 和 `ThetaImplicitEM.cpp`，让参数索引不再只覆盖容器型工厂分派。
- 2026-05-16：继续推进参数系统主线，新增 `notes/code-reading/utils/05-deep-solver-object-parameter-families.md`，把 `fluids`、`hybrid_pic_model`、`macroscopic` 和 `effective potential` 四组更深的 solver-object 参数继续拆成 `existence gate / object creation / instance-local parse / runtime materialization`，让参数入口图开始覆盖 root gate 之后的对象生命周期层。
- 2026-05-16：继续推进参数系统主线，新增 `notes/code-reading/utils/06-external-vector-potential-and-poisson-boundary-parameters.md`，把 `external_vector_potential.*`、`boundary.potential_*` 与 `warpx.eb_potential(x,y,z,t)` 三组更细的边界/子对象参数继续拆到 `parent gate / subobject parse / parser build / runtime apply`，并把 Python 覆盖 `PoissonBoundaryHandler` 字符串这一条旁路入口也记录进索引。
- 2026-05-16：继续把参数系统主线闭合到 validation 层，新增 `notes/code-reading/utils/07-parameter-validation-links-for-boundary-and-external-fields.md`，把 `boundary.potential_*`、open-boundary Poisson、effective-potential 和 `warpx.eb_potential(x,y,z,t)` 接回 `electrostatic_dirichlet_bc`、`open_bc_poisson_solver`、`effective_potential_electrostatic`、`ion_beam_extraction` 四条最稳定 examples/analysis 链，并同步压实 `docs/example-regression-map.md` 中 `ion_beam_extraction` 的粗分类条目。
- 2026-05-16：继续推进参数系统主线，把 `docs/parameter-chapter-index.md` 从第一层对象前缀导航继续细化成可直接跳转的子族导航：在 `species / laser / diagnostics / collision` 之外，补出 `Collision/QED` 内部分叉、`boundary / solver / psatd / implicit`、`fluids / hybrid / macroscopic / effective potential` 三组更窄入口，减少后续查参数时在大表里来回搜索。
- 2026-05-16：继续回到 `docs/parameter-map.md` 做尾项清理，先补了一批高置信空源码命中：覆盖 species 初始化、space-charge 初始化、field ionization、resampling、virtual photons、collision product species、`external_vector_potential.<field_name>.*`、以及 diagnostics / BTD 主参数；同时保留剩余 `collision` 细项、writer backend 与部分 species runtime 标志作为后续 batch。
- 2026-05-16：继续回到 `docs/parameter-map.md` 做尾项清理，把上一轮遗留的 `collision` 细项、diagnostics writer/backend / particle-output 细项，以及一批 `species` runtime 标志也补到真实源码入口；本轮结束后，针对 `species / laser / diagnostics / reduced_diags / collision / external_vector_potential / load_balance` 这组高频参数族，表内已不再残留空的“初步源码命中”列。
- 2026-05-16：继续推进参数系统主线，新增 `notes/code-reading/utils/08-low-frequency-parameter-families-and-pass-throughs.md`，把 `parameter-map` 最后一批低频空项压成正式源码结论：区分文档 grouped alias、AMReX-owned pass-through 输入、外场聚合开关、PSATD/centering grouped key、macroscopic/hybrid parser 参数和 Schwinger 区域边界框；并同步把整张 `docs/parameter-map.md` 的空“初步源码命中”列清到 0，同时在第 3A 章补上 alias/pass-through 参数的正文解释。
- 2026-05-16：继续沿参数系统主线收尾，更新 `notes/code-reading/utils/07-parameter-validation-links-for-boundary-and-external-fields.md` 与 `docs/example-regression-map.md`，把 `external_vector_potential.*` 的当前验证边界压实为“已有 `ohm_solver_cylinder_compression` 这组 hybrid PIC PICMI tests 提供最近的 runtime coverage，但 `analysis=OFF`、仍只有 checksum baseline，因此还不能当作独立强 regression”。
- 2026-05-16：继续清理 `hybrid PIC / Ohm solver` 这组回归索引，更新 `notes/code-reading/fieldsolver/13-fieldsolver-verification-map.md` 与 `docs/example-regression-map.md`，把 `ohm_solver_em_modes`、`ion_beam_instability`、`ion_Landau_damping`、`magnetic_reconnection` 从同一个粗桶里拆开，明确区分脚本级强断言（RZ EM modes、ion beam instability）与主要依赖可视化加 checksum 的条目（Cartesian EM modes、Landau damping、reconnection）。
- 2026-05-16：继续清理 `general / to classify`，更新 `docs/example-regression-map.md`，把 `pierce_diode`、`spacecraft_charging`、`particle_boundaries`、`field_probe` 四组条目从粗分类中拆开；并把对应结论回填到 `notes/code-reading/diagnostics/05-reduced-diagnostic-case-studies.md`、`notes/code-reading/diagnostics/09-python-boundary-buffer-callback-case.md`、`manuscript/chapters/04-particle-pushers.md`、`manuscript/chapters/06-field-solvers.md`、`manuscript/chapters/08-diagnostics-cases.md`。
- 2026-05-16：继续清理 initialization 相关的粗分类回归条目，更新 `docs/example-regression-map.md`，把 `flux_injection`、`gaussian_beam`、`projection_div_cleaner`、`space_charge_initialization` 四组拆成更准确的注入/初始化合同；并把 `NFluxPerCell`、Gaussian-beam photon/PICMI 变体、projection-cleaner 的 script-local assert 与 3D/2D self-field 断言边界回填到 `notes/code-reading/initialization/14-initialization-validation-map.md` 和 `manuscript/chapters/03a-warpx-initialization.md`。
- 2026-05-16：继续清理 diagnostics/beam 相关的粗分类回归条目，更新 `docs/example-regression-map.md`，把 `btd_rz`、`collider_relevant_diags`、`diff_lumi_diag` 三组分别压实为 `RZ BackTransformed diagnostics`、`ColliderRelevant` 和 `DifferentialLuminosity(2D)`；并把这些 reduced/BTD 强 analysis 的合同回填到 `notes/code-reading/diagnostics/05-reduced-diagnostic-case-studies.md` 和 `manuscript/chapters/08-diagnostics-cases.md`。
- 2026-05-16：继续清理 `general / to classify`，更新 `docs/example-regression-map.md`，把 `divb_cleaning`、`dive_cleaning`、`initial_distribution`、`initial_plasma_profile` 四组分别压实为 `div(B) cleaning`、`div(E) cleaning / PML`、`particle distributions` 和 `parabolic-channel plasma profile`；并把其中与初始化有关的分布/API 合同回填到 `notes/code-reading/initialization/14-initialization-validation-map.md` 和 `manuscript/chapters/03a-warpx-initialization.md`。
- 2026-05-16：继续清理 `general / to classify`，更新 `docs/example-regression-map.md`，把 `radiation_reaction`、`repelling_particles`、`secondary_ion_emission` 三组分别压实为 `classical radiation reaction`、`two-particle electrostatic self-field repulsion` 和 `embedded-boundary secondary ion emission / Python callback`；并把对应结论回填到 `notes/code-reading/particles/09-radiation-reaction-implicit-photon-pushers.md`、`notes/code-reading/diagnostics/09-python-boundary-buffer-callback-case.md`、`notes/code-reading/initialization/14-initialization-validation-map.md` 与第 3A / 第 4 章。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 的剩余粗分类，压实 `test_1d_fel`、`test_2d_bilinear_filter`、`test_2d/rcylinder/rz_curl_curl_petsc_pc` 的真实验证合同；并把 FEL gain-length / wavelength 断言与 PETSc `1 Newton + 1 GMRES` 结构判据回填到 `notes/code-reading/laser/06-rigid-injection-btd-and-undulator-coupling.md` 与 `notes/code-reading/fieldsolver/13-fieldsolver-verification-map.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 的剩余粗分类，压实 `test_2d_energy_conserving_thermal_plasma`、`test_2d_id_cpu_read_picmi`、`test_2d_runtime_components_picmi` 的真实验证合同；并把 `idcpu` / runtime-components / checkpoint scaffold 的 PICMI 边界回填到 `notes/code-reading/particles/28-particle-diagnostics-python-interface-validation-map.md` 与 `notes/code-reading/diagnostics/04-io-formats-and-restart.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 的剩余粗分类，压实 `test_3d_acceleration`、`test_3d_beamsize_effect`、`test_3d_eb_picmi` 的真实验证合同；并把 acceleration restart 可重复性、EB PICMI checkpoint scaffold 与 virtual-photon beam-size effect 的边界回填到 `notes/code-reading/diagnostics/04-io-formats-and-restart.md`、`notes/code-reading/particles/17-qed-chi-virtual-photons-and-linear-breit-wheeler.md` 和第 8 章。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 的剩余粗分类，压实顶层 `Examples/analysis_default_regression.py`、`test_3d_hard_edged_quadrupoles*` 与 `test_3d_pmc_field` 的真实验证合同；新增 `notes/code-reading/accelerator-lattice/03-validation-map.md`，并把 hard-edged quadrupole 与 PMC standing-wave 的验证边界回填到第 4/7 章。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `langmuir` / `langmuir_fluids` 的 1D/2D/3D/RZ、MR、PSATD、JRhom、current-correction、Vay、PICMI 与 cold-fluid 变体统一接回 `analysis_1d/2d/3d/rz/r1d.py` 和 `analysis_utils.py` 的共享合同；并把这组 Langmuir 验证树的真实边界回填到 `notes/code-reading/initialization/14-initialization-validation-map.md`、`notes/code-reading/particles/30-pml-eb-contact-and-mr-validation-map.md` 与 `manuscript/chapters/08-diagnostics-cases.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `uniform_plasma` 目录本体和 `nci_psatd_stability` 里的 `uniform_plasma_psatd_JRhom_CC1` 明确拆开：前者当前只有 2D/3D checksum 与 3D restart 强对照，后者则是 `JRhom=CC1 + div cleaning` 的 PSATD/NCI 稳定性基准；并把这组边界回填到 `notes/code-reading/diagnostics/06-writer-comparison-and-minimal-cases.md`、`notes/code-reading/fieldsolver/13-fieldsolver-verification-map.md` 与 `manuscript/chapters/08-diagnostics-cases.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `implicit` family 从笼统的 “implicit solver” 桶拆成五条验证线：1D Picard 能量守恒、exactly energy-conserving symmetry、Strang implicit spectral EM、planar pinch，以及 JFNK + Villasenor / PEC cropping；并把这组边界回填到 `notes/code-reading/nonlinear-solvers/07-implicit-regression-assertion-map.md` 与 `manuscript/chapters/06-field-solvers.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `silver_mueller` family 压实为“残余反射幅值必须远小于入射脉冲峰值”的开放边界强回归，并区分 1D 轴向、2D `x` 向、2D `z` 向与 RZ `z` 向四条最小基准；同时把这一层回填到 `notes/code-reading/boundary/02-pec-insulator-silver-mueller.md` 与 `manuscript/chapters/07-boundaries-amr.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `pec` family 压实为 PEC standing-wave、PEC standing-wave / MR、PECInsulator implicit energy accounting、PECInsulator implicit restart continuation，以及粒子侧 PEC checksum baseline 五条边界；并把这组边界回填到 `notes/code-reading/boundary/02-pec-insulator-silver-mueller.md` 与 `manuscript/chapters/07-boundaries-amr.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `pml` family 压实为 Yee/CKC 理论反射率、PSATD/Galilean 低反射率、RZ residual-field decay，以及 Yee/PSATD restart reproducibility 四层合同；并把对应结论回填到 `notes/code-reading/boundary/01-pml-data-and-update.md` 与 `manuscript/chapters/07-boundaries-amr.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `reduced_diags` family 压实为 compact-observable cross-check 与 `LoadBalanceCosts` efficiency 两棵验证树，并把 `timers_psatd` / `single_precision` 的当前源码树边界回填到 `notes/code-reading/diagnostics/05-reduced-diagnostic-case-studies.md` 与 `manuscript/chapters/08-diagnostics-cases.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `vay_deposition` family 压实为 2D/3D `vay + psatd + collocated` 下的离散 Gauss-law 强断言，并把对应结论回填到 `notes/code-reading/particles/07-vay-deposition-d-field-structure.md` 与 `manuscript/chapters/05-deposition-shapes.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `nci_fdtd_stability` 压实为 2D drifting-plasma + `use_fdtd_nci_corr=1` 的 FDTD NCI 抑制基准，明确 non-MR 的 `1e24` 场能代理阈值、MR 变体的 full-domain fine-tag 目标，以及 `analysis_ncicorr.py` 当前 MR 路径区分仅在脚本层预留、未从活跃 `CMakeLists.txt` 参数层显式选通的源码树边界；并把对应结论回填到 `notes/code-reading/fieldsolver/13-fieldsolver-verification-map.md` 与 `manuscript/chapters/06-field-solvers.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `maxwell_hybrid_qed` 压实为 2D collocated + PSATD + `use_hybrid_QED=1` 的真空色散基准，明确 `analysis.py` 实际检查的是脉冲相速度与 hybrid-QED 理论值的 `1.25%` 误差，而不是粒子 QED 事件；并把对应结论回填到 `notes/code-reading/fieldsolver/13-fieldsolver-verification-map.md` 与 `manuscript/chapters/06-field-solvers.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `python_wrappers` 压实为 2D PICMI + PSATD + PML + div-cleaning 下的 Python field-wrapper 强接口自检，明确 `sim.fields.get(...)` 对 `E/B/F/G` 与 `pml_E/B/F/G` 的逐分量 benchmark 断言；并新增 `notes/code-reading/python/03-field-wrapper-validation-map.md`，把这条 validation 接回 `pywarpx/fields.py`、`MultiFabRegister.py`、`Source/Python/MultiFabRegister.cpp` 与第 8 章。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `plasma_acceleration` 压实为 PWFA application workflow matrix：明确这组 active tests 全部 `analysis=OFF`、目录内 helper 只提供 checksum、真正覆盖的是 moving window、boosted frame、rigid bunch、NCI correction、mesh refinement、hybrid grid 与 PICMI front-end；并把“3D PICMI 版当前仍不是 boosted-frame 等价前端”的 README 边界回填到 `notes/code-reading/laser/05-application-and-diagnostic-cases.md` 与 `manuscript/chapters/08-diagnostics-cases.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `beam_beam_collision` 压实为 collider-QED application baseline：明确 active regression 只有 checksum helper、输入真正覆盖的是 relativistic electrostatic self-field、Quantum Synchrotron、Breit-Wheeler 与 `ColliderRelevant/ParticleNumber` reduced diagnostics 的联合路径，而 `plot_fields.py` / `plot_reduced.py` 只是可视化 helper；并把这组边界回填到 `notes/code-reading/diagnostics/05-reduced-diagnostic-case-studies.md` 与 `manuscript/chapters/08-diagnostics-cases.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `plasma_mirror` 压实为 2D laser-solid surface-plasma checksum baseline：明确 active regression 只有 checksum helper、输入真正覆盖的是 Gaussian laser、前后指数梯度 + 过密 plateau 固体靶、PML、field filter 与 full diagnostics 的联合骨架，当前没有独立 analysis 也没有 PICMI；并把这组边界回填到 `notes/code-reading/laser/05-application-and-diagnostic-cases.md` 与 `manuscript/chapters/03a-warpx-initialization.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `thomson_parabola_spectrometer` 压实为 `BoundaryScraping/openPMD + prescribed-field test-particle optics` 强基准：明确 active `analysis.py` 会用 `particles_at_zhi` 与 `diag0` 的 `id` 回连重建 detector screen 上的离子分离图，而不是普通 `PEC / conducting boundary` test；并把这组边界回填到 `notes/code-reading/diagnostics/08-template-cases-and-boundaryscraping-example.md` 与 `manuscript/chapters/08-diagnostics-cases.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `capacitive_discharge` 压实为两层结构：1D PICMI `background_mcc` / `dsmc` 使用 `analysis_1d.py` 与 `analysis_dsmc.py` 对 Turner case-1 离子密度 profile 做强对照，2D native / PICMI 变体则仍是 workflow checksum baseline，同时把 `test_2d_background_mcc_dp_psp` 明确降格为当前 `CMakeLists.txt` 中被注释掉的遗留 benchmark 名；并把对应边界回填到 `notes/code-reading/particles/22-perez-bremsstrahlungevent-and-fusion-probability-control.md` 与 `manuscript/chapters/04-particle-pushers.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `laser_acceleration` 压实为 LWFA runtime matrix：明确 `README.rst` 的 `Analyze` 仍是 `TODO`、目录里大多数 active tests 都是 `analysis=OFF`，而现有强断言只局限于 1D boosted fluid ODE 对照、`refine_plasma=1` 连续注入一致性，以及 RZ openPMD diagnostics 合同；并把 `inputs_base_1d/2d/3d/rz` 的运行骨架语义回填到 `notes/code-reading/laser/05-application-and-diagnostic-cases.md` 与 `manuscript/chapters/03a-warpx-initialization.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 里仍写成“checksum 基线；需反查对应 inputs 和分析脚本”的家族条目，把 `embedded_circle` 压实为 2D circular embedded-boundary electrostatic + PIC-MCC + BoundaryScraping workflow baseline：明确它通过 `eb_implicit_function`、`eb_potential`、双物种 `background_mcc`、`save_particles_at_eb`、`BoundaryScraping` 和 timer-based load balance 把多物理工作流接在一起，但当前 `analysis=OFF`、只有 checksum helper；并把对应边界回填到 `notes/code-reading/embedded-boundary/02-particle-scraping-and-deposition-near-eb.md` 与 `manuscript/chapters/07-boundaries-amr.md`。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 的 `load_external_field` restart 尾项，把 `analysis_default_restart.py` 与 3 条 restart 输入压实为 external-field state reproducibility：明确它们通过 `amr.restart=...` 从中间 checkpoint 恢复，并用逐字段对照验证 `read_from_file` / dependency parser 构造出的 grid external field 与 particle external field 状态在 restart 后不漂移；并把对应边界回填到 `notes/code-reading/initialization/15-initialization-validation-map-external-relativistic-openbc.md`、`notes/code-reading/diagnostics/04-io-formats-and-restart.md` 与第 3A 章。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 的 `restart` 尾项，把 `analysis_default_restart.py`、`inputs_base_3d` 与 `test_3d_acceleration_psatd*` 这一串压实为 field-level restart reproducibility 与 PSATD/time-averaged acceleration restart reproducibility：明确它们不是新的色散 physics benchmark，而是在 3D boosted acceleration workflow 上验证 `psatd`、Galilean 和 `psatd.do_time_averaging=1` 这些 solver path 的 checkpoint/restart 不漂移；并把对应边界回填到 `notes/code-reading/diagnostics/04-io-formats-and-restart.md` 与第 8 章。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 的 `restart_eb` 尾项，把目录内 `analysis_default_regression.py` / `analysis_default_restart.py` 与 `inputs_test_3d_eb_picmi.py` 一起压实为 EB + PICMI + checkpoint scaffold：明确 active test 仍只有 checksum baseline，而逐字段 restart 对照仍停留在 `CMakeLists.txt` 里被注释掉的 `test_3d_eb_picmi_restart`；并把这层“helper 已在、注册未开”的边界回填到 `notes/code-reading/diagnostics/04-io-formats-and-restart.md` 与第 8 章。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 的 `laser_injection` / `laser_injection_from_file` helper 尾项，把两个目录的 `analysis_default_regression.py` 压实为 checksum helper，并把 `laser_injection_from_file` 里 1D/2D/3D/RZ/binary 的 `*_prepare.py` 全部改写成外部 laser 文件生成阶段：明确它们在 `CMakeLists.txt` 中都是 `analysis=OFF`、`checksum=OFF` 的 dependency，真正构成的是 `prepare -> inject -> analysis` 三段回归链；并把这层边界回填到 `notes/code-reading/laser/03-laser-validation-map.md` 与第 3A 章。
- 2026-05-17：继续清理 `docs/example-regression-map.md` 的 helper 尾项，把 `Examples/Tests/subcycling/analysis_default_regression.py` 和 `Examples/Tests/pec/analysis_default_regression.py` 分别压实为 AMR-subcycling 组合稳定性基线 helper 与 PEC family checksum helper：明确它们都只是目录内 checksum 副本，不替代 `subcycling_mr` 的真实组合边界，也不替代 `analysis_pec.py` / `analysis_pec_mr.py` / `analysis_pec_insulator_implicit.py` 的强断言；并把对应边界回填到 `particles/30` 与 `boundary/02`。
- 2026-05-17：切回 `docs/parameter-map.md` 做第二轮人工复核，先修正 `particle_thermalizer.species` 与 `particle_thermalizer.theta` 的误命中源码入口：两者真实都由 `Source/Particles/ParticleThermalizer/ParticleThermalizer.cpp` 解析，而不是 diagnostics/flush 路径；同时把条目说明改写成 `queryarr("species")` 的 species 过滤语义，以及 `theta -> sqrt(theta)` 的热速度重采样合同。
- 2026-05-17：继续推进 `parameter-map` 第二轮人工复核，又修正了两条“命中到了消费点而不是主解析入口”的运行控制参数：`warpx.numprocs` 的真正读取链在 `WarpX.cpp -> WarpXAMReXInit.cpp -> WarpXInitData.cpp`，`warpx.dt_update_interval` 的真正读取链在 `WarpX.cpp -> WarpXComputeDt.cpp -> WarpXEvolve.cpp`；相应移除了把 `FullDiagnostics.cpp` 当作主解析入口的误导写法。
- 2026-05-17：继续推进 `parameter-map` 第二轮人工复核，又收了一批运行控制与 diagnostics 易误命中条目：把 `max_step` / `stop_time` 改回 `WarpX::ReadParameters()` 这条无前缀解析入口，把 `geometry.dims` 压实为 `WarpXInit.cpp` 中的编译维度一致性检查，并把 `reduced_diags.intervals` 收束到 `ReducedDiags.cpp`，明确 `FullDiagnostics` / `BoundaryScrapingDiagnostics` 里的 `intervals` 属于各自诊断体系的独立参数，不应混写成 reduced-diags 主入口。
- 2026-05-17：继续推进 `parameter-map` 第二轮人工复核，又修正了 `authors`、`amr.max_level`、`amr.restart`、`warpx.verbose` 四条：把 `authors` 改回 `WarpX::ReadParameters()` 到 `WarpXOpenPMDPlot::setAuthor` 的真实透传链，把 `amr.restart` 改回 `WarpX.cpp` 主解析 + `WarpXIO.cpp` / `ReducedDiags.cpp` 消费链，把 `warpx.verbose` 改回 `WarpX.cpp -> main.cpp / WarpXEvolve.cpp`，并把 `amr.max_level` 明确降格为 `AMReX/AmrCore-owned input`，只记录 WarpX 本地消费者而不再伪造一个并不存在的统一 WarpX 解析入口。
- 2026-05-17：继续推进 `parameter-map` 第二轮人工复核，又修正了 `warpx.used_inputs_file`、`warpx.boost_direction`、`warpx.do_electrostatic`、`amr.n_cell` 四条：把 used-inputs 文件名压实为 `WriteUsedInputsFile()` 中的 `queryAdd + write_used_inputs_file` 链，把 boosted-frame 方向压实为 `ReadBoostedFrameParameters()` 的真正读取入口和当前“只能沿 z boost”的源码边界，把 `do_electrostatic` 改回 `WarpX.cpp` 的枚举解析与求解器互斥逻辑，并把 `amr.n_cell` 明确记成 `AMReX/AmrCore-owned input` 加 WarpX 启动期预解析路径，而不是伪装成普通 WarpX 自有参数。
- 2026-05-17：继续推进 `parameter-map` 第二轮人工复核，又修正了 `warpx.gamma_boost`、`warpx.n_rz_azimuthal_modes`、`geometry.prob_lo/hi`、`warpx.poisson_solver` 四条：把 `gamma_boost` 收束到 `ReadBoostedFrameParameters()` 这条 boosted-frame 主入口，把 RZ 方位模数改回 `WarpX.cpp` 的真实解析位置，把 `prob_lo/hi` 写实为 `WarpXAMReXInit.cpp` 启动期预解析 + `WarpXUtil.cpp` boost 坐标改写的两段链路，并把 `poisson_solver` 改回 `WarpX.cpp` 主解析加 `PoissonBoundaryHandler.cpp` 分派消费的真实边界。
- 2026-05-17：继续推进 `parameter-map` 第二轮人工复核，又修正了 `warpx.zmax_plasma_to_compute_max_step`、`warpx.compute_max_step_from_btd`、`warpx.do_moving_window` / `moving_window_*`、`warpx.fine_tag_lo/hi`：把 boosted wakefield 的自动 `max_step` 计算链压实到 `WarpX.cpp + WarpXInitData.cpp`，把 BTD 驱动的 `max_step/stop_time` 自动补足压回 `BTDiagnostics.cpp`，把 moving-window 的总入口收束到 `WarpXInit.cpp` 的 `read_moving_window_parameters()`，并把静态 refined patch bbox 入口与 boosted-frame 坐标改写链明确拆开。
- 2026-05-17：继续推进 `parameter-map` 第二轮人工复核，又修正了 `boundary.field_lo/hi`、`boundary.particle_lo/hi`、`warpx.ref_patch_function(x,y,z)`：把 field boundary 压实为 `parse_field_boundaries()` 入口加 `geometry.is_periodic` 反推和 PML/solver 兼容性消费链，把 particle boundary 压实为“未显式给出时继承 periodic、显式给出时做一致性检查”的真实入口，并把 refined patch parser 入口和 `fine_tag_lo/hi` 的覆盖关系写回参数表。
- 2026-05-17：对 `Laser/` 未完成项做了当前工作树审计：确认 sibling `Source/Laser/` 只包含 `LaserProfiles.H` 与三类 `LaserProfilesImpl/*`，而现有 `notes/code-reading/laser/00-07` 已连同 `Particles/LaserParticleContainer.*`、`Utils/WarpXMovingWindow.cpp`、`Initialization/ExternalField.*` 的 laser 侧耦合一起覆盖，因此把 `TODO.md` 里过期的 `Laser/` 与 `ExternalField.* / WarpXMovingWindow.cpp` 未完成项收口，并补齐 `docs/source-map.md` 中缺失的 laser 笔记映射。
- 2026-05-17：对 `TODO.md` 里过宽的“验证：laser injection、load density、initial distribution、moving-window / boosted examples”做了当前工作树审计并拆分状态：`laser injection`、`load density`、`moving-window / boosted examples` 已有足够的源码笔记、validation map 与第 3A 章证据支撑而正式收口；`initial distribution` 仍保留未完成，因为当前本地 checkout 的 `test_3d_focusing_gaussian_beam_from_openpmd` 还缺名义上的 `analysis.py`，不应过度宣称整条初始化分布验证已闭环。
- 2026-05-17：继续把 `initial distribution` 从单一宽泛未完成项拆成当前工作树可证明的三层边界：`gaussian_beam` 已由 focusing/rotation/PICMI/photon 变体闭环，`external_file` 的 PICMI openPMD variant 已由 `analysis_focusing_beam.py` 闭环，而 native openPMD variant 仍保持未完成，因为本地 `gaussian_beam/CMakeLists.txt` 指向的名义 `analysis.py` 仍缺失。
- 2026-05-17：继续细化 `initial distribution / external_file / native openPMD variant`：确认这条 native 路径并非“完全无覆盖”，而是已经具备 `prepare -> external_file inputs -> checksum` 的 workflow baseline；真正未完成的是名义 `analysis.py` 缺失导致的强 analysis 层，而不是输入生成或注入链本身。
- 2026-05-17：继续推进 `Particles/ElementaryProcess/Ionization.*`：在已有 ADK 主链笔记基础上，补齐了 `physical_element -> ion_map_ids/ion_energy_offsets/table_ionization_energies` 这条内建能表链、`ionization_initial_level` 在 `DefaultInitialization.H` 与 `AddParticles.cpp` 中的初始化传播、`ionizationLevel` 如何通过沉积层把有效电荷计成 `w q_e * level`，并明确记录当前工作树只有 `ADK + Zhang correction`、没有独立 `OTB` 分支。
- 2026-05-17：对 `QED internals、quantum synchrotron、Breit-Wheeler、Schwinger process` 这条未完成项做了当前工作树审计：确认 `particles/14-17` 已分别覆盖 `InitQED()` 与 product-species/runtime-attribute 入口、QED kernels 与 wrapper/table 生命周期、`QedChiFunctions` 与 strong-field QED 主链、virtual photons/`linear_breit_wheeler` 的 `BinaryCollision` 分叉，以及 Schwinger 的 field-to-pair 第三条创建路径，因此把这条过期未完成项正式收口。
- 2026-05-17：继续推进 `Fluids/` 主线：新增 `fluids/00-fluid-container-map.md`、`01-muscl-hancock-update.md`、`02-fluid-pic-coupling.md`，把 `fluids.species_names -> MultiFluidContainer -> WarpXFluidContainer` 这条对象图、nodal `N/NU` level data、shared injector 初始化、moving-window 连续再注入、MUSCL-Hancock + Rusanov + positivity limiter 更新，以及 fluid species 如何 gather `Efield_aux/Bfield_aux`、复用 Higuera-Cary source-step、把 `rho/J` 沉积回普通场寄存器并与 `HybridPICModel` 电子闭合区分开来，正式压成源码笔记。
- 2026-05-17：继续推进 `AcceleratorLattice/` 主线：新增 `accelerator-lattice/00-lattice-data-model.md`、`01-lattice-elements.md`、`02-element-finder-device-path.md`，把递归 `lattice.elements/line/reverse` 输入树、`z_location -> zs/ze` 几何账本、`drift/quad/plasmalens` 的 hard-edged 元件语义、`hard_edged_fraction()` residence correction、per-tile `LatticeElementFinder` lookup table，以及 boosted-frame 下先按 lab-frame 取场再反变换回粒子推进的运行链压成源码笔记，并同步回填 `source-map`、第 4 章与 `TODO` 状态。
- 2026-05-17：对 `TODO.md` 里过期的验证汇总项做了当前 worktree 审计并正式收口：`particles/12-19,21-23` 已把 `collision / ionization_dsmc / field_ionization / qed / linear_breit_wheeler / linear_compton` 的 runtime 主链与 analysis-vs-checksum 边界压实，`accelerator-lattice/03-validation-map.md` 已把 hard-edged quadrupole 的 lab/boosted/moving-window 强 regression 压实，而 `fieldsolver/13-14` 已把 `ohm_solver_*` 明确分成有显式 assert 的 RZ EM modes / ion-beam-instability 与主要依赖谱图或 checksum 的其他变体，因此该 validation bundle 不应再保留为未完成。
- 2026-05-17：开始推进“应用案例综合章”，先收口 `Langmuir wave`：新增 `notes/code-reading/applications/README.md` 与 `00-langmuir-wave.md`，把 `Examples/Tests/langmuir/*` 和 `langmuir_fluids/*` 从冷等离子体解析振荡、输入骨架、初始化/粒子/场求解/诊断四条源码路径，到 `analysis_1d/2d/3d/rz/r1d.py`、`analysis_utils.py`、PICMI scaffold 与 cold-fluid 对照树的验证分层，整理成第一条正式应用主线，并同步扩展第 8 章和 `source-map`。
- 2026-05-17：继续推进“应用案例综合章”，收口 `Uniform plasma`：新增 `notes/code-reading/applications/01-uniform-plasma.md`，把 `Examples/Physics_applications/uniform_plasma/*` 从均匀热等离子体背景、粒子噪声/并行稳定性、full diagnostics 与 checkpoint/restart 最小工作流，一路整理到应用级主线；同时把“应用目录本体主要是 noise/performance/writer/checkpoint baseline，热等离子体总能量强断言来自相邻 `energy_conserving_thermal_plasma`，PSATD/JRhom 强稳定性断言来自 `nci_psatd_stability`”这层边界同步写回第 8 章与 `source-map`。
- 2026-05-17：继续推进“应用案例综合章”，收口 `LWFA/PWFA`：新增 `notes/code-reading/applications/02-lwfa-pwfa.md`，把 `laser_acceleration` 与 `plasma_acceleration` 从分散在 `laser/05`、第 8 章和 regression 索引里的材料，重新收束成共享 moving window、boosted frame、diagnostics、MR 和 PICMI/native 前端分裂的 wakefield acceleration 主线；同时明确 `laser_acceleration` 当前只是 `LWFA runtime matrix`、`plasma_acceleration` 当前只是 `PWFA workflow matrix`，不能被误写成统一 physics benchmark。
- 2026-05-17：继续推进“应用案例综合章”，收口 `Laser ion / plasma mirror / RPA/TNSA`：新增 `notes/code-reading/applications/03-laser-targets-rpa-tnsa.md`，把 `laser_ion` 的强场靶 + diagnostics 组合、`plasma_mirror` 的 laser-solid surface-plasma workflow baseline，以及 `RPA/TNSA` 在当前 worktree 中仅作为机制标签而非独立本地 application tree 的边界一次写清，并同步回填第 8 章与 `source-map`。
- 2026-05-17：继续推进“应用案例综合章”，收口 `Capacitive discharge`：新增 `notes/code-reading/applications/04-capacitive-discharge.md`，把 `Examples/Physics_applications/capacitive_discharge/*` 从 1D Turner benchmark、Python callback Poisson solver、可切换 DSMC 分支和 `background_mcc` 骨架，一路整理到应用级主线；同时把“1D PICMI 入口有 case-1 ion-density 强对照、2D native/PICMI 当前仍主要是 workflow checksum baseline、`dp_psp` 仍是被注释掉的遗留分支”这层边界同步写回第 8 章与 `source-map`。
- 2026-05-17：继续推进“应用案例综合章”，收口 `Magnetic reconnection`：新增 `notes/code-reading/applications/05-magnetic-reconnection.md`，把 `Examples/Tests/ohm_solver_magnetic_reconnection/*` 从 `HybridPICModel`、force-free-sheet 初场、reduced `FieldProbe`、重联率提取、动画输出和 checksum 分层一路整理到应用级主线；同时明确保留当前 worktree 的真实边界：这条应用线依赖的是 field-solver 内部的 electron-fluid Ohm closure，不是 `Fluids/` 的 cold-fluid runtime layer，而 `analysis.py` 当前提供的是重联率可观测量提取与可视化，不是带显式容差 `assert` 的强 benchmark。
- 2026-05-17：继续推进“应用案例综合章”，收口 `Beam-beam / luminosity / FEL / ion extraction`：新增 `notes/code-reading/applications/06-beam-collider-fel-extraction.md`，把 `diff_lumi_diag`、`beam_beam_collision`、`free_electron_laser`、`ion_beam_extraction` 与 `accelerator_lattice` 从分散在 diagnostics、laser、utils 和 accelerator-lattice 笔记里的材料重新收束成一条束流与加速器应用主线，并明确保留当前 worktree 的真实分层：`DifferentialLuminosity` 是 reduced-diagnostic 强谱基准，`beam_beam_collision` 只是 collider-QED 应用骨架，`free_electron_laser` 是 boosted rigid-beam + undulator + BTD 的强 benchmark，`ion_beam_extraction` 是 EB electrostatic extraction 的强应用入口，而 `accelerator_lattice` 则提供 beamline optics 的强回归层。
- 2026-05-18：继续推进文献处理主线，先处理 `references/02_books_lecture_notes/` 下现成的两份开放讲义：为 `no-year_PICSimulationNotesYoujunHu2019_Particle_In_Cell_PIC_simulation` 与 `no-year_ComputationalMethodsPlasmaPhysicsNotes_Computational_Methods_in_Plasma_Physics_lecture_notes` 建立论文专属目录，实际跑通 MinerU 转 Markdown，补齐 `images/`、中文讲解笔记骨架与 `reading-log.md`，并同步把 `docs/literature-map.md` 与 `references/00_index/current_inventory.md` 刷新到当前真实文件树。
- 2026-05-18：继续压实文献处理边界，核对 `Birdsall-Langdon / Hockney-Eastwood / Dawson` 在当前工作树中的真实可用状态：前两者目前只有 BibTeX 与待寻找清单，没有本地合法 PDF；`Dawson` 当前已有 `TajimaDawson1982`、`Tajimaprl79`、`DawsonRMP83` 三个文章级条目，但仍没有落到 `references/` 里的 PDF、论文专属目录或 MinerU 产物，因此把这组缺口从“可直接转文献”改写成更精确的 `book-level missing` 与 `article-level not yet materialized` 两层。
- 2026-05-18：继续沿 `research-paper-explainer` 文献主线推进，实际从本机 `/Users/yuxiangzhang/Documents/Zoteropaper/` materialize 三份核心 PIC foundations 资料：`Birdsall 1985`、`Tajima 1979`、`Dawson 1983`。其中 `Birdsall 1985` 因 `469` 页超出 MinerU `200` 页上限，先用新增的 `scripts/split_pdf_pypdf.py` 拆成三段再完成 MinerU 转换；三份资料当前都已具备项目内专属目录、原 PDF、Markdown/`images/`、中文讲解笔记骨架与 `reading-log.md`，并已把文献缺口收缩到 `Hockney-Eastwood` 与 `TajimaDawson1982` 两项。
- 2026-05-18：继续对剩余文献缺口做本机证据收口，额外扫描了 `Zoteropaper`、`llm-for-zotero-mineru` 以及更宽的 `Documents/` 范围；当前仍未发现 `Hockney-Eastwood` 或 `TajimaDawson1982` 的本地 PDF/MinerU 产物，因此这两项已从“待处理文献”进一步压实为“本机当前无现成资产”的明确缺口。
- 2026-05-18：继续从文献“落盘/转换”推进到正式精读，已开始 `Birdsall 1985` 第一分卷的顺序化中文笔记：当前已覆盖 `Foreword`、`Preface`、Part One 入口、Chapter 1，以及 Chapter 2 的 `2-1` 到 `2-4`，并把 PIC 合法性论证、Debye 长度与 `N_D`、粒子-网格-场循环、以及 leapfrog mover 的最小教学骨架写回书籍专属中文讲解笔记。
- 2026-05-18：继续推进 `Birdsall 1985` 第一分卷精读，从 Chapter 2 的 `2-5`、`2-6` 进入 `3A ES1` 开头，已把静电 `FFT` 场求解、离散算子 `kappa/K^2`、NGP/CIC weighting、以及 `INIT -> SETRHO -> FIELDS -> SETV -> ACCEL -> MOVE -> HISTRY` 的最小程序骨架写回中文讲解笔记，并把下一步推进点明确前移到 `3-7 SETRHO`、`3-8 FIELDS`、`3-10 SETV`、`3-11 ACCEL`、`3-12 MOVE`。
- 2026-05-18：继续推进 `Birdsall 1985` 第一分卷精读，已补入 `3-7` 到 `3-12` 和 `4-6` 到 `4-10`：把 `SETRHO` 写成 `t=0` 的第一次真实 charge deposition，把 `FIELDS` 写成 `rho -> FFT -> phi -> E -> field energy` 的完整离散合同，把 `SETV/ACCEL/MOVE` 写成最小 leapfrog runtime contract，同时把 `S(x)/S(k)`、finite-size particles、grid force、aliasing、Poisson 系统误差与 `rho_k phi_k^*` 场能量账本写回书籍专属中文笔记，并已最小回填到第 5 / 6 章正文。
- 2026-05-18：继续推进 `Birdsall 1985` 第一分卷精读，已补入 Chapter 4 的 `4-3` 到 `4-5`：把 electric impulse 与 magnetic rotation 的几何分裂、`tan(theta/2)` 与 `t/s/c` 半角变量、向量 Boris 形式、以及 `1d2v/1d3v` 的建模边界写回中文讲解笔记，并把这条文献来源最小回填到第 4 章 Boris pusher 相关正文。
- 2026-05-18：继续推进 `Birdsall 1985` 第一分卷精读，已补入 Chapter 8 的 `8-1` 到 `8-7` 与 `8-10` 到 `8-13`：把 spatial-grid theory、sampled density aliasing、同一 `S` 对应的 momentum conservation 条件、`K/\kappa/S` 与 alias-summed dielectric function、以及 cold beam / warm plasma 的 nonphysical instability 条件写回中文讲解笔记，并已最小回填到第 5 / 6 章正文。
- 2026-05-18：继续推进 `Birdsall 1985` 第一分卷精读，已补入 Chapter 9 的 `9-4`、`9-7` 与 Chapter 10 的 `10-2` 到 `10-4`、`10-9` 到 `10-10`：把 finite-`\Delta t` time alias、numerical instability / nonphysical heating、`subcycling/orbit-averaging/implicit` 的共同数值动机、`momentum-conserving` 路线下严格总能量不存在的原因、以及 `energy-conserving` 路线对 `W_E=(V_c/2)\sum \rho_j\phi_j` 与 reciprocity 的依赖写回中文讲解笔记，并已最小回填到第 5 / 6 章正文。
- 2026-05-18：继续做 `Birdsall 1985` 的系统正文回填，已把 Chapter 10 关于 `energy-conserving` / `momentum-conserving` 两套离散守恒合同的理论边界补进第 4 章 gather-family 小节，明确这不只是 `IndexType` / centering 的实现差异，还涉及 `W_E=(V_c/2)\sum \rho_j\phi_j`、粒子受力构造和 reciprocity 所服务的不同守恒目标。
- 2026-05-18：继续推进 `Birdsall 1985` 第一分卷精读，已补入 Chapter 12 的 `12-3` 到 `12-7`：把 `(\rho^2)_{k,\omega}` fluctuation spectrum、`1/2\\rho\\phi` 场能量谱、`grid collisions` 的 kinetic equation、`H`-theorem 对 nonphysical heating 的硬解释，以及 drag / diffusion / entropy production 的统一图像写回中文讲解笔记，并已最小回填到第 6 章。
- 2026-05-18：继续把 Birdsall 文献结论和 WarpX 本地 validation 配对：已把 `energy_conserving_thermal_plasma`、`langmuir*`、`uniform_plasma`、`nci_psatd_stability` 分别挂到 Chapter 8/10/12 的 `aliasing / 守恒合同 / fluctuation energy / entropy production` 边界上，避免再把这些测试笼统写成“能量脚本”或“基础波例子”。
- 2026-05-18：继续推进 `Birdsall 1985` 的 Chapter 13，已把 `13-2` 与 `13-3` 的已核实部分写回中文讲解笔记：当前已明确压实一维 sheet model 对 Maxwellian、Debye shielding、wake、drag、diffusion 与 relaxation-time 的解释，区分了 `2N_D/\\omega_p` 级的 randomization/correlation time 与更慢的 `N_D^2` 级 whole-distribution thermalization，并已最小回填到第 1 章理论入口。
- 2026-05-18：继续推进 `Birdsall 1985` 的 Chapter 13，已把 `13-4` 与 `13-5` 的已核实部分写回中文讲解笔记：当前已明确压实 `tau_H` 对 `lambda_D/Delta x`、shape order、`v_t Delta t / Delta x` 与 mover phase error 的依赖，区分了 self-heating、nonphysical cooling、`tau_s`、`tau_H/tau_s` 和 Hockney 2d2v optimum path 的工程意义，并已最小回填到第 5 / 6 章。
- 2026-05-18：继续压实 `Birdsall 1985` `13-5` 后半段，已把 `K_4`、QPM、`E_x^2` fluctuation 的 `1/N_C` 缩放、以及近线性 stochastic heating 这组经验量补进中文讲解笔记，并把后续缺口更明确收缩到 `Hockney-Eastwood` 原书级核对。
- 2026-05-18：继续对 `Hockney-Eastwood` 缺口做证据收口：再次确认本机常用文献目录里没有原书或其直接 article-level fallback PDF/MinerU 产物；同时已把 `Hockney (1971)`、`Hockney et al. (1974)`、`Eastwood and Hockney (1974)`、`Abe et al. (1975)`、`Peiravi and Birdsall (1978)` 这组 fallback targets 写回文献索引，避免后续继续以“缺一本书”的模糊状态接续。
- 2026-05-18：继续压实 `Hockney-Eastwood` fallback 线的外部元数据：已核到 `Hockney 1971` 的正式题名 *Measurements of collision and heating times in a two-dimensional thermal computer plasma*，以及 `Hockney et al. 1974` / `Eastwood and Hockney 1974` 的 DOI 级落点；当前虽然本机仍无现成 PDF，但后续已不再依赖从 Birdsall 转述里反推题名。
- 2026-05-18：继续推进 `Hockney 1971` 的 metadata 级压实：其摘要层已经足够确认 `tau_coll / tau_pe = n(\lambda_D^2 + W^2)`、`E^2` fluctuation 的 `1 / n(\lambda_D^2 + W^2)` 缩放、optimum path `( \omega_{pe}\Delta t )_{opt}`，以及 `K_2` 对 NGP/HNGP/CIC/HCIC 的数值分层；也就是说，在拿到全文前，这篇 article 已经不是“只有题名”的状态。
- 2026-05-18：继续压实 `TajimaDawson1982` 的 metadata 边界：现已确认其正式出版信息为 *AIP Conference Proceedings* `91(1):69-93`、`Sep 1982`、DOI `10.1063/1.33805`，并再次确认本机常用文献目录与 `minerU/md_output` 中都没有现成 PDF/MinerU 产物；因此这条线当前已从“BibTeX 级缺口”推进到“metadata 已确认、全文仍待 materialize”。
- 2026-05-18：继续收紧 `TajimaDawson1982` 的 acquisition 边界：现已确认 FNAL 可访问的 `p169.pdf` *Laser accelerator by plasma waves for ultra-high energies* 是 `T. Tajima` 单作者的相关 1982 conference note，而不是 `TajimaDawson1982` 本文；同时也已把 `Hockney 1971 / 1974 / Eastwood-Hockney 1974` 的当前 web 证据层级明确写成“题名/DOI/摘要级已知，但本地 full-text 仍未 materialize”。
- 2026-05-18：继续做 acquisition 实证核对：`Hockney et al. 1974` 虽然已能定位到 ScienceDirect 的 `.../pdf` 端点，但当前环境实际返回的是 download-preparation / browser-compatibility 页面，而不是可直接 materialize 的正文 PDF；`TajimaDawson1982` 这边也仍只有 DOI 级引用与 FNAL 相关 note，尚未找到可合法落地到本地目录的全文。
- 2026-05-18：从获取层切回可直接推进的精读主线，已开始正式精读 `Tajima and Dawson 1979`：当前已把摘要、引言与最小解析模型写回中文讲解笔记，已明确压实 `driver -> wake -> trapping -> acceleration` 闭环、`v_p = v_g^{EM}`、`L_t = \lambda_w/2`、`eE_L \cong mc\omega_p`、`\gamma_{\max} \simeq 2\omega^2/\omega_p^2` 与 `l_a \cong 2\omega^2 c/\omega_p^3` 这组 earliest LWFA scaling，并已最小回填到 `LWFA/PWFA` 应用笔记与第 8 章正文。
- 2026-05-18：继续推进 `Tajima and Dawson 1979`，已把 simulation 与结果段落补进中文讲解：当前已明确写清 `1 1/2-D` relativistic electromagnetic code、Gaussian finite-size particles、固定离子背景、通过改变 `c` 扫描 `\omega/\omega_p` 的相似律设置、wake amplitude 达到 `0.6 mc\omega_p/e`、successive / multiple forward Raman scattering、photon deceleration，以及 simulation 最大能量对最早期解析 scaling 的直接贴合；相关边界也已继续回填到 `LWFA/PWFA` 应用笔记与第 8 章。
- 2026-05-18：继续完成 `Tajima and Dawson 1979` 文末边界精读：当前已明确降级写清 `feasible within present-day technology` 只是 1979 年的工程可行性语气，short-pulse shaping 仍是当时 bottleneck；原文保留 `\Delta\omega = \omega_p` 的 two-laser / beat-wave alternative，因此它支撑的是更宽的 early wakefield family；而 pulsar / cosmic-ray 段落只应视为 speculative extrapolation，不进入现代 `LWFA/PWFA` 应用合同。
- 2026-05-18：继续完成 `Tajima and Dawson 1979` 图文对应精读：当前已把 Fig.1 压实为 `transverse quiver -> longitudinal wake -> accelerating field` 的三联机制图，把 Fig.2(a) 压实为 driver spectrum splitting / multiple Raman scattering / photon deceleration 证据图，把 Fig.2(b) 压实为 earliest LWFA scaling 的 simulation-vs-theory 对照图；这意味着该文献的第一轮精读主线已经基本收口。
- 2026-05-18：已启动 `Dawson 1983` 的第一轮正式精读，并完成引言、particle models 总述与 electrostatic/FSP 主链的第一批中文讲解：当前已明确把 computer simulation 写成 experiment / analytic theory 之外的第三种研究手段，把 PIC 定义压回“跟踪大量带电粒子在自洽电磁场中的运动”，把 `superparticle` 写成 practical necessity，并把 finite-size particles、grid coarse graining 与 `shape factor -> charge sharing -> FFT Poisson -> gather back` 的标准合同最小回填到第 1 / 5 / 6 章。
- 2026-05-18：继续推进 `Dawson 1983` 的第一轮精读，已补入 `electromagnetic particle models`、`fractional dimensional models`、`Darwin` 与 diagnostics 哲学：当前已明确写清 `1 1/2-D / 1 2/2-D / 2 1/2-D` reduced-dimension fully electromagnetic models 的真实含义、full EM 时间步受最高频 light mode/CFL 限制、Darwin route 通过去掉 radiation branch 换取低频磁化问题的较长时间步、以及 diagnostics 的目标是提炼 physics essence 而不是保留全部 detail，并已最小回填到第 4 / 6 / 8 章。
- 2026-05-18：继续推进 `Dawson 1983` 的第一轮精读，已补入 `numerical stability`、`quiet starts` 与 diagnostics 细分量：当前已明确把数值不稳定压成 spatial aliasing 与 time aliasing 两类 stroboscopic errors，把 diagnostics 的第一性分类写成 particle-motion measurements 与 wave measurements，并把 random start 对 small-`k` 长波模式的过激发、quiet start 降噪及其自身 ordering instabilities 的 tradeoff 写回中文讲解；相关结论已最小回填到第 6 / 8 章和 `uniform_plasma` 应用笔记。
- 2026-05-18：继续推进 `Dawson 1983` 的第一轮精读，已补入 `electrons of many sizes, charges, and masses`、quiet-start instabilities 的后续工程边界，以及 `free-electron laser` 历史例子：当前已明确把非等权宏粒子写成保持相同 `q/m` 的 weighted macroparticle kinetic model，把 quiet order 的退化压回离散 beam normal-mode instability 与有限 growth-time 窗口，并把 FEL 写成 relativistic EM-PIC 的经典代表问题，包含 matching-condition 谱证据、约 `36%` 的 longitudinal current 下降、约 `30%` 的束流能量转辐射和 `2\\lambda_0` backward mode 风险；这些结论已最小回填到第 1 / 8 章和束流-FEL 应用笔记。
- 2026-05-18：继续完成 `Dawson 1983` 的 FEL 子线收口，已把 `Fig.54-57` 与 efficiency estimate 的图文对应写回中文讲解：当前已明确 `Fig.54` 同时承担 lab-frame kinematics 与 beam-frame pump-wave 图像，`Fig.55` 用来核对 EM/electrostatic matching condition，`Fig.56` 用来同步检查 EM energy、electrostatic energy 与 longitudinal current 的转折，而 `Fig.57` 则把 trapping-based efficiency estimate 与 simulation 并排比较；相关结论已最小回填到第 8 章和束流-FEL 应用笔记。
- 2026-05-18：继续推进 `Dawson 1983` 统计理论 examples 的第一批入口，已把 `Tests of the statistical theory of plasmas` 与 `Kinetics of a one-dimensional plasma` 的开场边界写回中文讲解：当前已明确一维 electrostatic sheet model 在这篇综述里承担无 grid、近 exact dynamics 的 benchmark role，且 drag、diffusion、field fluctuations、correlation measurements 被直接定位成检验 subtle plasma statistics 的合同；相关结论已最小回填到第 6 / 8 章。
- 2026-05-18：继续推进 `Dawson 1983` 统计理论主线，已补入 drag、velocity diffusion 与 thermal field fluctuation 的第一批测量合同：当前已明确 drag 依赖 velocity-shell ensemble 的平均速度衰减、velocity diffusion 具有 `\tau^2 -> \tau` 的双阶段时间结构、decorrelation time 同时控制统计独立性与 kinetic interpretation，而 point-particle `KT/2` 型 modal fluctuation level 会被 finite-size particle shape 系统改写；这些结论已最小回填到第 6 / 8 章。
- 2026-05-18：继续推进 `Dawson 1983` 的 wave-side measurements，已补入 power spectrum、time correlations 与 magnetized fluctuation peaks 的第一轮中文精读：当前已明确 power spectrum 的任务是分离 Debye-cloud continuum 与 plasma spike，`\\Delta\\omega \\simeq 1/T` 直接限制谱分辨率，而磁化谱会把 Bernstein、upper-hybrid、ion-cyclotron/lower-hybrid 及 `\\omega=0` convective-cell 结构直接编码进诊断图；相关结论已最小回填到第 6 / 8 章。
- 2026-05-18：继续推进 `Dawson 1983` 的 wave-side measurements，已补入 nonuniform-plasma normal-mode reconstruction：当前已明确离散谱线可通过与 `\\sin\\omega t` / `\\cos\\omega t` 的相关积分重建空间波函数与相位，integration window `T` 必须短于 damping time，长运行应拆成多个短窗口平均；同时已明确 continuous spectrum 既可能对应 localized modes，也可能只是 random particle motion，因此必要时要从 `\\phi(x,\\omega)` 继续下钻到 `\\delta v(\\mathbf v,x,\\omega)` 这类 kinetic diagnostics。
- 2026-05-18：继续推进 `Dawson 1983` 的 statistical-theory / quiet-start 边界，已补入 `\\delta v(\\mathbf v,x,\\omega)` 的 kinetic diagnostics 落点，以及 random start 对 weak-instability growth-rate measurement 的限制：当前已明确可用增长窗口近似受 `\\gamma t \\sim \\frac{1}{2}\\ln N` 限制，纯随机空间加载还会强烈过激发 small-`k` long-wavelength electrostatic modes，因此 quiet-start / cell-neutral loading 的价值在于提升弱效应测量的可识别动态范围，而不只是让初值更平滑。
- 2026-05-18：继续推进 `Dawson 1983` 的 quiet-start implementation 边界，已补入 phase-space cell construction、equal-area velocity bins 和 arbitrary distribution 的 cumulative-map 构造：当前已明确 quiet start 的本质是用更规则的有限粒子 phase-space covering 换取更大的 weak-effect dynamic range，但其 tail / low-density critical region 表示能力有限，因此需要和 weighted particles / many-size electrons 一起理解，而不是单独神化成“更真实初态”。
- 2026-05-18：已基于当前中文讲解、reading-log、正文回填和 TODO 状态评估 `Dawson 1983` 的 statistical-theory / quiet-start 主线，当前判断这条线已经到自然收口点；下一未完成基础文献前沿已前移到 `Yee 1966`，并已把其 DOI `10.1109/TAP.1966.1138693` 与“本机常用文献目录暂无现成 PDF / MinerU 资产”的 acquisition 边界写回项目索引。
- 2026-05-18：继续压实 `Yee 1966` acquisition 边界：针对 DOI、IEEE 文章号 `1138693/01138693`、完整题名 stem，以及 `Documents/Desktop/Downloads` 的本机扫描均无命中；当前证据已足以说明这不是“常用目录里漏找一个文件”，而是确实还没有本机可直接走 MinerU 的正文资产。
- 2026-05-18：继续验证 `Yee 1966` 的外部全文落点：当前只确认到 CiNii / ScienceOpen 的 metadata 记录，以及 CiNii 暴露出的历史 IEEE PDF 端点模式 `01138693.pdf?arnumber=1138693`；但本轮仍未获得可合法直接进入 MinerU 的全文 PDF，因此这条线依旧停留在 acquisition 边界，而不是正文精读阶段。
- 2026-05-18：在 `Yee 1966` acquisition 阻塞的同时，已把下一组可直接推进的 pusher 文献接入项目：`Vay 2008` 已复用本机现成 MinerU Markdown 与 `images/` materialize 到 `references/04_particle_pushers_deposition_shapes/2008_VayPOP2008_Simulation_of_beams_or_plasmas_crossing_at_relativistic_velocity/`；`Higuera 2017` 已在项目内通过 `scripts/mineru_convert_stdlib.py` 完成 MinerU 转换并建好论文专属目录、中文讲解骨架和 `reading-log`。当前这组文献已经可直接进入第 4 章相关精读。
- 2026-05-18：已启动 `Vay 2008` 第一轮正式精读，当前已把摘要、引言和 `II.A/II.B` 的最小历史论证链写回中文讲解笔记：明确这篇文章的核心问题是 boosted-frame / relativistic-crossing 场景下 electric/magnetic cancellation property 的离散失真，而 WarpX `UpdateMomentumVay.H` 对应的是一条以 frame-change consistency 为目标的专门修正路线，不应被泛写成“另一种 relativistic mover”。
- 2026-05-18：已继续完成 `Vay 2008` 主文后半段精读，并把 single-particle moving-frame tests、bounded Darwin-lite field solver 边界，以及 ultrarelativistic beam / electron-cloud 一致性示例正式写回中文讲解、`reading-log` 和第 4 章。当前已明确：这篇文献最硬的证据不是一般轨道图，而是同一物理系统换参考系后仍要保持解析一致；field side 只是 problem-specific 的 `N` 次拉伸 Poisson solve 近似，不能误写成 WarpX 通用 EM solver 的直接来源。
- 2026-05-18：已启动 `Higuera 2017` 第一轮正式精读，并把摘要到结论的主线写回中文讲解、`reading-log` 和第 4 章。当前已明确：这篇文献的主问题不是 boosted-frame cancellation，而是 Boris / Vay / Higuera-Cary 在 `E=0` 能量守恒、`\vec E\times\vec B` drift 与 volume-preservation 三条性质上的三角比较；它给 WarpX `UpdateMomentumHigueraCary.H` 提供的不是一般“更准 pusher”来历，而是一条以 geometric/topological preservation 为核心的历史与数学边界。
- 2026-05-18：已继续把 `Higuera 2017` 从主文概述推进到关键公式层：当前已经把论文里的 `\vec\epsilon / \vec\beta / \vec u_- / \gamma_{new}`、Boris-like rotation equation，以及 Jacobian / volume-preservation 证明直接映回 WarpX `UpdateMomentumHigueraCary.H` 的 `qmt / beta* / um* / sigma / ust / tx/ty/tz / s / up*` 变量链，并补入 `notes/code-reading/particles/03-vay-higuera-cary-pushers.md` 与第 4 章。现在这条文献线已经不只是在讲“性质上更好”，而是把论文公式、源码变量和 practical-timestep topology 证据闭合起来了。
- 2026-05-18：已继续把 `Higuera 2017` 和 WarpX validation 更明确配对：当前最直接能接论文主线的是 `Examples/Tests/particle_pusher` 这条 force-free Higuera-Cary 强 analysis，它能支撑 relativistic force-free / drift-preservation 的本地强断言；而 `larmor` 目前仍只有 checksum，不能冒充论文第 VI 节 Poincare topology / resonance-island 结论的完整本地复现。
- 2026-05-18：已继续把 `Higuera 2017` 的 Jacobian / volume-preservation 从结果描述推进到更完整的符号链：当前已把 `I-\Omega`、determinant lemma、`J_{f,new}` 标量化，以及“前后半步 determinant 互为逆所以整步 Jacobian 为 1”这条主干，分别回填到论文中文讲解、`particles/03-vay-higuera-cary-pushers.md` 和第 4 章。
- 2026-05-18：已继续收紧 `Higuera 2017` 的 Jacobian 记号边界：当前已明确 `J_{i,new}` / `J_{f,new}` 虽然写成同形标量式，但对应的是前/后半步相反方向映射上的 determinant，因此在整步组合里处于 reciprocal 位置；同时也补写了 Vay Jacobian 只是在一般场景下不 volume-preserving、而在时空常量磁场里会 telescoping 的例外边界。
- 2026-05-18：已核对 `Boris` 原始资料的 acquisition 边界：当前项目里只有 `docs/literature-map.md` 中的 `Borisjcp73 / BorisICNSP70 / Habericnsp73` metadata，以及本机导出代码/讲义残片（如 `boris_method.tex`、`boris_cpp.tex`、WarpX/PICSAR 的 `UpdateMomentumBoris.H` / `boris_2d.F90`），但在 `Zoteropaper`、`llm-for-zotero-mineru`、更宽的 `Documents/Desktop/Downloads` 中都没有可直接进入 MinerU 的 primary-source PDF；因此 `Boris` 这条线当前也仍停留在 acquisition，而不是正文精读。
- 2026-05-18：已回到源码主线继续推进 `WarpX` root 生命周期：补完 `notes/code-reading/root/02-construction-and-level-allocation.md` 中 `AllocLevelData()` / `AllocLevelMFs()` 的剩余深层分支，当前已明确 `effective potential` 在 level 分配期只复用 `rho_fp/phi_fp` 静电合同、`HybridPICModel` 通过 `AllocateLevelMFs()` 把 electron-pressure / plasma-current / external-current 字段写入共享 `m_fields` register、implicit 路线在 level 分配期只先放 `current_fp_non_suborbit` 与 `E_old` 而把 `MassMatrices_*` 延后到 `InitializeMassMatrices()`，以及 `PML` 不是和主网格字段一起出生而是 `InitFromScratch()` 末尾附加的边界 patch；这些结论已同步回填到第 3 章并写回 `TODO`。
- 2026-05-18：已继续把上述 root 生命周期关系压成显式结构：在 `notes/code-reading/root/02-construction-and-level-allocation.md` 与第 3 章新增了 root lifecycle 的 Mermaid 状态图和对象/字段落点表，把 `WarpX::WarpX()`、`AllocLevelMFs()`、`InitFromScratch()/InitPML()`、`InitializeMassMatrices()` 之间的“对象创建 vs. level 字段分配 vs. 初始化末段附加边界 vs. 线性求解准备期”四层顺序写死，避免继续靠长段落维持这些边界。
- 2026-05-17：对 `Diagnostics/` 做了当前 worktree 收口审计：确认 `notes/code-reading/diagnostics/00-09` 已覆盖 `BoundaryScraping`、`MultiDiagnostics/Diagnostics/FullDiagnostics`、`ComputeDiagFunctors`、`ReducedDiags`、`FlushFormats/WarpXIO/BTDiagnostics`、writer/layout/reader-side 工具链与 Python boundary-buffer 消费模式；同时把这些 diagnostics 笔记正式登记进 `docs/source-map.md`，并把 `TODO.md` 里的 `Diagnostics/` 与 `reduced_diags/restart/particle_data_python/python_wrappers/openPMD examples` 这组过期未完成项收口。
- 2026-05-17：继续推进 `Python/` 主线：新增 `python/00-python-module-init.md`、`01-callbacks.md`、`02-field-particle-access.md`，把维度专用 pybind module 的编译期分裂、`_libwarpx.py` 的 geometry-aware lazy load、`WarpX.cpp` binding 的 singleton/runtime-object 暴露、Python `CallbackFunctions` 到 C++ `ExecutePythonCallback(name)` 的聚合桥，以及 `multifab_register` / `multi_particle_container` / `particle_boundary_buffer` 与 PICMI `sim.fields` / `sim.particles` / `Simulation.step()` 的真实映射压成源码笔记，并同步回填 `source-map`、第 4 章与 `TODO` 状态。
- 2026-05-17：继续推进 `Tools/*` 审计：新增 `tools/README.md`、`00-parser-postprocessing-algorithms-boundary.md`、`01-qed-tables-utils.md`，把 top-level `Tools/Parser` 压实为轻量输入卡 helper、把 `Tools/PostProcessing` / `Tools/Algorithms` 和现有 diagnostics/fieldsolver 笔记的覆盖边界写清，并把 `Tools/QedTablesUtils` 压实为 `qed_table_generator` / `qed_table_reader` 两个离线 QED lookup-table 工具的命令行分派、二进制布局和 reader/dumper 路径。
- 2026-05-17：继续推进构建系统主线：新增 `build/README.md`、`00-cmake-superbuild-and-module-aggregation.md`、`01-gnu-make-and-dimension-variants.md`、`02-hpc-machine-profiles.md`，把顶层 `CMakeLists.txt` 的 option/variant 矩阵、依赖 superbuild 与 `lib_${SD}` / `pyWarpX_${SD}` 聚合链，`Source/Make.WarpX` 的旧 GNUmake feature 宏和维度/后缀编码，以及 `Tools/machines/*` 的 `warpx.profile -> install_dependencies.sh -> submit.sh/.sbatch/.pbs/.flux` 三件套压成源码笔记，并在第 3A 章补写了 `geometry.dims` 强一致断言背后的构建期几何变体来源。
- 已建立项目控制文件：`AGENTS.md`、`README.md`、`TODO.md`。
- 已确认同级 `../warpx` 存在，并在其中识别出主要一手资料：
  - `README.md`
  - `Docs/source/index.rst`
  - `Docs/source/theory/`
  - `Docs/source/usage/`
  - `Docs/source/developers/`
  - `Docs/source/refs.bib`
  - `Source/`
  - `Examples/`
  - `Regression/`
  - `Tools/`
- 本地 `../warpx/Docs/source/refs.bib` 当前包含 251 条 BibTeX 入口，可作为第一批文献池。
- 当前书稿绑定的本地 WarpX 源码状态：
  - 分支：`pkuHEDPbranch`
  - commit：`063f8b586f04321e13150ae3e730e0794ca75cb1`
  - 源码映射记录：`docs/source-map.md`

## 计划文档

初版总计划见：

- `docs/master-plan.md`
- `docs/warpx-full-code-reading-book-plan.md`
- `docs/paper-reading-workflow.md`
- `docs/source-map.md`
- `docs/parameter-map.md`
- `docs/parameter-chapter-index.md`
- `docs/chapter-template.md`
- `docs/warpx-source-reading-framework.md`

其中 `docs/warpx-full-code-reading-book-plan.md` 是 2026-05-14 之后的最高优先级写作计划。它取代早期“先写粗略完整版本”的路线，改为从物理过程、数学推导、数值算法到 WarpX 全源码模块逐行/逐块精读的路线。

`docs/warpx-source-reading-framework.md` 是该总计划下面的执行框架：它按 `Source` 的真实模块、构建边界和依赖关系规定精读顺序，后续推进应优先按这个框架逐阶段执行，而不是继续只围绕局部源码扩写。

## 书稿入口

当前 Markdown 书稿见：

- `manuscript/README.md`
- `manuscript/chapters/00-preface.md`
- `manuscript/chapters/01-kinetic-models.md`
- `manuscript/chapters/02-pic-loop.md`
- `manuscript/chapters/03-warpx-evolve.md`
- `manuscript/chapters/03a-warpx-initialization.md`
- `manuscript/chapters/04-particle-pushers.md`
- `manuscript/chapters/05-deposition-shapes.md`
- `manuscript/chapters/06-field-solvers.md`
- `manuscript/chapters/07-boundaries-amr.md`
- `manuscript/chapters/08-diagnostics-cases.md`
- `manuscript/chapters/09-literature-roadmap.md`

当前主循环精读笔记见：

- `notes/code-reading/README.md`
- `notes/code-reading/evolve/README.md`
- `notes/code-reading/evolve/00-lifecycle-and-callgraph.md`
- `notes/code-reading/evolve/01-pic-time-layers.md`
- `notes/code-reading/evolve/02-evolve-source-evidence.md`
- `notes/code-reading/evolve/03-subcycling-and-jrhom.md`
- `notes/code-reading/evolve/04-compute-dt-and-adaptive-timestep.md`
- `notes/code-reading/evolve/05-moving-window.md`

当前粒子推进与沉积精读笔记见：

- `notes/code-reading/particles/README.md`
- `notes/code-reading/particles/00-particle-evolve-callchain.md`
- `notes/code-reading/particles/01-pusher-and-deposition-evidence.md`
- `notes/code-reading/particles/02-gather-shape-deposition-kernels.md`
- `notes/code-reading/particles/03-vay-higuera-cary-pushers.md`
- `notes/code-reading/particles/04-amr-gather-deposition-buffers.md`
- `notes/code-reading/particles/05-current-deposition-algorithms-near-amr-buffer.md`
- `notes/code-reading/particles/06-charge-conserving-current-kernel-structures.md`
- `notes/code-reading/particles/07-vay-deposition-d-field-structure.md`
- `notes/code-reading/particles/08-particle-class-and-attribute-map.md`
- `notes/code-reading/particles/09-radiation-reaction-implicit-photon-pushers.md`
- `notes/code-reading/particles/10-implicit-suborbit-mass-matrices-jfnk.md`
- `notes/code-reading/particles/11-particle-boundaries-buffer-sorting.md`
- `notes/code-reading/particles/12-field-ionization-adk-pipeline.md`
- `notes/code-reading/particles/13-collision-handler-stepping-and-regression-map.md`
- `notes/code-reading/particles/14-qed-entrypoints.md`
- `notes/code-reading/particles/15-qed-kernels-and-wrapper-contracts.md`
- `notes/code-reading/particles/16-qed-table-generation-and-serialization.md`
- `notes/code-reading/particles/17-qed-chi-virtual-photons-and-linear-breit-wheeler.md`
- `notes/code-reading/particles/18-binary-collision-product-creation-and-linear-compton.md`
- `notes/code-reading/particles/19-backgroundmcc-pulseddecay-and-dsmc-branches.md`
- `notes/code-reading/particles/20-coulomb-bremsstrahlung-stopping-and-fusion.md`
- `notes/code-reading/particles/21-collision-kernel-details-and-product-initializers.md`
- `notes/code-reading/particles/22-perez-bremsstrahlungevent-and-fusion-probability-control.md`
- `notes/code-reading/particles/23-resampling-thermalizer-and-sorting.md`
- `notes/code-reading/particles/24-thermalizer-validation-and-checksum-boundaries.md`
- `notes/code-reading/particles/25-gather-variants-and-external-particle-fields.md`
- `notes/code-reading/particles/26-pusher-single-particle-and-photon-validation-map.md`
- `notes/code-reading/particles/27-particle-fields-plasma-lens-and-mpi-validation-map.md`
- `notes/code-reading/particles/28-particle-diagnostics-python-interface-validation-map.md`
- `notes/code-reading/particles/29-boundary-and-python-front-end-validation-map.md`
- `notes/code-reading/particles/30-pml-eb-contact-and-mr-validation-map.md`
- `notes/code-reading/diagnostics/00-boundary-scraping-diagnostics-python.md`
- `notes/code-reading/diagnostics/01-diagnostics-dispatch.md`
- `notes/code-reading/diagnostics/02-field-and-particle-functors.md`
- `notes/code-reading/diagnostics/03-reduced-diagnostics.md`
- `notes/code-reading/diagnostics/04-io-formats-and-restart.md`
- `notes/code-reading/diagnostics/05-reduced-diagnostic-case-studies.md`
- `notes/code-reading/diagnostics/06-writer-comparison-and-minimal-cases.md`
- `notes/code-reading/diagnostics/07-output-layouts-and-reading-tools.md`
- `notes/code-reading/diagnostics/08-template-cases-and-boundaryscraping-example.md`
- `notes/code-reading/diagnostics/09-python-boundary-buffer-callback-case.md`

其他 Source 模块的精读入口已按框架建立在 `notes/code-reading/` 下，包括 `root/`、`initialization/`、`fieldsolver/`、`boundary/`、`parallelization/`、`diagnostics/`、`embedded-boundary/`、`filter/`、`laser/`、`fluids/`、`nonlinear-solvers/`、`accelerator-lattice/`、`python/`、`utils/`、`ablastr/`。

其中 `fluids/` 不再只是入口壳：`00-fluid-container-map.md`、`01-muscl-hancock-update.md`、`02-fluid-pic-coupling.md` 已把当前 worktree 下的冷流体 species 对象图、MUSCL-Hancock 守恒更新以及与 PIC 主场/HybridPIC 的真实耦合边界补齐。

Parallelization / AMR 通信精读笔记见：

- `notes/code-reading/parallelization/README.md`
- `notes/code-reading/parallelization/00-guard-cell-model.md`
- `notes/code-reading/parallelization/01-current-rho-sync-paths.md`
- `notes/code-reading/parallelization/02-regrid-and-load-balance.md`
- `notes/code-reading/parallelization/03-amr-coarse-fine-substitution.md`
- `notes/code-reading/parallelization/04-warpxcomm-kernel-execution-model.md`

Root / WarpX 主类状态精读笔记见：

- `notes/code-reading/root/README.md`
- `notes/code-reading/root/01-warpx-state-map.md`
- `notes/code-reading/root/02-construction-and-level-allocation.md`

Initialization 初始化精读笔记见：

- `notes/code-reading/initialization/README.md`
- `notes/code-reading/initialization/00-init-callgraph.md`
- `notes/code-reading/initialization/01-external-fields.md`
- `notes/code-reading/initialization/02-plasma-injector.md`
- `notes/code-reading/initialization/03-density-momentum-dispatch.md`
- `notes/code-reading/initialization/04-particle-creation-kernels.md`
- `notes/code-reading/initialization/05-projection-div-cleaner.md`
- `notes/code-reading/initialization/06-gaussian-beam-openpmd-injection.md`
- `notes/code-reading/initialization/07-temperature-velocity-properties.md`
- `notes/code-reading/initialization/08-initialization-bootstrap.md`
- `notes/code-reading/initialization/09-preconstruct-parameter-locking.md`
- `notes/code-reading/initialization/10-readparameters-runtime-landing.md`
- `notes/code-reading/initialization/11-readparameters-combination-constraints.md`
- `notes/code-reading/initialization/12-alloclevelmfs-specialized-branches.md`
- `notes/code-reading/initialization/13-initdata-postallocation-consumption.md`
- `notes/code-reading/initialization/14-initialization-validation-map.md`
- `notes/code-reading/initialization/15-initialization-validation-map-external-relativistic-openbc.md`
- `notes/code-reading/initialization/16-initialization-validation-map-density-magnetostatic-nodal.md`
- `notes/code-reading/initialization/17-initialization-validation-map-electrostatic-sphere.md`
- `notes/code-reading/initialization/18-initialization-validation-map-dirichlet-and-effective-potential.md`
- `notes/code-reading/initialization/19-initialization-validation-map-openbc-relativistic-nodal.md`
- `notes/code-reading/laser/00-laser-profile-dispatch.md`
- `notes/code-reading/laser/01-gaussian-from-file-and-runtime.md`
- `notes/code-reading/laser/02-field-function-and-particle-update-kernel.md`
- `notes/code-reading/laser/03-laser-validation-map.md`
- `notes/code-reading/laser/04-moving-window-external-field-coupling.md`
- `notes/code-reading/laser/05-application-and-diagnostic-cases.md`

FieldSolver 场求解器精读笔记见：

- `notes/code-reading/fieldsolver/README.md`
- `notes/code-reading/fieldsolver/00-fieldsolver-dispatch.md`
- `notes/code-reading/fieldsolver/01-fdtd-evolve-e-b.md`
- `notes/code-reading/fieldsolver/02-fdtd-pml.md`
- `notes/code-reading/fieldsolver/03-pml-damping-current.md`
- `notes/code-reading/fieldsolver/04-noncartesian-fdtd.md`
- `notes/code-reading/fieldsolver/05-psatd-spectral-flow.md`
- `notes/code-reading/fieldsolver/06-psatd-galilean-current-correction.md`
- `notes/code-reading/fieldsolver/07-psatd-jrhom.md`
- `notes/code-reading/fieldsolver/08-psatd-rz-hankel.md`
- `notes/code-reading/fieldsolver/09-electrostatic-magnetostatic.md`
- `notes/code-reading/fieldsolver/10-implicit-and-hybrid.md`
- `notes/code-reading/fieldsolver/11-psatd-coefficient-derivation.md`
- `notes/code-reading/fieldsolver/12-hybrid-pic-model-deep-dive.md`
- `notes/code-reading/fieldsolver/13-fieldsolver-verification-map.md`
- `notes/code-reading/fieldsolver/14-fieldsolver-analysis-criteria.md`
- `notes/code-reading/fieldsolver/15-implicit-jacobian-preconditioner-coupling.md`
- `notes/code-reading/nonlinear-solvers/00-solver-abstractions.md`
- `notes/code-reading/nonlinear-solvers/01-newton-picard.md`
- `notes/code-reading/nonlinear-solvers/02-preconditioners-and-petsc.md`
- `notes/code-reading/nonlinear-solvers/03-residual-jacobian-operator-chain.md`
- `notes/code-reading/nonlinear-solvers/04-petsc-matrixpc-assembly-chain.md`
- `notes/code-reading/nonlinear-solvers/05-warpxsolverdof-and-matrixpc-geometry.md`
- `notes/code-reading/nonlinear-solvers/06-bcmask-massmatrix-window-and-petsc-submit.md`
- `notes/code-reading/nonlinear-solvers/07-implicit-regression-assertion-map.md`
- `notes/code-reading/nonlinear-solvers/08-vandb-jfnk-validation-map.md`

Boundary / AMR 边界与网格细化精读笔记见：

- `notes/code-reading/boundary/README.md`
- `notes/code-reading/boundary/00-field-boundary-parameters.md`
- `notes/code-reading/boundary/01-pml-data-and-update.md`
- `notes/code-reading/boundary/02-pec-insulator-silver-mueller.md`
- `notes/code-reading/boundary/03-boundary-parameter-table.md`
- `notes/code-reading/boundary/04-silver-mueller-internal-stencil.md`

Embedded boundary 精读笔记见：

- `notes/code-reading/embedded-boundary/README.md`
- `notes/code-reading/embedded-boundary/00-eb-initialization.md`
- `notes/code-reading/embedded-boundary/01-face-extensions.md`
- `notes/code-reading/embedded-boundary/02-particle-scraping-and-deposition-near-eb.md`

## 资料使用原则

1. 一手资料优先：WarpX 本地源码、官方文档、官方示例、测试、论文和 BibTeX。
2. 二手资料辅助：DeepWiki、Zread、Context7 等只用于快速定位模块和问题，不直接作为最终权威依据。
3. 章节写作必须同时覆盖：
   - 物理问题是什么；
   - 连续方程和离散算法如何推导；
   - WarpX 中哪个源码模块实现，并在对应物理/算法章节内完成必要的逐行代码讲解；
   - 输入参数如何控制；
   - 示例和测试如何验证；
   - 文献出处是什么；
   - 与其他 PIC 程序或新方法相比的边界在哪里。
4. 源码解释必须绑定当前 `../warpx` 的 commit、准确文件路径、函数名和必要行号；不能凭二手解读或记忆描述实现。
5. 讲解源码时必须放上真实源码原文：每个关键函数/内核先给源码块，再逐行或逐块解释其物理、算法、数据结构、时间层和边界状态。
6. 论文阅读采用 `research-paper-explainer` 风格：先用 MinerU 转 Markdown，转换结果和中文讲解笔记保存在论文同目录或论文专属子目录，按原文顺序逐段总结，图片随文插入，公式用 `$$ ... $$` 并详细推导。

## 预期目录

```text
PIC-tutor/
  AGENTS.md
  README.md
  TODO.md
  docs/
    master-plan.md
    paper-reading-workflow.md
  manuscript/
    chapters/
    figures/
    appendices/
  bibliography/
  notes/
  scripts/
```

## 外部参考入口

- WarpX 官方文档：https://warpx.readthedocs.io/
- WarpX GitHub：https://github.com/BLAST-WarpX/warpx
- DeepWiki 解读：https://deepwiki.com/BLAST-WarpX/warpx
- Zread 解读：https://zread.ai/BLAST-WarpX/warpx
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `FieldProbe` 几何铺设 companion 条目继续压到更真实的运行链：本轮细化了 `<reduced>.x/y/z_probe / x1/y1/z1_probe / target_normal_* / target_up_* / detector_radius`，明确它们不仅在 `FieldProbe` 构造函数解析，还会继续进入 `InitData()` 的 point/line/plane 探针网格生成、`m_probe.AddNParticles(...)` 的 probe 宏粒子落点，以及 `ComputeDiags()` 里的 moving-window 平移和 `ProbeInDomain()` 入域判定链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里一批 `FieldProbe` 相邻 companion 条目继续压到更真实的运行边界：本轮细化了 `<reduced>.resolution / interp_order / raw_fields`，明确 `resolution` 不只决定线/面探针的离散步长，还决定 `m_probe.AddNParticles(...)` 的 probe 粒子总数与最终输出行数；`interp_order` 会在 `ComputeDiags()` 中直接传入 `doGatherShapeN(...)` 生成 `E/B/S` 采样值；而废弃的 `raw_fields` 现在唯一合法迁移路径就是 `interp_order = 0`。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 [parameter-map.md](/Volumes/PHILIPS/programs/PIC/PIC-tutor/docs/parameter-map.md) 里 `FieldProbe.integrate / do_moving_window_FP` 继续压到更细的运行语义：本轮补清了 `integrate` 不只控制 `ComputeDiags()` 中的 `E/B/S += instantaneous * dt` 积分累加，还会改写 header 单位，并形成“每步持续累加、到输出 interval 才真正打包写出”的调度分离；同时补清了 `do_moving_window_FP` 的位移不是固定单步 `dt*v`，而是按 `step - m_last_compute_step` 累积得到 `move_dist`，再进入 probe 粒子位置更新与后续场采样链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `amrex.async_out / amrex.async_out_nfiles` 继续压到更具体的本地 writer 邻接链：本轮补清了它们虽然仍是 AMReX-owned input，但在 WarpX 本地只会邻接到 `FlushFormatPlotfile::WriteToFile()` 的 `amrex::WriteMultiLevelPlotfile(...)` 路径，而不会影响 `FlushFormatCheckpoint` 中逐个 `VisMF::Write(...)` 的 checkpoint 写盘链。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
- 2026-05-18：继续推进阶段 A 总索引人工收口，已把 `amrex.abort_on_unused_inputs / amrex.use_profiler_syncs` 继续压到更精确的 startup 分界：本轮补清了它们在 WarpX 本地都只邻接 `amrex_init() -> amrex::Initialize(..., overwrite_amrex_parser_defaults)` 这一层，而 `overwrite_amrex_parser_defaults()` 自身并不会读取或覆写这两条；其中 WarpX 唯一真正协调的 profiler 同步项仍是 `tiny_profiler.device_synchronize_around_region` 与 `warpx.do_device_synchronize` 的关系。当前 `parameter-map` 剩余工作已进一步收缩到更零散的 grouped alias、AMReX 邻接 pass-through，以及少量 still-coarse 的 diagnostics / reduced-diags / particle-output instance-local 条目。 
