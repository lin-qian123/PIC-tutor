#!/usr/bin/env perl
# latexmk configuration for the PIC-tutor native LaTeX book.
$pdf_mode = 1;                          # xelatex -> PDF
$xelatex = 'xelatex -interaction=nonstopmode -halt-on-error -file-line-error';
$bib_mode = 0;                          # no bibliography in the Phase 1 sample
$max_repeat = 6;
$clean_ext = 'aux fdb_latexmk fls log out toc nav snm vrb';
