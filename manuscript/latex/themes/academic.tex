% PIC-tutor — academic theme (A4, print-first), Phase 1 default.
% Mirrors the Pandoc-era academic.tex look: A4, 30/24mm inner/outer margins,
% page number on the outer head, running title on the inner head.

% Page geometry is set in preamble/layout.tex; themes only adjust appearance.
