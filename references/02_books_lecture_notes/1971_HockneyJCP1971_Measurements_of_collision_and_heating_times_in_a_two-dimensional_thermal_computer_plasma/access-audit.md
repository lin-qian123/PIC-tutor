# Hockney 1971 access audit

## Sources checked

| source | result |
|---|---|
| IBM Research publication record | Abstract and bibliographic metadata available |
| DOI landing page | DOI identity recorded; publisher full text not materialized locally |
| Local `references/` and common document locations | No local PDF or MinerU output found |

## Bounded abstract evidence

The author-affiliated IBM Research record identifies the 1971 paper and exposes an abstract covering:

- NGP, CIC, HNGP and HCIC particle models;
- collision-time scaling with density and particle width;
- electric-field-energy fluctuation scaling;
- an optimum `omega_pe Delta t` path in the `H/lambda_D` plane;
- reported `K_2` values for the four models and cost-based model ranges.

These are abstract-level claims. They are not a substitute for checking the paper's derivation, figures, notation, or numerical tables from the full text.

## Access boundary

- `publisher_pdf`: missing in the current workspace;
- `mineru_markdown`: not generated because the full PDF is absent;
- `full_text_line_by_line_compare`: not completed;
- `abstract_backed`: true;
- `metadata_verified`: true.
