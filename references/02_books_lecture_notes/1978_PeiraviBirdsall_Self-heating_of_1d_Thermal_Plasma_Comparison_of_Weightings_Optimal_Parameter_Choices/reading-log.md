# Reading Log

## 2026-07-13 第一轮

- 从 UC Berkeley Digital Collections record `137351` materialize `ERL-m-78-32.pdf`。
- PDF 共 40 页；MinerU 已完成 Markdown、表格和图片资产转换。
- 本轮覆盖摘要、正文 Sections I--VII，以及 Appendices A--C 的结构和主要结论。
- 已确认最值得回链 PIC-tutor 的量：
  - `tau_h` 是热能翻倍时间；
  - `omega_p tau_h` 在固定 `lambda_0/Delta x` 下与 `N_C+N_D` 近似成正比；
  - NGP、CIC-PIC、QS 的最佳 `v_t Delta t/Delta x` 不同；
  - 高阶 weighting 延长自热时间，但必须用每粒子每步成本折算 gain；
  - k-space truncation 会进一步延长 `tau_h`，其收益随 weighting order 增强；
  - thermal loader 的一阶/二阶矩修正会改变初始噪声和早期 heating slope。
- 证据边界：当前可引用的是机构技术报告全文，不把它包装成 publisher journal PDF，也不把 OCR 生成的复杂表格数字当作未经视觉复核的精确数据。
